# 設計の自己検証に使う。CI（.github/workflows/checks.yml）と同じものを入れる。
#
# **手で並べない。** tools/ が実際に import している外部モジュールから導く。
# 最初 grep で並べたら matplotlib が抜けており、CI だけが落ちた
# （ローカルには入っていたので気づけなかった）。
#
#   .venv/bin/python3 -c "..."  で import を走査して更新する
build123d==0.11.1
trimesh==5.0.0
numpy==2.5.1
matplotlib==3.11.1
pillow==12.3.0
PyYAML==6.0.3
pytest==9.1.1

# **import 名では出てこないが、無いと落ちる。**trimesh の `mesh.contains()`
# （電源スイッチの受けが中空かを見る検査）は空間索引に rtree を要る。
# 手元には入っていて CI だけが落ちていた——**matplotlib と同じ轍で 2 度目。**
# 走査（test_requirements）は直接の import しか見ないので、ここは手で足す。
rtree==1.4.1
