CONFIG_ZMK_KEYBOARD_NAME="HHKB Split"
CONFIG_ZMK_SPLIT=y

# シフトレジスタは SPI 経由で駆動するので必須。
CONFIG_SPI=y

# 深いスリープ。理由と検証は左側の .conf に書いた。
# 周辺側も同じ時間で寝かせないと、片方だけ電池が減る。
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=1800000
