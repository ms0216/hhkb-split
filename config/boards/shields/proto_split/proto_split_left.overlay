/* 左（セントラル）。列 0・1 を使うので col-offset は不要。 */
#include "proto_split.dtsi"

/* ⚠️ [一時的] ログを UART（D6）へ。**電流を測るときは外す（+0.46mA 乗る）。** */
/ {
	chosen {
		zephyr,console = &uart0;
	};
};

&uart0 {
	status = "okay";
	current-speed = <1000000>;
};
