"""買う部品。**種類 → LCSC の部品番号**。

export_fab.py（KiCad の Python で動く）と検査（venv で動く）の両方が読む。
**pcbnew を import しないこと。**

番号を埋めるときは、JLCPCB の在庫ページで現物を確認してから書く。
型番だけ合っていても、パッケージが違えば実装できない。フットプリントは
pcb/ に出ている実物と突き合わせること。

「基本部品(Basic)」なら段取り費がかからない。「拡張部品(Extended)」は
1 種類あたり段取り費がかかるので、種類を増やすほど高くなる。
"""

from circuit import WIRE_PAD_KINDS

# 基板に載らない部品。リード線で繋ぐもの（電池・電源スイッチ）と、
# 利用者が挿す XIAO。**BOM にも CPL にも出さない。**
NOT_ASSEMBLED = set(WIRE_PAD_KINDS) | {"xiao_nrf52840"}

# 種類 → LCSC の部品番号。**フットプリントは pcb/ の実物と突き合わせて選んだ。**
#
# ## 費用の構造（2026-08-08 調べ）
#
# 部品の単価より **段取り費**が効く。JLCPCB は部品を 3 つに分ける。
#
#   Basic     … 段取り費 $0
#   Preferred … 段取り費 $0（**Basic と同じ扱い。ここを見落としやすい**）
#   Extended  … **1 種類あたり $3**（約 ¥450）
#
# 半田付けは 1 点 $0.0017。左は約 150 点で $0.26 なので、**点数は誤差**。
#
# ## 埋まっていないもの（発注ページで確認して埋める）
#
# **cap_100u と ffc_12p。**どちらも Basic の一覧に無く、番号を外から
# 確定できなかった。**推測で書くと、パッケージ違いで実装できない。**
#
# ### ffc_12p（調べた結果、**安い互換品は使えない**）
#
# **最初に「Hirose は $1.79、互換品なら ¥3,700 安くなる」と書いたのは誤り。**
# $1.79 は数量 1 の値段だった。数量 100 なら FH12 級は $0.21〜0.49 で、
# 15 個でも ¥1,100 程度。差は ¥1,000 ほどしかない。
#
# 安価品 JUSHUO AFC01-S12FCA-00（C262661・$0.064・在庫 99,485）を調べたが、
# **ランドパターンが違う。**データシートの推奨パターンは縦の総寸法 4.13mm、
# FH12 は 5.0mm（信号 y=-1.85・取付 y=+1.40）。**差し替えられない。**
# KiCad の標準ライブラリに AFC01 のフットプリントは無い（AFA07 は 1.0mm
# ピッチで別物）。Amphenol F32Q/F32R も取付パッドが (±5.0, -1.775) で違う。
#
# 自分でフットプリントを起こす手はあるが、寸法図が図形で数値を機械的に
# 抜けず、**間違えると基板が使えなくなる。**¥1,000 のために冒す risk では
# ないと判断した。
#
# **ただし在庫は要確認。**FH12-12S-0.5SH(54) は製造中止で、後継は
# FH52K-12S-0.5SH。LCSC に 12 極の FH12 があるか確定できなかった。
# **無ければフットプリントごと変える必要がある。発注前に必ず見ること。**
#
# ### 74HC595 は TSSOP-16 のまま
#
# **Basic の一覧に TSSOP-16 の 74HC595 は無い**（SOIC-16 の C5947 はある）。
# もし発注ページで Extended だったら $3 かかる。SOIC-16 へ替えれば Basic に
# できるが、縦向きだとコートヤード 10.40mm で帯 9.25mm に入らない。
# **90 度回せば 7.40mm で入る**（余裕 +0.92mm。実測済み）。ただし幅が
# 6.4→10.4mm に増えて隣と 0.4mm 重なり、配線し直すと右基板に違反が 9 件
# 出た。**Preferred なら $3 はかからないので、まず区分を確認すること。**
# 確認せずに DRC 0 の基板を崩す価値は無いと判断した。
PARTS = {
    # --- 段取り費 $0（Basic と確認済み）---------------------------------
    "diode":     {"lcsc": "C81598", "desc": "1N4148W SOD-123 75V 150mA"},
    "schottky":  {"lcsc": "C8598",
                  "desc": "B5819W SOD-123 ショットキー 40V 1A"},
    "cap_100n":  {"lcsc": "C49678",
                  "desc": "0.1uF 0805 50V X7R (CC0805KRX7R9BB104)"},
    "res_1M":    {"lcsc": "C17514",
                  "desc": "1MOhm 0805 1% 1/8W (0805W8F1004T5E)"},

    # --- 区分の確認が要るもの -------------------------------------------
    # Basic の一覧には無い。Preferred なら $0、Extended なら $3。
    "74HC595":   {"lcsc": "C3007949",
                  "desc": "74HC595 TSSOP-16 (HGSEMI 74HC595-50MT/TR)"},
    # ホットスワップソケットは Extended しか無い（$3 は避けられない）。
    # 単価 $0.069 x 61 個 = $4.2/セット。
    "keyswitch": {"lcsc": "C5184526",
                  "desc": "Kailh CPG151101S11-1 MX ホットスワップソケット"},

    # --- まだ番号が無いもの（発注ページで確認して埋める）-----------------
    "cap_100u":  {"lcsc": None,
                  "desc": "100uF 1206。**6.3V の MLCC は 3V で容量が半減する**"},
    "ffc_12p":   {"lcsc": None,
                  "desc": "FFC 12P 0.5mm 下接点 右アングル（FH12-12S-0.5SH 互換）"},
}
