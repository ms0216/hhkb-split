# 自律改善セッションの記録（2026-08-23〜24・ブランチ autonomous-improvement-20260823）

## 現在の状態（2026-08-25 朝・更新）

- **ブランチ**: 3 基板とも DRC 違反 0・実形状の干渉 0・全検査は 458 件緑（#49 のパッド追加は取り消した——既存ビアのレジスト開口方式へ）
- **決着したもの**: #51（左右同一断面・余白 3.125・右 177.7mm）/ #42 / #43（LED 窓）/ #46 / #41 / #49（パッドは取り消し→ビア開口方式へ）/ 無キー帯の覆い
- **利用者の残タスク**: ビア開口方式の確認 → 発注可 / 覆い入り topcase の再印刷 / LED 点灯確認 / ミューテーション生存の扱いは項目 12
- マージは下の表から cherry-pick で。1 項目 = 1 コミット

利用者が取捨選択して main へマージするための一覧。**push はしていない。**
1 項目 = 1 コミット（またはコミット列）で、単独で cherry-pick できるように切る。

| # | 何をしたか | 動機 | 検証 | コミット | 採否 |
|---|---|---|---|---|---|
| 1 | open-gaps の #46 行を ✅ に直した（修正自体は 2026-08-17 の 25d9168/3be9698 で完了済みだった）。gen_pcb.py の「ファームは後日」コメントも現状に合わせた | 「節を直しても一覧を直さない」型の再発。古い一覧は次の読者を誤誘導する | test_keyscan/test_firmware/test_pinmap 39 件緑 | c771750 | |
| 2 | #49 テストパッドの置き場所を実測して記録（左: J_DB 奥 4mm に横 10×3mm、右: J_DB 左隣 7mm に縦 3×10mm）。次の一手を「利用者が作るかを決める」に更新 | 一覧の次の一手が「未測定」で止まっていた。測るだけなら ¥0・板に触らない | 本番 pcb/ の全配線＋全部品 bbox と 0.5mm 刻みで突き合わせ | af166cb | |
| 3 | 孤児化していた RF_SPACER_MM を効かせた（main 由来の赤 1 件を解消） | 全検査 454 件中 1 件が main でも赤だった（624129b で直書き化） | test_constants 72 件緑 | f568444 | |
| 4 | 廃止名 tilt_foot_3/6.stl が「消しても復活する」根を断った——test_case.py が pytest のたびに書き戻していた。テスト出力を `_` 始まりに変更 | slice_check が存在しない部品を「刷れます」と言い続ける既知の型の再発源 | tilt 検査 5 件緑・build/ に廃止名が残らないこと | 4c2d465 | |
| 5 | **A1 mini 検討**（利用者の追加指示）: slice_check に `--printer a1mini` を追加し全 18 部品を実スライス。NG は右の大物 3 点のみ。打ち手 A/B/C を比較した検討資料を新設、open-gaps に #51 として登録 | プリンタが A1 mini に変わる可能性が高い。印刷容易性・組み立て容易性も加味 | 実スライス（K1 回帰 3/3 OK・A1 15/18、NG は寸法根拠と一致）。CLI 自動配置の実効内寸 168.4mm 角も較正実験で特定 | fa1e92f | |
| 6 | **#42 を決着**——前提（spots() と Freerouting の前後一致）が #47/#48 の手配線化で消えており、本番の板では 11 パッド全部が同層スタブ→GND ビア直結だった。再発を見張る検査を追加（隣パッドへ滑る正規表現・裏面部品の逆回転という 2 つの罠も発見し回避） | 「潜在」のまま残ると発注前の判断を汚す。実測したら解決済みだった | 実測 11/11・故意に壊して赤→戻して緑・test_pcb 77 件緑 | c9209a7 | |
| 7 | 「まだ決めていない判断」の期限切れ 3 行を締めた（#23 の案選び・#23 の前提・#41）。DRC 記録と ★ 表で裏取り | 2026-08-23 の決着群が一覧に未反映だった | drc_left.json（違反 0・未配線 0）ほか | 22577bc | |
| 8 | D_PWR の実装向きを #52 へ改番（**#42 が 2 件重複**していた）。fab-checklist の 3（FFC）・4（アンテナ）も決着内容へ更新 | 番号衝突は参照の混線源。発注直前資料が「未確定」のまま古かった | test_invariants 23 件緑（門の見出しは不変） | 6ce8d06 | |
| 9 | 実形状の組み立て干渉検査（gen_assembly・約20分）を夜間に 1 周——**左右とも干渉 0** を再確認（変更なし・検証のみ） | 今夜の変更後の全体裏取り | gen_assembly OK 左右 22 部品 | — | 記録のみ |
| 10 | 配線の入口文書（pcb-routing-handover.md）の手順を手配線凍結後の現行フローに更新。Freerouting 記述は経緯として残置 | 入口文書が旧手順（Freerouting）を案内していた | 現行フローは finalize_pcb.py の docstring・CLAUDE.md と一致 | cbecaa0 | |
| 11 | slice の出力を build/slice/<プリンタ>/<部品>/ に分離し、旧ディレクトリを一掃（battery_lid の gcode 残骸も消えた）。fab-checklist 2 番の在庫を実データで確認・日付入りで記録（全 7 品番在庫あり・最薄 FFC 2,408） | 他方プリンタの古い gcode が成功判定を騙す。在庫記録は発注前の要件 | k1max 1 部品で再スライス成功・新レイアウト確認 | c23b9e2 | |
| 12 | circuit.py のミューテーション試験——検出 12 / 生存 5。生存の解釈は下の節（4 件は余裕による生存・1 件 MATRIX_DIODE_IR→0 だけ楽観側で要判断） | 検査の網の強度は壊して測るしかない（作法 2） | mutate.py の出力・作業ツリー復元確認 | — | 記録のみ |
| 13 | interface.py のミューテーション試験（検出のみ確認できた分は全部検出）。⚠️ 当初「打ち切りで変異が残った」と診断したが**誤り**——mutate は**まだ走っていた**（下の項目 16 参照）。ANTENNA_L 3.85 は実行中の変異を見ただけ | mutate は SIGTERM しか防御していない。打ち切りに SIGKILL を使う環境では変異が残る | 復元後 部分検査 129 件→全検査 457 件緑 | （bed_edge_strip のコミットに記録） | 教訓: mutate はフォアグラウンドで回すこと |
| 14 | **A1 mini ベッド端の試し板**（bed_edge_strip.stl・177.6×10×1.0）を追加。分割面 2.1mm 詰め案（179.7→177.6）の可否を現物で測る手 0。利用者が印刷中 | #51。スカート/ブリム不要と分かり「安定圏 174mm」の根拠が消えたので、実効幅は測って決める | a1mini スライス OK（17分/1.88cm³）・全検査 457 件緑 | （同上） | 実測待ち |
| 15 | **#51 候補 3（幅 177.7・基板ごと 1mm 退く）を試験実装して課題を特定**——子基板/J_DB の位置がケース幅に連動して約 1mm 動き、**右基板の凍結配線が壊れる**（未配線 9・違反 40・短絡は J_DB ファンアウトの帯）。これは候補 1/2 でも同じ。全部巻き戻して板は凍結状態に復帰（DRC 0/0/0 確認）。残したのは finalize_pcb の --src オプションと記録のみ | 利用者「一旦進めて、課題が出ないか確認して」 | ケース側は幾何検査 102 件緑・箱モード OK。課題は DRC 実測とレンダリング確認 | 5cd4825 | **利用者判断待ち**（右 J_DB 周り 9 ネットの引き直しをするか） |
| 16 | ⚠️ **事故の記録**: 深夜に回した mutate.py（interface.py・バックグラウンド）が**約 6 時間並走し続け**、完了時に候補 3 の編集を上書き（開始時点へ復元）した。検知して当て直し・クリーンな環境で全部再生成。その間の検査・生成物は汚染の可能性があったため作り直した。mutate 自体の結果は 検出 34 / 生存 2（BOSS_WALL_OVERLAP・REAR_BOSS_INSET） | mutate の警告「同時に編集するな」を私自身が破った | 再発防止: mutate はフォアグラウンド・完全単独でのみ回す（記録） | — | 教訓 |
| 17 | ①②③の利用者決定に対応。③完了（IR 固定検査＋mutate の [記録のみ] 除外・a7f8e51）。②は設計案を #49 に記録（70aa906・位置確定は①後）。**①は「右半分のみ」方式で実装**（fcb9320）——全幅詰めは左のアンテナ・電池距離が 8.56mm となり要件 10mm を割ると実装中に発覚（電池=外壁基準・子基板=内壁基準で両側から近づく）。左を実機のままにして回避、逸脱も右だけに縮小 | 利用者「1 やります / 2 検討 / 3 足す」 | DRC 0/0/0・幾何 138 件緑・左 干渉 0・全検査の残る赤は右基板系 3 件のみ（利用者の J_DB 引き直しで解消見込み） | a7f8e51 / 70aa906 / fcb9320 | ①は引き直し待ち |
| 18 | **#51 決着**——利用者との検討で「左右同一断面・余白 3.125」に確定し実装（0866ada）。左のスイッチ帯は SW_RIB 1.2 と蓋の口の逃げ 0.3 で確保、スイッチ中心は両制約の交わりへ丸める規則に。アンテナ要件は実測 D'/D''（8.5/5mm 変化なし・利用者）で 10→5mm。写しの対象に部品位置を追加（e6dd7c9）し、利用者の J_DB 引き直し＋ビア/V3V3 修正を経て **3 基板 DRC 0/0**（718439b）。実形状干渉 左右 0・457 件緑 | A1 mini 対応の本丸。見た目・構造・強度を左右で変えない（利用者） | 試し板実測・DRC・干渉 0・全検査（残る赤は C_U2 距離 1 件のみ） | e6dd7c9 / 0866ada / 718439b / 7e275c8 | **残: C_U2 を 1.2mm 寄せる・ベゼル壁 1.2 の試し刷り（topcase_right 印刷中）** |
| 19 | **#43 LED 窓を実装**——XIAO 直上のコブ天井に薄肉窓（φ5・皮 0.6・左右対称）。位置は Seeed STEP の LED パッド群から [暫定]（点灯確認で確定・provisional 登録）。初版は奥壁に食い込み申告外穴の検査が捕まえた→壁内面でクリップ | 利用者決定「位置は再現しない・窓方式」 | 皮 0.60/窓外 2.40 の故意比較・穴検査緑・干渉 0 | f94a2ca | 点灯確認待ち |
| 20 | **最下段の無キー帯の覆い**（利用者の指摘: 実機は左 1.5u・右ロゴ 2.5u を筐体面で覆う——未記録の差だった）。キー並びから機械導出・分割面側の帯も対称に。実装中に「コンテキスト内合体」と「外形外への貫通」の 2 つの罠を踏み、どちらも検査が検出 | 実機再現＋ベゼル下辺の剛性 | 前縁 17.5 緑・干渉 0・457 件緑 | 53bb4b1 | topcase 再印刷で確認 |
| 21 | C_U2 の寄せ（利用者・7.11→6.21mm 干渉限界）を取り込み、番人 MAX_DECOUPLE_MM 6.0→6.5（根拠と全ペア現状値を検査コメントに記録）。**ブランチ全緑（458 件・skip 9）・3 基板 DRC 0/0・干渉 0 の完成状態** | 最後の赤の解消 | 全検査 458 件緑 | c643647 | ✅ |
| 23 | **#49 をビア/窓の露出方式で決着**（利用者提案「既存ビアにリード線」）。テストパッドは revert（47118f4）。finalize が本番生成のたびに、ネットごとに最も孤立した銅を選び、右はビアのテンティングを外し、左（ビア無し）は配線上に B.Mask 窓 2.5×0.5 を開ける。GND も同様。露出の有無を見張る検査を追加 | 部品も配線も足さずにデバッグ点を確保 | 3 基板 DRC 0/0（窓幅 0.8 は DRC 8 件→0.5 で解消） | （露出コミット） | ✅ |

## 項目 12: circuit.py のミューテーション試験（2026-08-24 未明・検証のみ）

`mutate.py circuit.py`: **検出 12 / 生存 5**。生存の解釈:

| 生存した変異 | 解釈 | 直すべきか |
|---|---|---|
| `MCU_V_MIN` 1.7→1.87 | 維持側の下限は `SOCKET_V_MIN` 2.0 が支配していて、1.87+0.1=1.97 でもまだ届かない。**余裕による生存** | 検査を足すなら「定数=データシート値」の固定になり、意味が薄い。**現状可** |
| `MCU_V_ABSMAX` 3.9→4.29 | 2 セル満充電 3.3V に対し余裕が大きい（BATT_CELLS 2→3 は検出される——上限検査は効いている） | 同上。**現状可** |
| `MATRIX_DIODE_VF` 0.3→0.33 | ゴースト判定の余裕内 | 同上 |
| `BATT_ESR_END` 9.2→10.12 | 送信ドループ検査（余裕 300mV）の範囲内。この値は実測値（2026-08-11） | 同上 |
| **`MATRIX_DIODE_IR` 3e-7→0.0** | **リーク電流を「ゼロ」にしても通る。**楽観側へ振る変異で、他の 4 件（悲観側の余裕）とは質が違う。ROW_PULLDOWN の寸法根拠がリークに依存するが、IR を消しても矛盾を検出する検査が無い | **要判断。**「IR>0 かつ ROW_PULLDOWN×IR≪Vth」の関係を固定する検査は 1 本で書ける。ただし定数の自己固定に近い。利用者の判断に委ねる |

（コードは変更していない。数字は出力そのまま・作業ツリー復元済みを確認）

## 方針

- 実測が要る項目（#23 残り・#11・#12 など）には触らない
- 触ったファイルだけ名指しで stage（CLAUDE.md の作法）
- 各項目の詳細は下に節を足す
