# 改善バックログ（2026-08-13 監査・引き継ぎ）

3 回の読み取り専用監査（リポジトリ全域／decisions 整合／firmware・CI・検査品質）で
見つかった残作業。**上から順に着手すること。**各項目に根拠と具体的な手順を書いた。
証拠の行番号は 2026-08-13 時点。着手前に grep で現物を確かめること。

**済んだもの（この日に修正・コミット済み。二重にやらないこと）:**
OrcaSlicer ログの掃除／check_zmk_config の CI 組み込み／slice_check の stale 赤化と
孤児 STL 削除／README・CLAUDE.md の古い数字／テスト名衝突 3 組の改名／
preview_parts・preview_layout の削除／SVG 4 枚の生成器名の逆引き／
右 9 列・595×2 の決定記録への伝搬／gen_pcb・circuit の嘘コメント／
shopping-list の -G 統一と死んだ C&K 行／cost-estimate の丸めと「4 割」表現。

**2026-08-13 に、この文書の 1〜9 を全部やった（下記「済ませた 9 件」）。
残っているのは 10 番（利用者の判断・実測待ち）だけ。**

## 作法（このリポジトリの前提）

- 着手前に `CLAUDE.md` を読む。特に: **名指しで stage・`git add -A` 禁止・
  コミット前に `git diff --cached` を読む**
- **検査の実行中にファイルを編集しない**（偽の赤/緑が出た実績あり）
- 修正したら **故意に壊して検出されること**を確かめる（規則 2）
- 全部終えたら `.venv/bin/pytest tools -q` を 1 回（約 14 分・448 件）

---

## 10. 【判断待ち・エージェントは着手しない】

- **open-gaps #23（アンテナ）**: 手 0（アルミ箔で RSSI）は利用者の実測待ち。
  発注はこれが門で止まる（export_fab.py）
- ~~**#39 C_RAIL の置き場所**~~: **2026-08-14 決着（削除）。**残りは
  跡地の浮き GND ビア 1 本 → **open-gaps #40**（大きな配線変更と併せて確認）
- **SW_PWR_* の実測**: 部品が届いたらノギス。データシート公称との照合は
  2026-08-13 に済み（端子長 5.0 = 図面 8.5 − 本体 3.5。食い違い無し）

---

## 済ませた 9 件（2026-08-13。**戻さないこと**）

1 項目 = 1 コミットで入れた。**各項目、故意に壊して検出されることを確かめてある。**

| # | 何を直したか | 故意に壊した確認 |
|---|---|---|
| 1 | `battery_alkaline.c` の負 ADC 読み。負を 0 に落とし、乗除を uint32 で受けて `UINT16_MAX` で飽和 | CI の ZMK ビルドが緑（手元に Zephyr SDK は無い） |
| 2 | `Kconfig` の `..._SOFT_OFF_SAMPLES` に `range 1 255` | 同上 |
| 3 | `build.yml` に `permissions: contents: read` と `paths:` | tools だけの push で ZMK ビルドが**走らないこと**を実測。`build.yml` を含む push では走った |
| 4 | 発注の門の検査を自己参照から実物の挙動へ（`export_fab_gate.py` に判定を分離） | 見出しを 1 字変える→赤／行頭固定 `^…$` を外す（2026-08-11 の壊れ方）→赤。**旧検査は後者を緑で通していた** |
| 5 | `rtree` を `mesh.contains()` の挙動で守る検査を追加 | `pip uninstall -y rtree`→赤／**`requirements-dev.txt` の行を消す→この 1 件だけが赤**（他 4 件は緑。塞ぎたかった穴そのもの） |
| 6 | `hhkb_split.dtsi` の旧構成コメント 3 件（右 9 列・15 列・ngpios の既定） | **コメントが主張する数を壊しても誰も気づかなかった**ので `test_keyscan` に 2 件追加。追加後は `columns <15>→<14>`・既定 `ngpios <8>→<16>` がそれぞれ赤 |
| 7 | 本番側の「74HC595」表記を 595（SN74LVC595A）へ。**治具と「なぜ 74HC を捨てたか」の記録は残した** | `circuit.py` の kind を `74HC595` に戻すと `test_circuit` が 5 件赤 |
| 8 | `refresh_view.sh` / `blend_assembly.sh` を `set --` ＋ `"$@"` へ | `"my half"` を渡すと旧形は 2 個に割れ、新形は 1 個のまま。引数なしの既定は両形とも同じ |
| 9 | `alk_channel_get` の「測った直後に読む」前提を driver 側に明文化（バックログの指示どおり、直さずに書いた） | 主張を現物で確認: `sensor_channel_get` の呼び手は `low_battery_off.c:63` の 1 か所だけで、読むのは電圧のみ |

**6 と 4 で分かったこと（次に効く）:** コメントや検査が「正しいことを
言っているか」は、**その主張を壊してみるまで分からない。**6 では散文が
主張する数（`columns`・既定 `ngpios`）を壊しても 46 件全部が緑のままだった。
**直すついでに、その主張を守る検査があるかを必ず見ること。**
