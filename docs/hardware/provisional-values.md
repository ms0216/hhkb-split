# まだ実測していない値

**推定で埋めた値と、確かめた値が区別できないと、どこがまだ危ないのかが
分からなくなる。** この案件では、タクトスイッチの向きも XIAO のピンピッチも
推定のまま断定し、どちらも利用者に指摘されて初めて誤りだと分かった。

ここは `[暫定]` と書かれた定数の一覧。
[tools/test_constants.py](../../tools/test_constants.py) が
**コードとこの表がずれたら落ちる**ようにしてある。書き漏らしも、
実測して確定したのに残っている行も、どちらも検出される。

---

## 一覧

| 定数 | 現在値 | 何の値か | どうやって確かめるか | 外したときの影響 |
|---|---|---|---|---|
| `CAP_LIFT` | 6.2mm | プレート上面からキーキャップ底面まで（MX ＋ キャップ） | MX スイッチとキーキャップが届いたら測る | **全段のキートップ高さが実機とずれる。** ここから `PLATE_TOP_FRONT` を逆算しているので影響は全体に及ぶ |
| `CAP_H_HOME` | 6.7mm | ホーム段のキーキャップ自体の高さ | 同上 | 同上 |
| `SOCKET_DROP` | 3.2mm | Kailh ホットスワップソケットが基板下へ出る量 | ソケットが届いたらノギスで測る | 大きすぎればケースが無駄に厚い。小さすぎればソケットがケース床に当たる |
| `PLATE_TO_PCB` | 3.5mm | プレート下面から基板上面までの距離 | MX スイッチが届いたら、プレートに挿して測る | **キートップの高さが実機とずれる。** HHKB 再現の根幹 |
| `AA_TERMINAL` | 8.0mm | **裸の電池 2 本**の両端に付く電極バネと配線に要る、長手方向の余裕（合計） | **電極バネ**の実物を測る。**電池ボックスを測っても分からない**（下記） | 足りないと電池が入らない。ケースの作り直し |
| `XIAO_L/W/H` | 21.0 / 17.5 / 5.0mm | XIAO 単体の外形（ピンソケット込みの概略） | 手元の実機をノギスで測る（**今日できる**） | 子基板の大きさとケースの切り欠きがずれる |
| `DB_STACK_H` | 4.0mm | 子基板の上面から XIAO の頭（USB コネクタ含む）まで | 同上 | USB-C の穴の高さがずれ、ケーブルが挿さらない |
| `SW_PWR_W` | 4.3mm | 電源スイッチ本体の幅（背面パネルに沿った左右） | 買う製品を決め、データシートの外形図から入れる。届いたらノギスで確認 | **左半分の空きは 10.6mm しかない。**超えると入らない |
| `SW_PWR_D` | 8.6mm | 同・奥行（パネルの裏へ入る量） | 同上 | コブの内寸 15.6mm を超えると入らない |
| `SW_PWR_H` | 4.0mm | 同・高さ（上下） | 同上 | コブの内部 15.5mm を超えると入らない |
| `FFC_LENGTH` | 100mm | 本体基板と子基板をつなぐ FFC の長さ（0.5mm ピッチ 12 芯） | 実際に買う製品を決めて、その長さを入れる | 短いと届かない。**組み立て時まで気づかない** |
| `FFC_SLACK` | 25mm | 直線距離に足す余裕（垂直の落差・両端の曲げ・抜け止め） | ケースを 1 台組んで、実際の引き回しで測る | 足りないとケーブルが張り、コネクタが抜ける |

---

## ⚠️ `AA_TERMINAL` は「電池ボックス」の寸法ではない（2026-08-08 に取り違えた）

**この表は以前「電池ボックスが届いたら測る」と書いていた。**
`AA_TERMINAL` の定義は「**電極バネ**と配線に要る余裕」なのに、測る対象が
「箱」になっていた。**定義と測定対象が食い違っていた。**

その結果、買い物リストに「電池ボックス 単3×2」と書いてしまい、
**設計に入らない横並び型（58 × 32mm）を買う**ことになった。
コブの内寸は 15.6mm しかない。

### いまの設計は「箱を使わない」

`envelopes.battery_envelope` と `gen_case` は、**裸の単3 を 2 本、
一直線に寝かせて**ケース自身で保持する形。

```
電池は 手前=仕切り壁 / 左右と奥=側壁 / 下=蓋 / 上=基板 で保持される
```

占有は **109 × 15.5 × 15.5mm**。必要な部品は**電極バネとリード線**だけで、
プラスチックの箱は買わない。

### 電池ボックスを使うなら

**箱の厚みぶん電池室を広げる必要がある。**一直線型（110 × 17 × 17mm 級）
なら +1〜2.5mm。**横並び型（58 × 32mm）は入らない**（コブを 17mm
深くすることになり、ケース奥行が 125 → 142mm。実機 120mm から +22mm）。

**どちらにするかは未決定**（open-gaps #22）。買う前に、実物の外形寸法を
`envelopes.py` に入れて `gen_assembly.py` を通すこと。

---

## 確定させる順番

1. **`XIAO_L/W/H` と `DB_STACK_H`** — 手元に XIAO があるので今日測れる。
   子基板の設計とケースの USB 切り欠きが、これで固まる
2. **`AA_TERMINAL`** — **電極バネ**の到着後。電池ボックスではない（下記）
3. **`SOCKET_DROP`** — ソケット到着後
4. **`PLATE_TO_PCB`** — MX スイッチとキーキャップ到着後。
   ここが決まると `PLATE_TOP_FRONT` を動かしてホーム段 31.6mm に合わせられる

---

## 確定させたときの手順

1. 実測値を `tools/envelopes.py` に入れ、タグを `[暫定]` → `[確定]` に変える
2. **この表から行を消す**（消し忘れると `test_the_provisional_list_has_no_stale_entries`
   が落ちる）
3. 根拠（測り方と測定値）を [dimensions.md](dimensions.md) に書く
4. `pytest tools` と `python3 tools/gen_assembly.py` を通す
