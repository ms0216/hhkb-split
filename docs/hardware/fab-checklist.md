# 発注の直前に見るもの

**ここに記録が無いと `pytest` が落ちる。**製造ファイル（`pcb/fab/`）が
存在するのに、下の各節が書かれていなければ
`tools/test_invariants.py` が止める。**黙って発注できないようにしてある。**

書いたら消さないこと。次に発注するときの記録になる。

---

## 発注履歴

| 日付 | 対象 | 実装枚数 | 表面処理 | 備考 |
|---|---|---|---|---|
| **2026-09-03** | **左・子基板**（右は未発注） | 左 5 / 子基板 5 | 有鉛 HASL | §1 の目視確認済みで発注。注文番号は非公開（このリポジトリは公開）。右は左の検証を経てから。**2026-09-04: JLC エンジニア調整後の「Confirm Parts Placement」図を拡大して再照合**——左 D1/D11/D12 の帯が下端・U1 の Pin 1 が右下・J_DB 口が奥、子基板 D_PWR の帯が左端・J_MAIN 口が手前。発注前の図・KiCad のパッド座標と全一致。子基板 Assembled 4 / Unassembled 0 |

---

## 0. 発注フォームで選ぶもの（**ファイルには入っていない**・2026-09-02）

ガーバー・BOM・CPL に書かれず、**発注ページで人が選ぶ値**。ここに無いと
毎回思い出すことになる。

| 項目 | 値 | 根拠 |
|---|---|---|
| 層数 / 厚さ | **2 層 / 1.6mm** | `envelopes.PCB_T = 1.6`。ケース・プレート・ソケットの落ち込みが全部この値で計算されている。**1.2 や 2.0 を選ぶとプレートからソケットまでの寸法が狂う** |
| 銅厚 | 1oz（既定） | `pcb_rules.JLC` は 1oz の能力値 |
| 表面処理 | **HASL（有鉛・¥0）**。無鉛 HASL は +¥222（2026-09-03 実測）。ENIG は任意 | 最細ピッチは FFC の 0.5mm。HASL で実装実績のある範囲。板はケースの中で、部品はんだは JLC が付けるので有鉛で機能上の差なし（2026-09-03 に利用者が有鉛を選択） |
| 注文番号の印字 | **Remove Order Number** を選ぶ（または任意の場所） | 選ばないと JLC が空いた所にシルクで番号を刷る。表面はキーの下で見えないが、裏面のシルクに重なりうる |
| 実装面 | **Bottom（裏面のみ）** | 3 枚とも SMD は全部 B.Cu。F_Paste は空（→ 1b） |
| 実装枚数 | 主基板は板 5 枚のうち **2 枚**を実装（→ 5.）。**子基板は 5 枚全部**（1 台に 2 枚要るので 2 枚だと予備 0。部品が安く差額は数百円。2026-09-03） | |
| 「Confirm Parts Placement」 | **Yes** | 第 1 節の目視はここで出るプレビューでやる |
| PCBA の区分 | **Economic**（Standard を選ばない） | Standard は部品〜板端 ≥2.5mm と 70×70mm 以上が条件。実測: 左 D24・右 D9/D31 が板端 1.40mm、子基板 J_MAIN 0.55mm、子基板は 21×32mm。Economic は 0.3mm・10×10mm〜・片面で、3 枚とも B 面のみなので足りる（[JLC PCBA capabilities](https://jlcpcb.com/capabilities/pcb-assembly-capabilities)・2026-09-02 §8b） |

## 0b. ⚠️ 実際の発注道具（Fabrication Toolkit）を本番の板に通した（2026-09-02）

**`export_fab.py` で直しても、発注は別の道具で作る**（#52）。その道具
（bennymeg/JLC-Plugin-for-KiCad。`~/Documents/KiCad/10.0/3rdparty/plugins/`）
の CLI を **本番の板 3 枚に実際に通して出力を読んだ。**3 つ出ていた:

| 出ていたもの | 何が起きたか | 直し |
|---|---|---|
| **BOM の LCSC Part # が全行空** | 番号は `parts.py` にしかなく、板には無かった。JLC は値と footprint から自動で候補を出すが、**FH12・BAT46W・74LVC595 は別品が当たりうる** | フットプリントに `LCSC` フィールドを焼き込む |
| **ソケット 61 個が CPL で `top`** | プラグインは `fp.GetLayer()` で面を決める。**1b と同じ穴・道具違い** | `FT Layer Override = bottom` フィールド（プラグインが読む上書き） |
| **XIAO（U_MCU）が BOM と CPL に載る** | 利用者がピンソケットで載せる部品。JLC に実装させない | `exclude_from_bom` `exclude_from_pos_files` 属性 |

道具は `tools/fab_fields.py`。`finalize_pcb.py` と `gen_daughterboard.py` が
保存前に呼ぶ。**直した板でもう一度プラグインを通し、BOM 全行に番号・
CPL 134 点すべて `bottom`・XIAO 無しを確認した。**さらに
**プラグインの CPL（回転・座標・面）と `export_fab.py` の CPL を 134 点
突き合わせて差 0**——別の作者の実装どうしが一致した。

検査: `test_every_assembled_part_carries_its_lcsc_number_on_the_board` /
`test_parts_soldered_on_the_other_side_say_so_for_the_fab_tool` /
`test_the_xiao_is_not_handed_to_the_assembler`。**3 つとも故意に壊して
赤になることを確認した。**

**実際に回したコマンド（2026-09-03・左と子基板）**。`cli.py` は相対 import なので
**プラグインのディレクトリからモジュールとして**呼ぶ。出力先は `pcb/production/` 1 か所で
**板ごとに上書きされる**ので、1 枚回すたびにサブフォルダへ移す。最後に wxWidgets の
assert が出るが出力はできている（出来た物を見て判定する）。

```
cd ~/Documents/KiCad/10.0/3rdparty/plugins
KPY=/Applications/KiCad/KiCad.app/Contents/Frameworks/Python.framework/Versions/3.9/bin/python3.9
"$KPY" -m com_github_bennymeg_JLC-Plugin-for-KiCad.cli -p <repo>/pcb/hhkb_split_left.kicad_pcb -t -nI
mv <repo>/pcb/production/{bom.csv,positions.csv,designators.csv,netlist.ipc,hhkb_split_left.zip} <repo>/pcb/production/left/
```

発注ページへ上げるのは zip・`bom.csv`・`positions.csv` の 3 つ。`designators.csv` は使わない
（XIAO が載っているが BOM/CPL には無い）。

⚠️ プラグインは **`-t`（Apply automatic position/rotation translations）を
付けて**回すこと。付けないと TSSOP の 270° 補正が入らない（CLI では `-t`、
GUI ではチェックボックス）。

## 1. 配置プレビューで部品の向きを見る（**機械では確認できない**）

**KiCad のフットプリントの向きと JLCPCB のライブラリの向きは一致しない。**
`tools/export_fab.py` は共有されている補正表（matthewlai/JLCKicadTools）を
当てているが、**それが JLCPCB にとって正しいかは、こちらからは検証できない。**

JLCPCB の発注ページに部品を並べた**配置プレビュー**が出る。そこで見る。

| 見るもの | 何が起きうるか |
|---|---|
| **74LVC595（U1・右は U2 も）** | 補正 270 度を当てている。**ずれていたら 90 度単位で回っている** |
| **ダイオード 62 個（D1〜61 + D_PWR）** | **向きが逆だと 1 つも反応しない。**カソード（帯）が行の側。**2026-08-16 判明・未対応**: `ROTATION_DB`（出典 matthewlai/JLCKicadTools）に SOD 系の補正値が 1 件も無く、無補正のまま出している。同ツールの Issue #28 で「同じ SOD パッケージでも LCSC 品番により向きが違う」既知の限界が報告済み。JLCPCB の配置プレビューで D_PWR（B5819W・C7420330）を目視すると「+」マークが出るが、**これが Pin 1 を指すのか、KiCad 側のカソード位置と何度ズレているのかは未確定**（JLCPCB の UI にツールチップ等の裏付けが無い）。**この基板は `export_fab.py` を使わず、別の KiCad プラグインで CPL を作る運用**なので、このリポジトリの `ROTATION_DB` を直しても実際の発注には効かない。**発注前に必ず配置プレビューで目視し、カソード（帯）が V3V3 側に来ているか確認すること**（[open-gaps #52](open-gaps.md)。旧 #42・GND スタブの #42 と衝突していたので 2026-08-24 に改番） |
| **FFC コネクタ（J_DB・J_MAIN）** | ⚠️ **「向き」には 2 つの意味がある**（2026-09-01・利用者の指摘で分けた）。**①ピン配置は確認済み**——KiCad 公式フットプリントのピッチ 0.500mm・12 極が Hirose の公表スペック（12 極 / 0.5mm / 全長 10.1mm / bottom contact）と一致し、**J_MAIN pin n ↔ J_DB pin 13−n が 12 本すべて同じネット**（→ §7）。**②実装の回転だけが未確認**——補正表に無く、JLCPCB が何度回して載せるかはこちらから検証できない。**配置プレビューで見るのはこちら** |
| **ホットスワップソケット（SW1〜）** | **補正表に無い。**確認していない。61 個ある。⚠️ **2026-09-01 追加: 面も見ること。**このリポジトリの `export_fab.py` は**ソケット 61 個を `top`（表面）と書いていた**（→ 1b）。直したが、**実際の発注は別プラグインで作る運用**（#52）で、**そちらも同じ間違いを出していた（2026-09-02 に実際に通して確認・→ 0b で修正済み）**。**配置プレビューを裏面（Bottom）に切り替えて、ソケット 61 個がそこに並ぶことを見る。**表面に並んでいたら CPL の Layer 列を手で `bottom` に直す |

0805 と 1206 の受動部品は向きが無いので見なくてよい。

**2026-08-30: SOD-123 は 62 個すべて同じフットプリント**（マトリクスの BAT46W も D_PWR の B5819W も `D_SOD-123`・パッド 1=K）。**どれか 1 個で向きが読めれば、62 個すべてに同じ結論が使える。**

| 見る部品 | 面 | 回転 | 帯(K)が来る側 |
|---|---|---|---|
| 子基板 D_PWR | B.Cu | 180° | V3V3 側（+X） |
| 左 D1 / 右 D1 | B.Cu | 90° | ROW 側（+Y・手前） |

逆向きだと **D_PWR は電池で動かず**（USB では動く）、**マトリクスはキーが 1 つも入らない**。

### 確認したら、ここに書く

<!-- 下の見出しをそのまま残して、日付と結果を書くこと -->

**2026-09-03 確認済み（左・子基板。発注ページの 3D 配置プレビュー・Bottom 表示・利用者のスクリーンショットを私が KiCad の板のパッド座標と突き合わせた）**

前提: JLCPCB の Bottom 表示は**裏から見た絵**（X だけ裏返り、Y はそのまま。裏面シルク "SSKB LEFT" が正しく読める）。

| 部品 | KiCad の板（pcbnew でパッド座標を読んだ） | JLCPCB の 3D プレビュー | 判定 |
|---|---|---|---|
| **左 D1**（BAT46W・SOD-123・62 個の代表） | Pin 1=K（ROW_C）が y=65.55、Pin 2=A（SW1_D）が y=62.25 → **帯は手前（画面の下）** | 3D 本体の灰色の帯が**下端**。シルク枠も下端だけ閉じている。「+」はアノード側の上パッド | ✅ 一致 |
| **子基板 D_PWR**（B5819W・同じ SOD-123） | Pin 1=K（V3V3）が x=153.16（+X）→ Bottom で裏返って**画面の左端** | 帯（白線）と三角の先が**左端**。「−」が左・「+」が右 | ✅ 一致 |
| **左 U1**（74LVC595・TSSOP-16） | Pin 1（COL1）が (192.28, 91.0) ＝ 左下 → Bottom で**右下** | 3D の Pin 1 の黒点が**右下**。シルクの三角も右下 | ✅ 一致 |
| **左 J_DB**（FH12） | パッド列 y=85.38 が中心 83.525 より手前 → **口は奥向き** | 金色のピンが下（手前）、口が上（奥） | ✅ 一致 |
| **子基板 J_MAIN**（FH12） | パッド列 y=109.15 が中心 111.0 より奥 → **口は手前向き**（J_DB と逆で、2 枚を並べるとケーブルが直線） | ピンが上（板の内側）、口が下（板の端） | ✅ 一致 |
| **ソケット 27 個** | パッドは B.Cu | Bottom にマゼンタの形で 27 個。3D モデルが無いだけで実装対象（CPL 57 点＝27+27+3） | ✅ |
| **XIAO（U_MCU）** | 発注しない | BOM・CPL とも無し。14 穴は空 | ✅ |

**「+」マークの意味（#52 の未確定だったもの）**: 62 個ともアノード側のパッドに「+」、D_PWR にはカソード側に「−」が出た。**「+」＝ Pin 1 ではなく、＝アノード**。判定は 3D 本体の帯で行い、「+」はその裏付けにだけ使った。

右基板は未発注（今回は左＋子基板だけ）。右を出すときは U1/U2 と D1 と J_DB を同じ手順で見ること。

---

## 1b. ⚠️ CPL の実装面が間違っていた（2026-09-01 発見・修正済み）

**61 個のホットスワップソケットが「表面に実装せよ」という指示で出ていた。**
実際にはんだ付けするのは**裏面**（パッドが B.Cu にある）。

**原因**: `export_fab.py` が実装面を `fp.IsFlipped()` で決めていた。
それは**フットプリントを置いた側**であって、**はんだ付けする側ではない**。
ホットスワップソケットはこの 2 つが食い違う——上流 perigoso のフットプリントは
`(layer "F.Cu")` に置きつつ、パッドを `(layers "B.Cu" "B.Mask" "B.Paste")` と
裏に宣言している（**キーは表から挿し、ソケットは裏から付ける**部品だから）。

**噛み合っていなかった証拠**: ペーストは `B_Paste` にしか無く
（左 140 / 右 186 / 子 20 フラッシュ）、**`F_Paste` は 3 枚とも空**。
それなのに CPL は左 27 点・右 34 点を `top` と書いていた。

**直し方**: **SMD パッドが実際にどちらの銅箔に載っているか**（`IsOnLayer(B_Cu)`）で
決めるようにした。⚠️ **`pad.GetLayerName()` は使えない**——
裏面のパッドでも `F.Cu` を返す（`build/fab_check/pads.json` もこれで
誤った面を記録している）。`IsOnLayer()` で見ること。

**確認**: 3 枚 134 点すべてで「CPL の面 ＝ パッドの面」を照合し、**食い違い 0**。
CPL は全点 `bottom` になり、`B_Paste` しか無いことと噛み合った。

⚠️ ソケットの回転が 0.0 → 180.0 に変わった（裏面の式 `(-rot+180)%360`）。
**ソケットは `ROTATION_UNVERIFIED` に入っているので、いずれにせよ
配置プレビューでの目視が要る**（第 1 節）。

## 2. 在庫と価格をもう一度見る

```
.venv/bin/python3 tools/jlcpcb_lookup.py --parts
```

**在庫は動く。**FFC コネクタ（C88360）が最も薄い。前回 2,868 個で、
必要なのは 15 個。**在庫 0 の部品を指定しかけたことが実際にある。**

| 確認日 | 結果 |
|---|---|
| 2026-08-24 | **全 7 品番在庫あり。**最薄は C88360（FFC）2,408 個。ほか C52287685（595）2,443 / C49352235（ソケット）21,949 / C54110（BAT46W）30,501 / C8598（B5819W）628k / C49678・C17514（0805）3.2M〜18.8M |
| **2026-08-31** | **全 7 品番在庫あり。**最薄は C88360（FFC）**1,916 個**（→ 2b）。ほか C52287685（595）2,326 / C49352235（ソケット）22,981 / C54110（BAT46W）25,497 / C8598（B5819W）589k / C49678・C17514（0805）2.9M〜18.2M。**5 セット実装しても在庫倍率は最小 75 倍**（ソケット）で数量は問題ない |

## 2b. ⚠️ FFC コネクタ（C88360）の在庫が減り続けている（2026-08-31 発見）

**数量は足りている。危ないのは「板を作ってから品切れる」こと。**

| 日付 | 在庫 |
|---|---|
| 2026-08-11 | 2,868 |
| 2026-08-24 | 2,408 |
| **2026-08-31** | **1,916** |

**20 日で 952 個減（1 日 48 個）。このペースなら約 40 日で 0。**
必要なのは 1 セット 3 個／5 セット 15 個なので**数量は全く問題ない**が、
**Extended 部品は再入荷が読めない。**

**なぜ他の部品と違って危ないか**: このフットプリントは
**Hirose FH12-12S-0.5SH の形に固定されている**（信号パッド 12×0.5mm ピッチ・
金具 2 個が (±4.65, +1.4)・パッド列は中心から y −1.85）。
JLCPCB には 12P 0.5mm の代替が多数あるが（05A20L12P・0.5K-A-12PB など、
在庫 1 万個超で単価も 1/3）、**どれも金具の位置やピンの並び（staggered など）が
違い、そのままでは載らない。**代替に替えるなら**基板の作り直し**になる。

**→ 対策は「在庫があるうちに発注する」だけ。**
⚠️ **この部品は JLCPCB が実装してくる。**利用者が別途買うものではない
（2026-09-01 に「確保とはどういうこと？」と聞かれた。**書き方が悪かった**）。
心配しているのは **JLCPCB の在庫**で、**発注が遅れて 0 になると
「その部品だけ実装されずに届く」か「発注できない」**という話。

**いま 1,916 個・必要 15 個・1 日 48 個減 → 数週間以内に発注すれば問題ない。**

## 2c. 在庫が切れたときの代替（2026-09-01 に調べた）

**「在庫があるか」だけでなく「切れたらどうするか」まで見た。**
Extended 部品は再入荷が読めないので、代わりが効くかどうかで危険度が変わる。

| 部品 | 在庫の減り | 代替 | 危険度 |
|---|---|---|---|
| **FFC コネクタ** C88360 | **1 日 48 個・約 40 日で 0** | **無い。**12P 0.5mm の代替は多数あるが（在庫 1 万個超・単価 1/3 の品もある）、**金具の位置とピンの並びが違って載らない**。替えるなら基板の作り直し | **★★ 高** |
| 74LVC595 C52287685 | 1 日 17 個・約 137 日で 0 | **ある。[C426713](https://jlcpcb.com) Nexperia 74LVC595APW,118 / TSSOP-16・在庫 3,592（本命より多い）・$0.2806。**同じ TSSOP-16 でピン配置も同じ。電源範囲 **1.2〜3.6V** でレール下限 1.83V の内側（TI は 1.1V。どちらも足りる） | 低 |
| ソケット C49352235 | 在庫 22,981（必要 305） | 未調査だが在庫が厚い | 低 |
| BAT46W C54110 | 在庫 25,497（必要 305） | バックアップは 1N4148W（C81598・Basic）。**`circuit.MATRIX_DIODE_VF` を 0.55 へ戻すと打ち止めが 1.20V/本に上がる**（parts-audit §2） | 低 |
| 0805・B5819W | 数十万〜数百万 | いくらでもある | 無 |

**→ 危ないのは FFC コネクタ 1 つだけ。**ただし**やることは「早めに発注する」だけ**で、
**利用者が別途買うものは無い**（7 品番すべて JLCPCB が実装してくる）。
在庫が切れて困るのは、**代替が無い**この 1 品番だけ、という意味。

## 3. FFC ケーブルの向き

`open-gaps` #19。**✅ 2026-08-16 決着: A タイプ（同面）・80mm**
（uxcell 20 本入り）。買うだけ。残りは `FFC_RIBBON_W` / `FFC_SLACK` の実測。

## 4. アンテナの risk を承知する

`open-gaps` #23。**✅ 2026-08-23 決着**——手 0（アルミ箔で地板を模擬・実測）で
E の差 −7dB・共振なしを確認し、**「### 承知して発注する」の節は作成済み**
（`pytest` と `export_fab.py` の門は開いている）。発注時に読み直すだけ。

## 4b. 生成のリハーサル（記録）

| 日付 | 結果 |
|---|---|
| 2026-08-25 | `export_fab.py` を通しで実行: **3 基板とも門 3 つ（アンテナ承知・部品表・回路図一致）を通過**。左 ガーバー 11 / BOM 5 行 / CPL 57 点、右 11 / 5 / 73、子基板 11 / 3 / 4。警告は「回転が未確認」（FFC コネクタとホットスワップソケット）のみ＝第 1 節の目視項目そのもの。出力は発注時に作り直すので削除した（残すと第 1 節の記録が無い限り pytest が落ちる） |
| 2026-08-27 | #49 露出後の板で再実行: 3 基板とも門を通過（左 11 / 5 / 60、右 11 / 5 / 73、子基板 11 / 3 / 4）。**B.Mask ガーバーに露出ビアの開口があることを座標で確認**（左 5 点・右 6 点すべて）。警告は前回と同じ回転未確認のみ。出力は削除 |
| 2026-08-28 | SPARE/SPARE2 露出＋右ビア径統一後に再実行: 3 基板とも門を通過（左 11 / 5 / 57、右 11 / 5 / 73、子基板 11 / 3 / 4）。**B.Mask の開口を SPARE/SPARE2 の 4 点で座標確認。**警告は回転未確認のみ。出力は削除 |

## 6. 595 と XIAO のピン配置を、独立した 4 視点で確認する（**A・B・D 済・2026-08-30**）

**利用者の指示（2026-08-28）: 「0Ω では救えない。その代わり、絶対に間違えが
ないことを複数の視点で確認する」。**間違っていたら板の作り直しになる 2 部品。
1 つの視点が壊れても別の視点で捕まるよう、**情報源が互いに独立**なものを選ぶ。
済んだ視点は日付と見た物を書く。

| 視点 | 何と何を突き合わせるか | 済 |
|---|---|---|
| A. データシート ↔ `pinmap.py` | TI SCASE93A Table 4-1 と [tools/pinmap.py](../../tools/pinmap.py)（ピン名→番号）。Seeed 公式ピン配置図と `XIAO_nRF52840.kicad_mod`（座標・名前） | 2026-08-28 機械照合（前回の回答）。**ただし同じ人が同じ表を読んだ 1 視点** |
| B. フットプリントの 1 番ピンの位置 ↔ 部品の実物写真 | `render_pcb.py --zoom U1`（右は U2 も）で 1 番ピンの丸印と、**LCSC C52287685 の商品写真のマーク**の向き。SPI_SCK が 11 番、VCC が 16 番に来ているかを**シルクの位置で**読む | |
| C. 回路図 ↔ 板（KiCad の突き合わせ） | `export_fab.py` の門 3「回路図一致」＋ **KiCad GUI で U1 の各ピンをクリックしてネット名を読む**（表示はデータシートのピン名で出る） | |
| D. Seeed 公式 KiCad ライブラリ（別の作者・別の出所） | `Seeed-Studio/OPL_Kicad_Library` の `XIAO-nRF52840-DIP.kicad_mod`（14 穴の座標）＋ `Seeed_Studio_XIAO_Series.kicad_sym`（ピン番号→名前: 1=D0 … 11=D10, 12=3V3_OUT, 13=GND, 14=VBUS）＋ フットプリントの USB の出っ張り（+x 端）を、自作 `XIAO_nRF52840.kicad_mod` と**鏡像・回転 8 通りで照合**。**鏡像なし・90° 回転で 14 穴すべて誤差 0.000mm、USB は自作の奥側**（`build/fab_check/xiao_vs_seeed.json`）。1:1 印刷案は取り下げ（足のパターンが対称で向きの誤りを検出できない） | **2026-08-30 済** |

視点 A が私の照合、B〜D は**別の物理的な情報源**（写真・GUI・実物）。
**2026-08-30 再実行（#19/#49 後の板で）**——`tools/fab_check/` の 3 本を通し、**595 48 パッド全一致（左 U1・右 U1/U2）・XIAO 14 パッド全一致（Seeed 座標と誤差 0.000mm）・キー 61 個の経路が dtsi と全一致・FFC 12 本が鏡像で 1 対 1**。板を作り直しても結線は不変であることを確認した。
**1:1 PDF も 6 本すべて出し直した**（08-28 のものは #19/#49 前の板だった）。実寸であることを外形の実測で確認（left 138.52×97.45 / 設計 138.36×97.40）。
**2026-09-03 再々実行（#53 のスタビ回転・#56 のスペース移動後の板で）**——`tools/fab_check/` の 3 本: **595 48 パッド一致・XIAO 14 パッド一致・61 キーの経路が dtsi と一致**（`evidence.json` の `u595_ok / xiao_ok / chain_ok` すべて true）。1:1 PDF 6 本も出し直し、Edge.Cuts だけを 300dpi で測って **left 138.51×97.45 / right 171.79×97.45 / 子基板 21.08×32.09**（設計 138.36 / 171.70 / 21.0。差は線の太さ分）。⚠️ 全層入りの PDF で測ると F.Fab の `MountingHole` の文字が外形の外にはみ出して **147.4mm に見える**——測るときは Edge.Cuts だけを出すこと。
**2026-08-28: 視点 B の機械版を実施**——板の全パッドを抜いて TI / Seeed の表と突き合わせ、48+14 パッド一致・61 キーの経路がファームと一致（`tools/fab_check/`・証拠ページ `build/fab_check/report.html`）。1:1 PDF は `build/fab_check/*_1to1_*.pdf`（取付穴間隔 16.00mm で確認済み）。
**2026-08-30 追加: 視点 E（外部ソースでの独立確認）。**A〜D とは別に、
**このリポジトリの外**の一次情報と突き合わせた。**A と evidence.py は同じ TI の表を
同じ人が読んだものなので、その読み違いは両方に同じように入る**（共倒れの穴）。
そこを別の出所で塞いだ。

| 何を | 何と突き合わせたか | 結果 |
|---|---|---|
| 595 の 16 ピン | **KiCad 公式 `74xx.kicad_sym` の `74HC595`**（KiCad ライブラリアンが独立に起こしたもの） | **16 本すべて `pinmap.py` と一致。**電気的種別まで一致（QH' が output で tri_state でないことも） |
| 595 の ~SRCLR / ~OE | 同上（能動 Low であること） | ~SRCLR→V3V3（解除）・~OE→GND（出力有効）。**3 個とも正しい** |
| 数珠つなぎの並び | **ZMK 実物のドライバ `app/module/drivers/gpio/gpio_595.c`**（"lowest byte is for the first in the chain" ＋ MSB first） | **index 8 → 鎖の奥 = U2 の QA。**基板は COL8 を U2.QA(15番) に配線。**index 0..8 が COL0..COL8 に一致** |
| SPI がどの足に出るか | **Zephyr 上流 `boards/seeed/xiao_ble/xiao_ble-pinctrl.dtsi`** | spi2 = SCK **P1.13** / MOSI **P1.15** / MISO P1.14 |
| D 番号 → 物理ピン | **Zephyr 上流 `seeed_xiao_connector.dtsi`** | D7=P1.12・D8=P1.13・D10=P1.15。→ **SCK=D8 / MOSI=D10 / CS=D7 が上流の定義で裏付く**（それまでは自分の dtsi のコメントだけが根拠だった） |

**2026-08-31 追加: フットプリントの出所を全部数えた。**
「複数の視点が一致すれば保証できるのでは」という問いに答えるには、
**まず自作（＝外の照合が無いもの）が何個あるかを数える**必要がある。数えた。

| フットプリント | 出所 | 外との照合 |
|---|---|---|
| XIAO_nRF52840 | **自作** | ✅ 視点 D。Seeed 公式ライブラリと 14 穴 0.000mm |
| SW_Hotswap_Kailh_MX ×5 | **perigoso/keyswitch-kicad-library**（⚠️ 当初「自作」と書いたが誤り。3D モデルの参照先で分かった） | ✅ **2026-08-31 に照合。**上流と**パッド完全一致**（5 サイズとも diff 差分なし）。加えて Cherry MX の公開規格（ステム φ4.0・位置決め ±5.08・端子 (-3.81,-2.54)/(2.54,-5.08)）とも一致。MX は無極性なので 1/2 の割り当てで壊れようがない |
| Stabilizer_Cherry_MX ×2 | **perigoso/keyswitch-kicad-library** | ✅ **2026-08-31 に照合。**上流と**穴 4 つが完全一致**（2.00u・3.00u とも）。⚠️ **「電気的接続が無いから確認不要」は誤り**——穴の位置がずれれば**スタビが入らず板の作り直し**になる。位置も見た（下記） |
| Hirose FH12-12S-0.5SH | **KiCad 公式** | ✅ **2026-08-31 に照合。**3 個（J_MAIN・左右 J_DB）とも公式と**0.0000mm 一致**。各々ひとつの変換でのみ一致＝向きも一意 |
| D_SOD-123 / TSSOP-16 / 0805 / MountingHole | **KiCad 公式** | 公式そのもの |

→ **真に自作なのは XIAO のフットプリントだけ**（視点 D で照合済み）。
ほかは KiCad 公式か perigoso で、いずれも上流と一致することを確認した。

**スタビライザーの位置（2026-08-31・利用者の指摘で確認）**

「電気的接続が無いから確認不要」と書きかけたが**誤り**。穴の位置がずれれば
**スタビが入らず、板の作り直し**になる。見た。

| 見たこと | 結果 |
|---|---|
| 上流との一致 | 2.00u・3.00u とも**穴 4 つが完全一致**（diff で差分なし） |
| スイッチとの同心 | 4 個すべて**ずれ 0.000mm**（ST19↔SW19・ST27↔SW27・ST24↔SW24・ST32↔SW32） |
| **付く場所が正しいか** | 幅 ≥2u のキーだけに付いている。**LSHFT 2.25u・RET 2.25u・SPACE 2.75u×2**（2026-09-02 再確認: スペースを内側端へ動かした後の板でも 4 個とも同心ずれ 0.000mm・スタビは 2.00u・最下段の 2 個は 180°・穴の縁→板前縁 2.14mm。→ [open-gaps #56](open-gaps.md)） |
| **付いていないキーが正しいか** | **BSPC は実機 HHKB では 1.50u**（標準キーボードの 2u ではない）、**RSHFT は 1.75u**。どちらもスタビ不要で正しい |
| 銅との干渉 | パッド・ビア・配線のいずれとも**近すぎ 0 件** |

⚠️ **HHKB の幅は標準配列と違う。**BSPC を 2u、RSHFT を 2.75u と思い込むと
「スタビが足りない」という誤った結論になる（実際に一度そう疑った）。
基板の 10 個の幅は**実機と全一致**（SPACE のみ 6u を左右 3u に分割）。

**視点 E で新たに分かったこと**（機械では気づけていなかった）:
`export_fab.py` の `ROTATION_UNVERIFIED` に **`D_SOD-123` が漏れていて、
62 個のダイオードが警告なしで出ていた**。§1 と open-gaps #52 は「目視せよ」と
書いてあるのに、**発注時に読む道具だけが黙っていた。**足した。

**4 つのうち 3 つが済むまで「確認した」と書かない。**（2026-08-30: A・B・D 済。C＝KiCad GUI でピンをクリックは利用者・任意）

## 7. FFC の接続関係を、独立した 4 視点で確認する（**A・B 済。C は組立時・D は通電前**）

**利用者の指示（2026-08-28）: FFC も XIAO・595 と同様に絶対に間違えてはいけない。
基板間の接続関係も含めて複数視点で。**逆なら 12 本が全部入れ替わる（GND↔ROW_E、
3V3↔ROW_D …）。板は無傷でもケーブル（A/B タイプ）の買い直しと日数を失う。

| 視点 | 何と何を突き合わせるか | 済 |
|---|---|---|
| A. 回路の宣言どうし | `circuit.py` の J_DB（主基板）と J_MAIN（子基板）のピン表が鏡像で 1 対 1（`test_the_two_boards_agree_on_the_cable` / `test_the_cable_pinout_table_matches_the_circuit`） | 検査は緑（2026-08-30）。**自分の生成物どうしの一致**なので 1 視点 |
| B. 幾何（板の上のピン 1 の側） | `render_pcb.py --zoom J_DB` と `--zoom J_MAIN` で **両コネクタのピン 1 の位置**と実装面（両方 B.Cu）を読み、**A タイプ（同面接点）でピン 1 がピン 1 に来る向き**かを紙の上で追う。FH12 は**下接点**（データシートの図で確認） | **2026-08-30 済**: J_DB を奥向きに回して直線経路に。J_MAIN ピン 1(+X) ↔ J_DB ピン 12(+X)、表は鏡像。組立モデル断面で目視 |
| C. 1:1 印刷＋実物 | 2 枚を 1:1 で印刷してケースの配置どおりに置き、**実物の FFC（買ったもの）を実際に渡して**、接点面と 1 番の側が両端で合うか | |
| D. 通電前の導通試験（**最終の門**） | 実装後・電池を入れる前に、テスターで **GND↔GND・3V3↔3V3 が導通、3V3↔GND が非導通、SCK が 595 の 11 番に着いている**ことを確かめる。**これが通るまで電源を入れない** | |

**2026-08-28 視点 B の結果 → 同夜に設計を直した**（[open-gaps #19](open-gaps.md)）。J_DB の口を奥向きに回し、
経路は J_DB → J_MAIN の直線。**A タイプで正しい。**J_DB のピン番号は J_MAIN の鏡像（13−n）。
✅ 2026-08-30 右も引き直し済み・右 J_DB は 1.63mm 手前。3 基板とも DRC 0。

ひねり（180°）は「接点面の反転 ＋ 1↔12 反転」を同時に起こすので、
B タイプの代用にはならない。実験で向きの種類を特定する用途だけ。

## 4c. ⚠️ ファームが 2 週間ビルドされていない（2026-09-01 発見）

**基板の発注はせき止めない**が、**板が届いてから焼けないと分かると痛い**ので書く。

**最後に CI が走ったのは 2026-08-16。**そのあと
`config/` `firmware/` に **10 コミット**入っているが、**一度もビルドされていない**。

| 何が入ったか | 規模 |
|---|---|
| `firmware/src/status_led.c` | **新規 560 行**（実機の LED インジケーター再現） |
| `firmware/Kconfig` | +29 行 |
| `firmware/CMakeLists.txt` | +5 行 |
| `hhkb_split_right.overlay` / `.conf` | 列割り当ての整理ほか |

**なぜ走っていないか**: `main` が origin より **138 コミット先行**していて、
**push されていない**（CLAUDE.md のとおり `config/` `firmware/` `build.yaml`
`zephyr/` を触った push だけが `.uf2` を作る）。

**手元でできること・できないこと**:

| | |
|---|---|
| ✅ 静的な整合 | `check_zmk_config.py` 問題なし（transform 61 箇所 × 4 レイヤー一致）。`test_firmware` / `test_keyscan` / `test_pinmap` **39 件緑** |
| ❌ **実際のコンパイル** | **手元に Zephyr SDK が無い**（CLAUDE.md）。**560 行の新規 C が通るかは未確認** |

**→ 板を発注する前後どこかで push して CI を通すこと。**
`.uf2` が出れば「焼ける」ことは確かめられる。**基板とは独立に進められる。**

## 4d. 検査に検出力があることを機械で確かめた（2026-09-01）

**「検査が緑」は「正しい」を意味しない。**通るだけの検査かもしれない。
`tools/mutate.py` は**定数を 1 つずつ故意に壊して、検査が気づくか**を測る。

    .venv/bin/python3 tools/mutate.py      # 遅い（数十分）

**結果: 検出 106 / 生存 0。**壊した 106 個すべてを検査が捕まえた。
**素通りする定数は 1 つも無い。**

個別にも、発注に効く誤りを故意に仕込んで赤になることを確認した:

| 壊したもの | 結果 |
|---|---|
| 595 の SH_CP ↔ ST_CP（SCK と CS の入れ替え） | **4 件赤** |
| マトリクスダイオードの K/A 逆転 | **4 件赤** |
| D_PWR の A/K 逆転（回路図側） | **子基板で赤** |
| 子基板のシルクを 1 本 0.12mm へ | **赤** |
| 子基板の `min_track_width` を 0.05 へ | **赤** |
| R-Space の x を 0.25u ずらす（キーの隙間） | **赤** |

**→ ピン配置・向き・製造規則のいずれも、壊れれば検査が止める。**

## 5. 実装する枚数を決める

**5 セット実装しても使い道が無い。**2 セットで 1 台分の予備が残る。
板は最低数量の 5 枚来るので、残りは未実装の予備になる。
約 ¥2,700 の差（[cost-estimate.md](cost-estimate.md)）。

## 8. 発注前の独立監査（2026-09-02・Fable 5 で 3 系統）

**「もう残っていない」と言うために、3 つの視点を別々の agent に読み取り
専用で回した。**結果は見つかったものだけを書く（通ったことは証拠にならない）。

### 8a. 電気（電源経路・電圧範囲・595・kscan・ADC・復旧）— **基板を作り直す欠陥なし**

外部事実と突き合わせたもの: SGM2040 データシート（LDO 出力への逆給電・
0.4µA 級）、ZMK `gpio_595.c`（MSB-first → `&shifter 8` = U2.Q0 = COL8）、
ZMK `battery.c`、TI SCASE93A のピン表。

| 見つかったもの | 基板に効くか | どうした |
|---|---|---|
| 打ち止め soft-off が ZMK の事象の出方（% が変わったときだけ）と噛み合わず、0% 張り付きで止まらない | **効かない**（ファーム） | [open-gaps #25b](open-gaps.md) 冒頭に記録。Task C4-4 の前に直す |
| 左 overlay の行対応の注記が実値と逆（行 0 以外全部） | **効かない**（板は実値から生成・DRC 0） | 注記に実値を追記。根拠は `circuit.py` 側 |
| D9（SPARE2）が xiao_spi の MISO として浮き入力のまま露出ビアまで伸びる | 効かない（µA 級の可能性のみ・記録済み） | 変更なし |

### 8b. JLCPCB の DFM（ガーバー・ドリル・BOM・CPL・在庫）— **受け付けられない項目なし**

Fabrication Toolkit の出力 3 枚を展開して数えた（Edge.Cuts 閉じている・
F_Paste 空・B_Paste = SMD パッド数・B_Cu/B_Mask/B_Paste のアパーチャ別個数
一致・NPTH 最小 1.75・ビア→板端 ≥0.60・外形外の穴 0）。部品 7 品番は
JLC API で在庫と区分を再確認（C88360 は **1,741** に減少中 → §2b）。

**発注ページで人が見ないと事故になりうるもの:**

| 何 | なぜ | どうする |
|---|---|---|
| PCBA 区分 | Standard だと板端 2.5mm・70×70mm に抵触 | **Economic** を選ぶ（§0 に足した） |
| **ソケット 61 個の CPL 座標が「パッド中心」ではなく「φ4.0 穴の中心」**（perigoso のフットプリントの原点）。パッド 2 枚の中心から約 **3.86mm** ずれている | JLC のライブラリ原点がソケット本体中心なら、プレビューでソケット像が 3.9mm ずれて出る。§0b の「export_fab と 134 点一致」は両方が同じ原点なので**検証にならない** | 「Confirm Parts Placement」のプレビューで**ソケット像がパッドに乗っているか**を見る。ずれていれば JLC のエンジニアにパッド基準で合わせてもらう（通常はそうする） |
| ソケットは F.Cu 置き・パッド B.Cu なので、裏面式 `180 − rot` に X 鏡像がかかっていない | パッドが非対称（(−7.085,−2.54)/(5.842,−5.08)）なので、向き違いは実装不能か誤実装 | §1 の目視に含める（同じプレビュー） |
| スタビ穴 φ3.988（NPTH）の縁が板端まで **0.381mm**（左右 2 穴ずつ） | 外形公差 ±0.2mm に対し薄い | **✅ 同日 #53 で解消**（スタビを 180° 回して 2.14mm） |
| 子基板のシルク文字が 0.8mm（参照記号 9 個・「USB」は線幅 0.12） | JLC 最小 1.0mm/0.15mm 未満。**かすれるだけで製造は止まらない** | 変えない（子基板は読めなくても困らない） |
| 右基板に 1 個だけ φ0.56 のビア（V3V3・(77.24, 89.22)、他 1,289 個は 0.6） | リング 0.13mm で JLC 最小は満たす。配線凍結時の名残 | 変えない |

### 8c. 機械・組立（干渉の網羅・板厚・取付穴・FFC・USB・スタビ・外形）— **要判断 1 件**

3 基板の全フットプリントが `pcb_parts.json` の STEP bbox で `gen_assembly`
に入っていること、ネジ頭 φ3.8 → 最寄りの裏面部品 1.10mm・頭の下は GND だけ、
FFC 口の直線距離 左 17.1／右 21.5mm（+SLACK 25 ≤ 80）、板厚 1.6 の一致、
角 R3.0 の一致、を数字で確認。

| 見つかったもの | 基板に効くか | どうした |
|---|---|---|
| **スペースのスタビ大穴の縁が板前縁まで 0.381mm**（8b と独立に同じ数字） | **効く。板を出してからでは直せない** | **✅ 同日決着**: 利用者が (b) を採用。スペースのスタビ 2 個を 180°（ワイヤ奥）に回し、縁まで 2.14mm。検査を足した → [open-gaps #53](open-gaps.md) |
| 左 J_DB が J_MAIN と X で 1.40mm ずれている（右は 0.00） | 効かない（コネクタの姿勢で吸収・組んで見る） | [open-gaps #54](open-gaps.md) |
| #19 節の「余り 26mm」「pad 1..12 が完全一致」は決着前の古い数字 | 効かない | #19 の本文は触っていない（数字は #54 と 8c に） |
| 未モデルのもの: 子基板裏の電池線パッドの半田盛り（床まで 4.0mm）・FFC の余り 33〜38mm の折り返し・スタビのクリップの板下 1.2mm（最寄り部品まで 2.08mm） | 効かない | 記録のみ |

### 8d. 物理適合を外部事実と照合（2026-09-02・4 系統目）— **板を作り直す欠陥なし・要判断 1 件は (a) で決着**

8a〜8c と #50/#53/#54 の**外**を探した。突き合わせた先は Cherry MX 規格・
Kailh 推奨ランド（parts-audit §8）・KiCad 公式 FP・Fabrication Toolkit の実コード・
JLC capabilities ページ。手元の生成物どうしの一致は数えていない。

| 見つかったもの | 基板に効くか | どうした |
|---|---|---|
| **子基板の電池線・スイッチ線のランド（`BT1_-`・`SW_PWR_1`）が SMD 2.0×2.0 のみ**（`gen_pcb.py` の `TestPoint_Pad_2.0x2.0mm`・スルーホール無し） | **穴は後から開けられない。**線を引っ張るとパッド剥離 | **✅ 2026-09-02 利用者が (a) を採用: このまま（SMD 2.0×2.0）。**組立時にリード線をケース側で固定して引っ張りを逃がす（組立手順に足す） |
| `pcb/hhkb_split_daughterboard.zip`（08-15 版・.gitignore 対象）が本番の隣に残っていた | 発注時に古い方を上げうる | **消した** |
| NPTH 穴縁 → GND ベタ 0.251mm（3 枚とも・左 SW25／右 H6／子 H1）。JLC 明記は「穴→配線 0.28」で NPTH→ベタは明記なし | ドリルが掠ってもベタなので機能は壊れない | 変えない。JLC が指摘したら「そのまま」 |
| シルクが銅・板端に掛かる 6 件（左 D12 が J_DB 金具上・ST27/ST32 が φ4 穴上・D24/D9/D31 が板端）。`--severity-all` で初めて見える | JLC がパッド上のシルクを自動で削る | 変えない（§1 の目視には影響なし） |

**問題なしを確認したもの（母数）**: ソケット FP 61 個の端子位置・パッド・
ホール間隔は Cherry/Kailh と 0.000（差は φ3.05/φ1.75 の +0.05 だけ）／
`PLATE_T 1.5 + PLATE_TO_PCB 3.5 = 5.0`（MX 規格）／SW 61 個の位置が layout と
0.0000・幅→FP 一致・回転 0°・取付穴 左 6 右 8 一致／スタビ FP は Cherry と最大
0.038mm・4 個とも SW 中心・プレートと基板が同じ `stab_flipped()` を読む／
板厚 3 枚 1.6／DRC 全重要度で error 0（warning は #50・#49・上表のシルクのみ）／
外形 1 輪郭閉・銅は Edge.Cuts 無し・ビア in パッド 0・最小パッド間隔 0.20（FFC）。
**見ていないもの**: Kailh の一次 PDF（404）・JLC の NPTH→銅の明示値。
実形状の干渉検査（`nothing_bites`）は #53 の反転後に左右とも通過（18 分）。

**#50 の補足**: JLC の表の「Via Hole-to-Hole 0.2mm」は φ0.3 ビアで中心どうしなら
成立しない → 表は**縁どうし**。0.4398 は本当に 0.01 不足。ただし製造前に
連絡が来る類で、**発注後に直せない誤りではない**。
