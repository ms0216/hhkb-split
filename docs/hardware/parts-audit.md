# 部品の棚卸し — 効く条件と、何で確かめたか

**2026-08-08。**CLAUDE.md「決める前に通す手順」を、既に選んだ部品すべてに
遡って当てた。**選ぶときにこれをやっていなかったので、遅れて 2 回覆した。**

読み方:

- **外** … データシート・JLCPCB の API・実測など、外の事実で確かめた
- **未** … まだ確かめていない。**「たぶん大丈夫」は未に入れる**

---

## 1. シフトレジスタ SN74LVC595APWR（C52287685）

| 効く条件 | 確かめたか | 値 |
|---|---|---|
| 動作電圧の下限 | **外** | 1.1V。レール下限 1.83V の内側 |
| 動作電圧の上限 | **外** | 3.6V。レール最大 2.90V（USB 時 3.3V）の内側 |
| 1.8V での出力電流 | **外** | ±4mA。列の駆動に十分 |
| ピン配置 | **外** | 標準の 595。ZMK の `zmk,gpio-595` がそのまま使える |
| **1.8V での最高周波数** | **外** | **fclock 104MHz @ 1.8V（−40〜125℃ 保証）。**SPI 4MHz に対し余裕 26 倍 |
| **出力降下 VOH** | **外** | **IOH=−100µA で VCC−0.2V。**0.05V と推定していたのは 4 倍の誤りだった |

**74HC595 から替えた理由**は動作電圧の下限（2.0V）。レールが打ち止めで
1.83V まで下がるので規格外だった。→ `test_every_ic_runs_down_to_the_cutoff`

## 2. マトリクスのダイオード BAT46W（C54110）

| 効く条件 | 確かめたか | 値 |
|---|---|---|
| **走査電流での VF** | **外（保証・動作点）** | **≦0.25V @ IF=0.1mA** |
| **低逆電圧での IR** | **外（保証・動作点）** | **≦0.3µA @ VR=1.5V** |
| 必要なレール | 計算 | (0.30+0.05)/0.3 = 1.17V。**マイコンの 1.80V が律速** |
| ゴーストの余裕 | 計算 | 余裕 190mV に対し、行 9 個同時押しで 35mV（25℃） |
| 高温 | **外** | 60℃ では 5µA。**5 個以上の同時押しで余裕を割る**（机上の 25〜30℃ なら問題ない） |
| 接合容量 | 計算 | 20pF ×9 = 180pF。列切替後 2.31µs で VIL。SPI 転送 4.0µs 以上あるので間に合う |
| フットプリント | **外** | SOD-123。1N4148W と同じで基板は変わらない |
| 在庫 | **外** | 25,893 |

**在庫 5,000 超の SOD-123 ショットキー 29 件と、BAT47/48/49/54/85・
1SS 系・NSR・RB751・PMEG・MBR05xx を当たった結果、VF と IR を両方
動作点で保証しているのは BAT46W だけ。**

**バックアップは 1N4148W（C81598・Basic）。**戻すときは
`circuit.MATRIX_DIODE_VF` を 0.55 に戻す。打ち止めは 1.20V/本 に上がり、
マトリクスがマイコンより先に落ちる壊れ方に戻る。

## 3. ショットキー B5819W（C8598）

| 効く条件 | 確かめたか | 値 |
|---|---|---|
| **Vf（打ち止めを決める）** | **外（比較済み）** | SOD-123 の候補を全部並べた。[parts.py](../../tools/parts.py) |
| 10〜15mA での Vf | **未（規定が無い）** | 保守値 0.4V を置く。Task C4 で 1N5819 を使って実測 |
| 逆漏れ | **外** | 逆バイアスは 0.3V 程度。分圧の 1.6µA と同じ桁 |

## 4. バルク 100µF CL31A107MQHNNNE（C15008）

| 効く条件 | 確かめたか | 値 |
|---|---|---|
| **DC バイアス後の実効容量** | **計算で無関係と判明** | 6.3V 品に 3V で半減するが、**結論が容量に鈍感**（下記） |
| 耐圧 | **外** | 6.3V。レール最大 2.90V の内側 |
| パッケージ | **外** | 1206。実基板と一致 |

**送信中の降下は「電流 × 電池の内部抵抗」で決まり、容量では消せない。**
100µF → 50µF → 25µF と減らしても、300µs のバーストでの降下は
19.5 → 22.1 → 22.5mV。定常値 22.5mV に対して**容量ゼロとの差が 3mV**。

余裕 `MCU_MARGIN` は 100mV なので成立する。
`test_the_margin_covers_the_transmit_droop` が見張る。

## 5. パスコン 0.1µF CC0805KRX7R9BB104（C49678）

| 効く条件 | 確かめたか | 値 |
|---|---|---|
| 誘電体 | **外** | X7R。温度で容量が動きにくい |
| 耐圧 | **外** | 50V。レールに対して桁違いの余裕 |
| DC バイアス | **外** | 50V 品に 3V なので実質なし |

## 6. 分圧抵抗 1MΩ（C17514）

| 効く条件 | 確かめたか | 値 |
|---|---|---|
| 誤差 | **外** | 1%。残量表示の精度に効く |
| 消費電流 | **外** | 1.6µA。スリープに対して無視できる |
| **ADC から見た電源抵抗** | **外** | 500kΩ（1M∥1M）。nRF52840 の Table 80 は 40µs で 800kΩ まで。**ZMK の battery_voltage_divider が 40µs を設定している**ので足りている |
| ADC のフルスケール | **外** | ゲイン 1/6・内部基準 0.6V → 3.6V。新品での分圧出力 1.65V は内側 |

## 7. FFC コネクタ FH12-12S-0.5SH(55)（C88360）

| 効く条件 | 確かめたか | 値 |
|---|---|---|
| ピン数・ピッチ | **外** | 12P 0.5mm。実基板と照合済み（検査あり） |
| 電流 | **外** | 0.5A。流すのは µA〜mA |
| 在庫 | **外** | 2,868 個。**この案件で最も薄い** |
| 挿抜回数 | **未** | 組み立てで数回なので問題にならないはず |

## 8. ホットスワップソケット CPG151101S11-2（C49352235）

| 効く条件 | 確かめたか | 値 |
|---|---|---|
| **フットプリント適合** | **外（推奨ランド図）** | 6.35 / 2.54 / φ3.00 / 2.55×2.5mm。**KiCad の実物と一致** |
| **最低使用電圧** | **外** | **2V DC min（錫めっき）。いまのレール下限を決めているのはこれ** |
| 最低使用電流 | **外** | 10µA min。走査は約 100µA なので内側 |
| 接触抵抗 | **外** | 100mΩ max |
| 寿命 | **外** | 5,000 回の挿抜 |
| 在庫 | **外** | 37,697（-16 は 283,792・同価格・**色違いだけ**） |

**金めっき品は JLCPCB に無い。**CPG151101S11 の全変種（-1/-2/-4/-16）を
確認した。-16 は筐体が紫、-2 は白。**接点はどちらも錫、2V min も同じ。**

**この 2V がレールの下限（2.00V）＝ 打ち止め 1.20V/本 を決めている。**
マイコン（1.80V）やマトリクス（1.67V）より厳しい。**電池の末尾 1 割ほどを
捨てることになるが、保証範囲の外で使うよりよい。**

---

## 9. 電源スライドスイッチ SS12D01G4-G（秋月 112723）

**2026-08-12 に利用者が決定。**基板には載らない（壁のポケットへ落とし込み、
リード線で `SW_PWR_1` / `SW_PWR_2` へ繋ぐ。decisions/2026-08-08-power-switch.md）。

| 効く条件 | 確かめたか | 値 |
|---|---|---|
| **ツマミの突出量** | **外** | **4.0mm**（図面の `L 4` と商品ページ「操作部長さ 4mm」が一致）。壁 2.4 − 窪み 1.0 = 1.4mm を貫いて外へ 2.6mm 出る。要求は 1.0mm 以上 |
| 本体の幅（左右） | **外** | 3.9mm。置ける隙間は 10.56mm |
| 本体の高さ（上下） | **外** | 8.8mm。受けの溝の高さを決める |
| 本体の奥行 | **外** | 3.5mm。**奥のリブの位置＝押し込み側の抜け止めがここで決まる** |
| 端子の長さ・ピッチ | **外** | 5.0mm / 2.5mm・3 本。リブの縦スリットを通す |
| ストローク | **外** | 2.0mm。スロット長 5.0mm に収まる |
| 定格 | **外** | 50VDC 0.3A。電池 2 本・待機 0.66mA に対し桁で余裕 |
| 接触抵抗 | **外** | 30mΩ MAX。候補中で最良（SS-12D00G3 は 100mΩ） |
| 操作力 | **外** | 200±80gf |
| 入手性 | **外** | 秋月 ¥30・在庫 15,062 個 |
| **入れられるか** | **外**（実形状） | 受けを本体の下半分（`SW_HOLD_H`）にして解決。**上へ 5.6mm → 手前へ**。`test_every_part_can_be_put_in_from_outside` |
| 経年の硫化膜 | **未** | 銀メッキ。スライドのセルフクリーニング頼み（#18）。**接触抵抗が低いぶん初期マージンはあるが、劣化を防ぐわけではない** |
| 実寸 | **未** | 図面と商品ページの値。**届いたらノギスで `SW_PWR_*` を入れ替える** |

⚠️ **前提だった C&K OS102011MA1QN1（右アングル）とは種類が違う**（こちらは
基板に垂直なストレート）。**それでも設計は変わらない**——スイッチを基板に
載せないので、基板とパネルの位置関係という問題が発生しない。

---

## 10. ピンソケット FHU-1x40SG-Y（秋月 110302）

**2026-08-12 に利用者が決定。プロトタイプ期のみの部品**（脱プロトタイプで
直付けへ戻し、ケースを刷り直す。#27）。1×40 を折って 1×7 を 4 本取る。

| 効く条件 | 確かめたか | 値 |
|---|---|---|
| **座高（スタック高さ）** | **外** | **8.5mm**。秋月の寸法ガイドが「寸法C … ハウジング高さ（**スタック高さ**）」と定義しており、**求めている量そのもの** |
| 手持ちヘッダとの噛み合い | **外** | XIAO のヘッダはピン 5.5mm・樹脂 2.0mm。5.5 < 8.5 なので**底突きしない**。`DB_XIAO_LIFT` = 8.5 + 2.0 = 10.5mm（コードの値と一致・変更不要） |
| リード長（基板側） | **外** | 3.0mm。子基板を貫く |
| ピッチ | **外** | 2.54mm。XIAO と一致 |
| 定格電流 | **外** | 3A。用途に対し過剰 |
| 分割できるか | **外** | 「分割」ロングピンソケット。1×7 を切り出せる |
| 実寸 | **未** | 商品ページの値。**挿した状態で「子基板の上面 → XIAO の基板下面」を直接測る**（分解しての足し算にしない） |

**アンテナへの影響は承知のうえ**（子基板が 10.5mm 持ち上がり、アンテナと
地板の隙間が 7.22 → 0.72mm。利用者が 2026-08-12 に許容と判断。#27 / #23）。

---

## まだ確かめていないもの

**無し。**（2026-08-08 に全項目を閉じた）

閉じ方の内訳:

| 項目 | どう閉じたか |
|---|---|
| マトリクスのダイオードの VF | **部品を替えた**（BAT46W。保証値で詰まる） |
| バルクの実効容量 | **計算で無関係と判明**（容量を倍にしても 3mV） |
| 595 の 1.8V での fmax | **データシート**（104MHz。余裕 26 倍） |
| 595 の出力降下 | **データシート**（VCC−0.2V。推定の 4 倍だった） |
| ソケットのフットプリント | **推奨ランド図と KiCad が一致** |
| ソケットの最低使用電圧 | **制約に追加**（2V。いまのレール下限を決めている） |
| ADC の取得時間 | **ZMK が 40µs を設定済み**。対応不要 |

**残っているのは「実測でしか埋まらないもの」だけ**（`SCHOTTKY_VF`・
`BLE_TX_CURRENT`・`BATT_ESR_END`・`SW_PWR_*`・`DB_SOCKET_SEAT`。
前 3 つは Task C4 で測り、後 2 つは届いたらノギスで測る。
[provisional-values.md](provisional-values.md)）。
