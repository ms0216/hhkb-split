# 本体基板の配線 — 引き継ぎ

**次のセッションはここから。** 残っているのは本体基板の引き回しの微調整だけ。

## いまの状態

| | 違反 | 未配線 |
|---|---|---|
| 本体基板 左 | **25** | **0** |
| 本体基板 右 | **28** | **0** |
| 子基板 | 0 | 0 ✅ |

**未配線 0 = 回路として繋がるべきものは全部繋がっている。**
残るのは形（クリアランス・交差・コートヤード）の問題。

再現手順:

```
KPY=/Applications/KiCad/KiCad.app/Contents/Frameworks/Python.framework/Versions/3.9/bin/python3.9
"$KPY" tools/gen_pcb.py          # 基板を生成
.venv/bin/python3 tools/drc.py   # DRC を走らせて pcb/drc_*.json に記録
.venv/bin/pytest tools -q        # 239 件通過（未完の基板は WIP_BOARDS で skip）
```

---

## 残っている違反の内訳（左 24 件の例）

| 種類 | 件数 | 中身 |
|---|---|---|
| clearance | 11 | `U1` のパッドが `ROW0` のバスに 0.000mm まで寄る／`V3V3` と `VBATT_SENSE` |
| shorting_items | 11 | 行のビアが表面（F.Cu）の列バスに乗る（`ROW3` と `COL1` など） |
| tracks_crossing | 6 | 内層 2 のレーンどうし |
| courtyards_overlap | 3 | `U1` と `SW11` |

---

## 分かっている原因と、試して**失敗した**手

### ★ U1（SOIC-16）が帯からはみ出している

帯は 9.25mm、SOIC-16 は既定で**縦 10.4mm**。行のバス（キー中心から
KiCad で +3.65mm）に重なる。

**`fp.SetOrientationDegrees(90)` で寝かせるのを試したが、違反が 24 → 144 に
悪化した。** 回転するとパッドの並びが変わり、`_route_electronics` の
レーン割り当て（x 順を前提）が崩れるため。回転させるなら、
ピンの並びに合わせた配置の見直しとセットで行うこと。

**代わりに位置で逃がした。**`PLACE` の 3 つ目の要素が帯の中での y の
微調整。-1.8 / -2.6 / -3.4 を実測して -1.8 を採った。

**注意: DRC の件数は実行ごとに数件ゆらぐ**（ゾーンの塗りとの兼ね合い）。
1〜2 件の差で良否を判断しないこと。

**別解**: 帯の中心を行のバスから離す方向へ数 mm ずらす。ただし
`BAND_Y` のずらし方向は**符号を間違えると大幅に悪化する**
（−2.3 で 96/130、+2.3 で 101/124 になった実験がある。
現在の値 `_SOCK_MID = +2.3` が最良）。

### ✅ 行のビアが表面の列バスに乗る — 解決済み

`_fcu_verticals` で表面の縦配線を拾い、`_clear_x` で避けるようにした。
列と行の短絡が 11 → 2 件になった。

### ★ V3V3 と VBATT_SENSE

FFC の 0.5mm ピッチから扇状に広げる部分。レーン（`_link` の `lane`）と
引き出し距離（`d`）の両方をずらしてあるが、まだ寄っている。

---

## 効いた手（同じ考え方が使える）

| | 効果 |
|---|---|
| **コネクタのピン順と部品の並びを揃える** | 子基板 33→3、本体 右 57→37 |
| **GND はベタで受ける**（配線しない） | 扇状に広げる本数が減り、順序を素直にできる |
| **穴避け（`_clear_x`）を内層にも適用** | 行 5 本が通り、未配線 5→0 |
| **通路・レーン・引き出し距離を「測って選ぶ」** | keep は 1.0/1.6/2.0/2.5 を実測して 2.0 を採用 |

---

## やってはいけないこと（実際に悪化させた）

1. **原因を特定せずにパラメータを振る。** レーンの取り方を推測で変えて
   25 → 34 に増やした。必ず `kicad-cli pcb drc --format json` で
   「どのネットとどのネットが、どこで」当たっているかを見てから動かす
2. **正規表現でコードを書き換えるとき、置換後を確認しない。**
   `BAND_Y` の置換を誤って 96/130 まで悪化させた
3. **接頭辞でフットプリントを走査する。** `D`／`SW`／`SW_` で拾うと
   電源部の `D_PWR`／`SW_PWR`／スライドスイッチを巻き込む。
   **今日 3 回同じことが起きた。** `re.fullmatch(r"D\d+")` のように絞る

---

## 次に試すと良さそうな案（未検証）

**V3V3 を配線ではなくベタで配る。** 内層 1 はいま GND のベタ 1 枚だが、
部品を置いた帯のところだけ V3V3 の島を作り、GND はその周りを埋める
（電源面の分割。よくある作り）。

  - V3V3 の配線が丸ごと不要になる。各パッドからビアを落とすだけ
  - GND で同じことをしたら、扇状に広げる本数が減って順序が素直になった。
    **同じ効果が V3V3 でも期待できる**
  - 注意: 帯にある GND のビアと重ならないこと。KiCad のゾーンは他ネットの
    ビアを自動で避けるが、島の範囲は確認すること

**一度試した結果（2026-08-08）**: 島を帯 0 の中心に高さ 9mm で作り、
V3V3 を GND と同じく「パッドの脇にビアを立てるだけ」にしたところ、
**未配線が 6〜7 本出た**（島に届いていない）。違反も 30/34 に増えた。

考えられる原因: ビアをパッドから 1.8mm ずらして立てているので、
帯の端にあるパッドでは島の外へ出る。島の高さを広げるか、
ビアの位置を島の内側に寄せる必要がある。**案そのものは筋が良いはずで、
ビアと島の位置関係を詰めれば効くと思われる。**

これで V3V3 が消えれば、残るのは VBATT_RAW / VBATT_SW / VBATT_SENSE の
3 本と行だけになり、内層 2 がかなり空く。

---

## 試す価値が無いと分かっているもの

**空き場所の探索で部品を自動配置する案は、そのままでは効かない。**
`_obstacles` / `_free_x` を書いて試したところ、コートヤードの重なりは
3→1、短絡は 5→1 に減ったが、部品が基板の縁へ押し出されて
`copper_edge_clearance` が 5 件出た。範囲を制限すると今度は
「置ける場所が無い」で止まる（帯 1 本では足りない）。

**方向としては正しい**（手で座標を決めるより筋がよい）が、
帯を跨いだ配置と、配置が変わったあとの配線の作り直しをセットで
考える必要がある。中途半端に入れると全体が悪化する（23→36 になった）。


**クリアランス規則を緩める案は効かない。** ネットクラスの 0.2mm を
JLCPCB の能力 0.127mm まで下げれば減るかと考えて測ったが、
違反の実測値は 0.015 / 0.072 ×2 / 0.10 ×5 / 0.156 ×2 で、
**8 割が 0.1mm 以下**。規則の問題ではなく配線の問題。
規則を緩めても 2 件しか消えず、製造の余裕を削るだけになる。

---

## 残る手順

1. 上記 3 つの原因を潰して DRC 0 にする
2. `tools/test_pcb.py` の `WIP_BOARDS` を空にする
   （`test_the_unfinished_boards_are_declared` が「綺麗になったのに
   残っている」状態を検出するので、忘れることはない）
3. ガーバー・BOM・CPL を出す（`kicad-cli pcb export gerbers` ほか）
4. [open-gaps](open-gaps.md) の #16 を閉じる

---

## この設計で守られていること（壊さないこと）

- `tools/interface.py` は**凍結境界**。プレート・基板・ケースが共有する
- 検査は「通ること」ではなく「**効いていること**」まで見る。
  故意に壊して検出できることを確かめてから足す
- 実機と違うところは [open-gaps](open-gaps.md) に書く。
  差があること自体は悪くない。**気づけないことが悪い**
