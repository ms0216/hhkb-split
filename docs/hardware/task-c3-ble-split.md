# Task C3 — BLE 分割の検証

**所要時間の目安: 半田付け 20 分 ＋ 配線と測定 30 分**

> 先に [Task C1](task-c1-smoke-test.md) を済ませておくこと。
> 2 個目の XIAO も、まず C1 を通してから使う。

---

## 1. なぜやるか

**左右間も無線であることは、この計画の必須要件**（仕様書の優先度「必須」）。
TRRS ケーブルは切り分け用に基板へフットプリントだけ残すが、常用構成には
含めない。したがって**分割無線が成立しなければ計画そのものが揺らぐ**。

確かめることは 3 つ。

| # | 確認対象 | 不成立だと |
|---|---|---|
| 1 | 左右がペアリングして、右のキーが左を経由してホストへ届く | 分割が成立しない |
| 2 | **左右同時押しが崩れない**（Shift＋文字など） | HHKB の使い勝手を再現できない |
| 3 | 遅延が体感できる水準でない | 実用にならない |

**2 が本命。** HHKB では左 Shift ＋ 右手の文字、左 Ctrl ＋ 右手の文字を
常時使う。ここが崩れると目的を満たさない。

## 2. なぜ本番シールドを使わないか

本番の `hhkb_split_left` / `right` は **74HC595 と 61 キー**が前提で、
分割の検証には重すぎる。配線を間違えたときに「BLE の問題か、
マトリクスの問題か」が切り分けられない。

そこで `proto_split` は**キースキャンを直結**にした。
ダイオードもシフトレジスタも使わない。左 2 キー、右 2 キーだけ。
**BLE 分割だけが変数になる。**

なお C1・C2 では変数を減らすため `CONFIG_ZMK_BLE=n` にしていたが、
**このタスクでは BLE が主役なので当然有効のまま**。

---

## 3. 用意するもの

| 品目 | 数 | 備考 |
|---|---|---|
| XIAO nRF52840 | **2** | 2 個目は先に [Task C1](task-c1-smoke-test.md) を通す |
| ピンヘッダ 7 ピン | 4 本 | 2 個目にも両側必要 |
| タクトスイッチ | 4 | 左右 2 個ずつ |
| ブレッドボード | **2** | 左右で 1 枚ずつ |
| ジャンパ線 | 10 本 | |
| USB-C ケーブル | 1〜2 | 左は常時接続。右は書き込み時のみ |

**ダイオードは使わない。**

---

## 4. 配線

**C1 とほぼ同じだが、⚠️ 使うピンが 1 つずれる。**

| | 使うピン |
|---|---|
| C1（`proto_direct`） | **D0・D1** |
| **C3（`proto_split`）** | **D1・D2** |

**Task C5 で D0 を ADC に取られたので、2026-08-08 にキーを D1・D2 へ
移した**（[proto_split.dtsi](../../config/boards/shields/proto_split/proto_split.dtsi)）。
**[Task C1](task-c1-smoke-test.md) の図をそのまま使うと、D0 につないだキーは
一生反応しない。**

<img src="img/breadboard-c3.svg" alt="Task C3 のブレッドボード配置。左右とも同じ。XIAO を 1〜7 列、タクトスイッチを 10・12 列と 14・16 列に挿し、18 列を GND バスにする。キーは D1（2 列）と D2（3 列）につなぐ" width="100%">

この図は [tools/gen_breadboard_c3.py](../../tools/gen_breadboard_c3.py) が生成する。
**SVG を手で編集しない。**挿す穴は同ファイルの `LINKS` が唯一の出どころで、
**穴の取り合い・本体の下に隠れる穴・節点のつながり方**を
`tools/test_constants.py` が検査している（D0 につないでしまう間違いも見張る）。

> **一度は「C1 の図に『1 列ずらして読め』と注記を足す」で済ませようとした。**
> だが台の上で見るのは図であって注記ではない。読み替えを利用者に押し付ける
> のは、図を作りたくない側の都合でしかない。

### 挿す場所（左右とも同じ）

| # | 挿すもの | 場所 |
|---|---|---|
| 1 | XIAO nRF52840 | 1〜7 列。上側の足が **h 行**、下側の足が **d 行** |
| 2 | タクトスイッチ **1** | **10 列と 12 列**（足は **b 行と e 行**） |
| 3 | タクトスイッチ **2** | **14 列と 16 列** |
| 4 | ジャンパ | **2 列の a 行 → 10 列**（**D1** へ）|
| 5 | ジャンパ | **3 列の a 行 → 14 列**（**D2** へ）|
| 6 | ジャンパ | 12 列の **a 行** → 18 列（GND バスへ） |
| 7 | ジャンパ | 16 列の **a 行** → 18 列の **b 行** |
| 8 | ジャンパ | **2 列の j 行** → 18 列の **c 行**（GND を上半分から引く） |

**2 列は上下で別の配線。**上半分（j 行）が **GND**、下半分（a 行）が **D1**。
中央の溝で分かれているので短絡しない。

**スイッチの足が入る列（10・12・14・16）で使える穴は a 行だけ。**
c・d 行はスイッチ本体の下に隠れる。

**タクトスイッチの向きはテスターで確かめる。**押したときだけ鳴る 2 本が
スイッチとして使う 2 本で、この案件の手持ち品では **2 マス（5.08mm）離れた
組**がそれ（[Task C2 の §6](task-c2-keyscan.md)）。

左右で違うのは**書き込むファームウェアだけ**。

| | 書き込むファーム | 押すと出る文字 |
|---|---|---|
| 左（セントラル） | `proto_split_left-xiao_ble__zmk-zmk.uf2` | 10 列 → **Shift**、14 列 → **b** |
| 右（ペリフェラル） | `proto_split_right-xiao_ble__zmk-zmk.uf2` | 10 列 → **c**、14 列 → **d** |

> **左の 1 つ目を Shift にしてある（2026-08-09）。**当初は a・b・c・d の
> 4 文字だったが、それでは**確かめたいものを確かめていなかった**。
> 守りたいのは「左 Shift ＋ 右手の文字」で、HID では修飾キーは通常のキーと
> **別のバイト**に載る。文字キー同士のロールオーバーが通っても保証にならない。
> 詳しくは [proto_split.keymap](../../config/boards/shields/proto_split/proto_split.keymap)。

**左右を取り違えないこと。** 両方に左用を書き込むと、2 台とも
セントラルになってペアリングしない。書き込んだらマスキングテープなどで
印を付けておくとよい。

---

## 5. 手順

### 5-1. 2 個目の XIAO を C1 で通す

**いきなり分割を試さない。** 2 個目の個体・ケーブル・書き込みが健全かを
先に確定させる。[Task C1](task-c1-smoke-test.md) をそのまま実施する。

### 5-2. 左右に書き込む

1. 左の XIAO に `proto_split_left` を書き込む
2. 右の XIAO に `proto_split_right` を書き込む
3. **左だけ** USB で Mac につなぐ
4. 右は USB でもモバイルバッテリーでも、電源が入っていればよい
   （この段階では乾電池はまだ使わない。乾電池は Task C4）

### 5-3. ペアリングを待つ

ZMK の分割は**自動でペアリングする**。ユーザー操作は不要。
初回は数秒〜十数秒かかることがある。

うまくいかないときは、両方を一度リセットする。それでも駄目なら
**`settings_reset` を焼く**（[build.yaml](../../build.yaml) に入れてある）。
ボンド（BLE の結合情報）が全部消える。**消したあと本来のファームを
焼き直すこと**（2 回焼く）。

> **Task C4 で実際にこれで直った（2026-08-09）。**「リストには出るのに
> 接続できない」は、ホストと装置のどちらか片方だけが相手を覚えている
> 状態の典型的な症状。この治具にはレイヤーが無く `&bt BT_CLR` を
> 割り当てたキーも無いので、`settings_reset` が唯一の手段になる。

---

## 5-4. 【C4 から持ち越し】起きているときの電流を測る

**配線を変えずにできる。先にこれを済ませると電池寿命が確定する。**

Task C4 の測定 C は **3.7mA** だったが、**その大半は「右半分を探し続けて
いる」ぶん**だった。ZMK のセントラルはペリフェラルが全部つながるまで
スキャンを止めず、Zephyr の既定は 30ms 受信 / 60ms 周期＝**デューティ 50%**。
nRF52840 の RX 4.6mA から、スキャンだけで 2.3mA 級になる
（[Task C4・C5 の §6](task-c4-c5-power.md)）。

**右がつながれば `stop_scanning()` が呼ばれて止まる。**そこが本来の待機電流で、
**電池寿命はこの 1 つの数字で決まる**（スリープ電流 2〜3µA とは 3 桁違うので、
平均は起きている時間の割合でほぼ決まる）。

### やり方

**左は C4 の治具のまま。**テスターが 29 列 c 行 ↔ 27 列 c 行 に直列に
入っている状態を崩さない。**左にキーは要らない。**

1. 2 個目の XIAO に `proto_split_right` を焼く
2. 右を USB かモバイルバッテリで給電し、左のそばに置く（キーは無くてよい）
3. 左右がペアリングするのを待つ（数秒〜十数秒）
4. **左のテスターの mA を読む**

### 実測結果（2026-08-09）

| 状態 | 実測 |
|---|---|
| 右が居ない | **3.7mA**（3.7〜5.0mA を上下） |
| **右がつながっている** | **0.66mA。安定** |

**5.6 分の 1 になった。**スキャンが原因だったことが実測で確定。

**「上下が止まる」ことがペアリング成立の合図**として使えた。接続に失敗して
再スキャンに戻るループが電流に出るので、**テスターが診断器として働く。**

### ここまでで確定したこと

**ペアリングは成立する**（キーが無くても左右はつながる）。
2 個目の XIAO・ケーブル・書き込みも同時に健全と分かった。

### 引っかかったこと

**右が別のセントラルとのボンドを持っていて、繋がらなかった。**
左は右を見つけているのに接続できず、電流が 3.7〜5.0mA を上下し続けた。
`settings_reset` → `proto_split_right` の 2 回焼きで解けた。

ソースにそう書いてある（[central.c](https://github.com/zmkfirmware/zmk/blob/main/app/src/split/bluetooth/central.c) の `split_central_eir_found`）。

> Once the central has bonded to its peripherals, the peripheral MAC
> addresses will be validated internally and the slot reservation will
> fail if there is a mismatch.

**新品でも中古でも、まず `settings_reset` を通してから始めるほうが速い。**

---

## 5-5. 右半分（ペリフェラル）の電流を測る

**いま「電池寿命 4.1〜11.6 ヶ月」と言っているのは左半分だけの話。**
左右で電池は独立しているので、**右が重ければ右が先に切れる**。そちらが
製品の寿命になる。

<img src="img/breadboard-right-current.svg" alt="C4 の治具と C3 の右のブレッドボードを線 2 本でつなぎ、右半分の電流を測る配線図。治具のシャントの位置にテスターを直列で入れ、レールを右の 3V3 へ、GND を右の GND へつなぐ" width="100%">

この図は [tools/gen_breadboard_right_current.py](../../tools/gen_breadboard_right_current.py) が
生成する。配線とスイッチの位置は**両方の生成器の `LINKS` から読んでいる**ので、
片方を直したときにこの図だけ古いまま、が起きない。

### ⚠️ XIAO を載せ替えない

右の XIAO を治具へ移せば配線は 1 か所で済む。**だが右のスイッチが
使えなくなり、「本当にペアリングしているか」を確かめる手段が消える。**

**広告中のペリフェラルを測って「これが待機電流だ」と書くのは、左で
3.7mA を掴んだのとまったく同じ間違い**（[Task C4・C5 の §6](task-c4-c5-power.md)）。
あのときは電流の上下で気づけたが、**今度は右の電流そのものが測定対象**なので、
それを判定に使えない。

→ **線 2 本で給電すれば右のキーが残るので、`c` `d` が出ることを確かめてから
測れる。**

### つなぐ 2 本

**右のブレッドボードは組んだまま。USB だけ抜く。**

| | 治具側 | → | 右のボード側 |
|---|---|---|---|
| **レール（3V3）** | **14 列 b 行** | → | **3 列 j 行** |
| **GND** | **12 列 d 行** | → | **2 列 i 行** |

治具の 14 列はショットキーの後（レール 2.77V）で、XIAO の動作範囲内。
右のボードの 3 列上半分が 3V3、2 列上半分が GND。
**2 列 j 行は C3 の GND ジャンパで埋まっている**ので i 行を使う。

治具の XIAO ソケットは**空のままでよい**。分圧（1MΩ×2）は節点 A に
ぶら下がったままだが、**それも右半分の消費に含めて数えるのが正しい**
（本番の右基板にも分圧は載るため）。

### ⚠️ 電流計を入れたまま「つながるか」を試さない

**電流計の負担電圧は、電流がいちばん大きいとき＝つながっていないときに
最大になる。**つまり**測りたい状態を測定器が壊す。**

**この案件で実際に起きた（2026-08-09）。**テスターの 2000µA レンジを
直列に入れたまま試したところ、未接続時（1mA 級）に右が接続できなくなった。
**外してただのジャンパにすると直った。**

**降下の大きさは測っていない。**取説（A&D AD-5526）に DCA の負担電圧の
記載が無く、同時に電圧を測るにはテスターがもう 1 台要る。**分かっているのは
「入れると繋がらない・外すと繋がる」という事実だけ。**

**正のフィードバックになる。**繋がらない → 電流が増える → 降下が増える →
もっと繋がらない。**「電源を入れる順序でつながらない」という別の不具合に
見えた**（当初 open-gaps #27 として記録し、あとで取り消した）。

→ **順序を分ける。**

| | やること |
|---|---|
| **1. つながることを確かめる** | **電流計を外し、ただのジャンパにする** |
| 2. つながった状態で電流を測る | 電流計を入れる。**接続中は 40µA なので降下 40mV で無害** |

**接続中の電流を測るぶんには問題ない。**接続中は 40µA なので降下は小さい。
壊れるのは未接続の状態だけ。

**レンジも選ぶ。**1mA を超える区間を含むなら **20mA レンジ**（分解能
0.01mA）。スリープの µA を読むときだけ 2000µA へ落とす。

### 手順

1. 右のブレッドボードの **USB を抜く**
2. 上の 2 本をつなぐ
3. 治具の**電源スイッチを入れる**
4. 左（USB 給電）を Mac につなぐ
5. **右の 10 列・14 列を押して `c` `d` が出ることを確認** ← **接続の証拠**
6. **10 秒ほど触らずに置いてから**、治具のテスター（mA レンジ）を読む

**5 を飛ばさない。**`c` `d` が出ないまま測った値は、待機電流ではなく
広告中の電流。

### 実測結果（2026-08-09）

**接続を確認してから測った**（右の 10 列・14 列で `cdcdcdcd` が出た）。
**テスターは接続が確立してから直列に入れた**（上の警告）。

| | 実測 | 平均電流（8.5h/日） | 寿命 |
|---|---|---|---|
| 左（セントラル・[§5-4](#5-4-c4-から持ち越し起きているときの電流を測る)） | **0.66mA** | 0.235mA | **11.6 ヶ月** |
| **右（ペリフェラル）** | **0.040mA** | 0.014mA | 5.7 年（電池の自己放電が支配） |

**右は左の 16 分の 1。**セントラルはホストとペリフェラルの 2 つの接続を
持ち、HID も送るが、ペリフェラルは接続 1 本を保つだけ。

### 未接続のときの右（2026-08-09・20mA レンジで測り直した）

| 状態 | 実測 | |
|---|---|---|
| 接続中 | **0.040mA** | |
| **未接続（低デューティの広告）** | **0.11mA** | 接続中の 2.75 倍。**2000mAh で 2 年相当。実質無害** |
| **起動直後／相手が消えた直後（一瞬）** | **5.5mA** | **高デューティの名指し広告** |

**5.5mA の一瞬は、ZMK のソースの読みを裏づけている。**ボンド後の
ペリフェラルは「相手を名指しした広告」を高デューティで始め、
**Bluetooth の仕様で 1.28 秒で打ち切られて低デューティへ落ちる**
（[peripheral.c](https://github.com/zmkfirmware/zmk/blob/main/app/src/split/bluetooth/peripheral.c)）。
**「一瞬」という体感が 1.28 秒と符合する。ソースを読んだだけの話が、
電流計で見えた。**

> **2000µA レンジで読むと、同じ未接続の状態が「1mA」に見えた。**
> 負担電圧でレールが沈み、**沈むと電流が増える**という正のフィードバックが
> 働いた結果で、9 倍ずれていた。**レンジの選び方が値を変える。**

**✅ 寿命を決めるのは左半分。**右は考えなくてよい。§5-4 で出した
**4.1〜11.6 ヶ月がそのまま製品の見込み**になる。

（40µA には治具の分圧 1.53µA が含まれる。本番の右基板にも分圧は載るので、
含めたまま数えるのが正しい。）

---

## 5-6. 電池を伸ばす余地（BLE 側）— **調査結果。今は変えない**

**2026-08-09 に調べた。実行はしていない。**将来「電池が足りない」となった
ときの判断材料として残す。**寿命 4.1〜11.6 ヶ月は仕様（3〜8ヶ月）を
すでに満たしているので、いま使い勝手を削る理由が無い。**

### 前提: 左は 2 つの接続を持ち、役割が違う

| リンク | 左の役割 | 決めるのは | 現状 |
|---|---|---|---|
| **左 ↔ 右（分割）** | **セントラル** | **左＝私たちのファーム** | **7.5ms / latency 30 / 1M PHY** |
| **左 ↔ ホスト** | **ペリフェラル** | **ホスト（macOS 等）** | **未測定**（左は 7.5〜15ms / latency 30 を希望するだけ） |

**セントラルは接続間隔ごとに必ず電波を出す。**ペリフェラルがいつ聞きに来るか
分からないのでサボれない。**slave latency はペリフェラルしか救わない。**

**これが 16 倍の差の理由。**

```
左（セントラル役）  7.5ms ごとに必ず起きる     → 133 回/秒 → 0.66mA
右（ペリフェラル役）latency 30 で 232ms に 1 回 →  4.3 回/秒 → 0.040mA
```

**左のホスト側は latency が効くので、すでに軽いはず**（許可されていれば
465ms に 1 回＝2.2 回/秒）。**つまり左の消費は分割リンクが支配的。**

### 伸ばせる要素は 4 つ

| # | 要素 | 効き | 払うもの | 使えるか |
|---|---|---|---|---|
| **1** | **分割リンクの接続間隔を伸ばす** | **大**（推定 +2〜3 ヶ月） | **打鍵の遅延** | **体感次第。要検証** |
| **2** | **分割リンクの PHY を 2M に** | 中（推定 2 割の短縮） | 受信感度 約 3dB | **アンテナの件（#23）が片づけば** |
| 3 | TX 電力を下げる | 小（推定 +1 ヶ月） | 通信距離 | **本番環境で余裕が過剰だと測れれば可**（下記） |
| 4 | ホストリンクの間隔を長く希望 | **条件つき** | 遅延 | **ホストが latency を拒否していた場合のみ** |

**1 と 2 は分割リンク＝私たちが完全に制御できる場所。**

**2 はホストに依存しない。**分割リンクの PHY は左右 2 台で決まるので、
**Mac でも Windows でも同じように 2M にできる。**

### 3（TX 電力）について — 「使えない」ではない

**距離を設計側で仮定してはいけない**（分割の目的）のと、**実測して余裕が
過剰だと分かったうえで削る**のは別の話。後者は根拠のある判断。

**ただし左では、分割リンクとホストリンクで送信電力を分けられない**
（接続ごとに変えるには `BT_CTLR_TX_PWR_DYNAMIC_CONTROL` ＋ VS HCI が要る。
Zephyr に機能はあるが **ZMK は未対応で前例も無い**）。

→ **律速はおそらくホスト側。**分割の距離は利用者が決めるが、ホストは机の下や
画面の裏で 1〜3m ということがある。**「分割の余裕が過剰」だけでは足りず、
「ホスト側の余裕も過剰」が要る。**

送信電力ごとの電流と技適の制約は [open-gaps #23](open-gaps.md) の
「判断材料: 送信電力」にまとめてある。

### 実測: ホストが許可した接続パラメータ（2026-08-09）

```
le_param_updated: 3C:A6:F6:3F:0B:48 (public): interval 12 latency 0 timeout 72
```

| | 値 | ZMK が希望した値 |
|---|---|---|
| 接続間隔 | **15ms**（12 × 1.25ms） | 7.5〜15ms → **最大値が採られた** |
| **latency** | **0** | **30 → 拒否された** |
| 監視タイムアウト | 720ms | 4000ms → 拒否 |

**macOS は latency 0 を返した。**4 回の更新すべて同じ値。

**つまりホストリンクは軽くなかった。**この節の冒頭に「左のホスト側は
latency が効くので、すでに軽いはず」と書いたが、**効いていない。**

```
分割リンク（セントラル）  7.5ms → 133 回/秒
ホストリンク（latency 0）  15ms →  67 回/秒
                          合計    200 回/秒   ← ホストが 3 分の 1
```

**遅延の鎖が埋まった。**

```
デバウンス        3ms
分割リンクの待ち  平均 3.75ms
ホストリンクの待ち 平均 7.5ms   ← **最大。しかも触れない**
合計              平均 14.25ms
```

**end-to-end の遅延には約 10.5ms（3 + 7.5）の下限がある。**分割を 7.5ms に
して買っているのは、その上の 3.75ms。

| 分割の間隔 | 合計遅延（平均） | 左の無線イベント |
|---|---|---|
| **7.5ms（今）** | **14.25ms** | 200 回/秒 |
| 15ms | 18.0ms（+26%） | 134 回/秒（**−33%**） |

**要素 4（ホスト側の間隔）が生き返った。**latency 0 なので 67 回/秒を
消費している。ただし払うのは**いちばん大きい遅延項**なので、分割側より分が悪い。

> **この測定は USB を繋いだ状態だった。**「BLE だけのときは違うかもしれない」
> という疑いが残ったので、**UART ログで測り直した（下記）。同じ値だった。**

### 分割リンクの PHY は測らない（設定から決まる）— **⚠️ 電波では見ていない**

```
BT_CTLR_PHY = y / BT_CTLR_PHY_2M = y（nRF52840 は 2M 対応）
BT_AUTO_PHY_CENTRAL = 2M（セントラルが接続直後に 2M を要求）
ZMK は上書きしていない
→ **定常状態は 2M。要素 2 に削りしろは無い。**
```

> **⚠️ これは設定から導いた結論で、電波では確かめていない。**
> Zephyr の Kconfig という外の事実は読んでいるが、**実際に 2M へ遷移した
> ことは見ていない。**「残っている調査」の B。

ログの `PHY: 1` は、**ZMK が接続時にしか PHY をログしないため**
（`split_central_process_connection` は `split_central_connected` からのみ
呼ばれる）。PHY 更新はその直後に走る。

> **`CONFIG_BT_CONN_LOG_LEVEL_DBG` を入れてはいけない。**分割リンクは
> 133 回/秒で、`bt_conn_tx_processor` が毎回ログを吐く。CDC の 1024 バイトの
> リングバッファが溢れ、**1.48 秒でログが止まり、ホストとの BLE 接続まで
> 確立しなくなった**（2026-08-09 に実際に起きた）。**測定器が測定対象を壊す形。**

### UART ログで測り直した（2026-08-09・**完了**）

**USB を挿していると測れないものが 2 つあった。**

| | |
|---|---|
| ① BLE だけのときの接続パラメータ | USB を挿すと ZMK は HID を USB へ流す |
| ② ログを取りながらの消費電流 | **USB 給電だと電池側に電流が流れない。原理的に同時に測れない** |

→ **ログを UART（D6・P1.11）へ出し、Saleae で読んだ。**

**「J-Link が要る」と一度書いたが誤り。**手元の道具（Saleae）を数えずに、
無い道具を前提に「できない」と言っていた。

**結果 — USB の有無で変わらなかった。**

```
[00:00:01.256] le_param_updated: 3C:A6:F6:3F:0B:48 (public): interval 12 latency 0 timeout 72
[00:00:05.348] le_param_updated: 3C:A6:F6:3F:0B:48 (public): interval 12 latency 0 timeout 72
[00:00:05.439] le_param_updated: 3C:A6:F6:3F:0B:48 (public): interval 12 latency 0 timeout 72
```

| | USB あり | **BLE のみ** |
|---|---|---|
| 接続間隔 | 12（15ms） | **12（15ms）** |
| latency | 0 | **0** |
| タイムアウト | 72（720ms） | **72（720ms）** |

**macOS は USB の有無で扱いを変えていない。**前回の測定は歪んでいなかった。
**ホストリンクは 67 回/秒を消費している**という結論はそのまま立つ。

分割リンク側も同じログで取れた。**設計どおり。**

```
split_central_process_connection: New connection params: Interval: 6, Latency: 30, PHY: 1
```

#### **ここで 3 回つまずいた。すべて「設定しただけでは効いていない」**

**1 回目 — ログの出口。**`CONFIG_LOG_BACKEND_UART=y` と書いても、
**Zephyr はこれを `zephyr,console` が指すデバイスへ出す。**XIAO の既定は
`xiao_ble_common.dtsi` が `common/usb/cdc_acm_serial.dtsi` を include して
いるので **USB CDC**。充電器給電では受け手が居ない。

→ overlay で `chosen { zephyr,console = &uart0; }` を書いて向け直した。

**2 回目 — ドライバが入っていない。**console を uart0 に向けたらビルドが
落ちた（`__device_dts_ord_128 undeclared`）。`.config` に
`CONFIG_UART_NRFX` が無かった。**ボードの dtsi は `status = "okay"` と
書いているのに、デバイスの実体が無い。**

→ overlay で `&uart0 { status = "okay"; }` を明示して引き込んだ。

**3 回目 — ログ水準。**読みたい `le_param_updated` は ZMK の `LOG_DBG`。
既定の INFO ではコンパイル時に消える。`CONFIG_ZMK_USB_LOGGING` はこれを
暗黙に上げていたので USB のときは出ていた。**USB から UART へ移したときに
その副作用を引き継いでいなかった。**

→ `CONFIG_ZMK_LOG_LEVEL_DBG=y`。**装置を壊した `BT_CONN_LOG_LEVEL_DBG`
とは別物**（あちらは Zephyr の BT スタック・133 回/秒）。

**切り分けに効いた手は 1 つ。**「リセットしても 1 ビットも動かない」を
確かめたこと。**ブートのメッセージは INFO なので、線が生きていれば必ず
出る。**出ないなら配線ではなく、喋っていないほうが疑わしい。

#### 測ったときの構成

| | |
|---|---|
| ファーム | `9080501`・**左のみ**・UART ログ有効 |
| 配線 | Saleae **CH0 → XIAO の `D6`**、**GND → XIAO の `GND`** |
| Saleae | Async Serial / **1000000 bps** / 8N1 / LSB first / 24MS/s |
| 電源 | 左は **USB 充電器**（Mac には繋がない）。右も電源オン |

**XIAO のピンは 14 本。**番号ではなく**基板の印字**で挿すこと（数え方が
食い違って往復した）。

**ログは 2 か所で溢れた**（`--- 72 messages dropped ---`）。1Mbps でも
起動直後の数百行には足りない。**読みたい行が残ったので追わなかったが、
起動直後を詳しく見るなら `CONFIG_LOG_BUFFER_SIZE` を上げる必要がある。**

**⚠️ `proto_split_left.conf` と `.overlay` の [一時的] は消すこと。**

### D1 — 分割リンクの接続間隔を変えて左の電流を測った（2026-08-10・**完了**）

<img src="img/d1-split-interval.svg" alt="接続間隔と左半分の消費電流。7.5/15/30/60ms で 0.66/0.47/0.38/0.33mA。反比例の当てはめと、0.28mA の床" width="100%">

図は [tools/gen_d1_interval.py](../../tools/gen_d1_interval.py) が生成する。
**SVG を手で編集しない。**実測値はそのファイルの `MEASURED` が唯一の出どころ。

**測り方。**左（セントラル）を C4 の治具に載せ、電池 ＋ テスター（mA 直列）で
読む。右（ペリフェラル）はタクトのボード。**毎回 `c` `d` が打てることを
確かめてから読んだ**（繋がっていないペリフェラルを測る間違いを避けるため）。

#### 結果

| 間隔 | 電流（高い側） | 寿命 | 合計遅延 | 打鍵感 |
|---|---|---|---|---|
| **7.5ms**（ZMK の既定） | 0.66mA | 4.2 ヶ月 | 14.25ms | — |
| **15ms** | **0.47mA** | **5.8 ヶ月** | 18.0ms | 違和感なし |
| 30ms | 0.38mA | 7.2 ヶ月 | 25.5ms | 違和感なし |
| 60ms | 0.33mA | 8.3 ヶ月 | 40.5ms | **違和感なし** |
| 480ms（2 変数） | 0.28〜0.30mA | — | 247ms | **明確に体感** |

**電流 ＝ 0.28mA ＋ 2.85 ÷ 間隔[ms]**

**7.5 と 15 の 2 点だけで作った式が、30・60・480ms を予言した。**当てはめに
使っていない点で当たったので、これは合わせ込みではない。

#### **床が 0.28mA ある**

**分割リンクをどれだけ削っても 0.28mA は残る。**480ms（分割リンクの寄与が
0.006mA）で 0.28〜0.30mA に着地したことで確定した。

→ **電池をさらに伸ばしたいとき、次に削るのは分割リンクではない。**
「接続間隔をもっと伸ばせば」という発想は、ここで頭打ちになる。

#### 交換レート

| | 得るもの | 払うもの | 1ms あたり |
|---|---|---|---|
| **7.5 → 15ms** | **＋1.7 ヶ月** | 遅延 ＋3.75ms | **0.45 ヶ月** |
| 15 → 30ms | ＋1.4 ヶ月 | 遅延 ＋7.5ms | 0.19 ヶ月 |
| 30 → 60ms | ＋1.1 ヶ月 | 遅延 ＋15ms | 0.07 ヶ月 |

**7.5 → 15ms がいちばん割がいい。**そこから先は効率が落ちる。

#### 変数を 1 つずつ動かした

**タイムアウトを変えずに伸ばせるのは 64ms まで。**

```
BLE の規格:  タイムアウト > (1 + latency) × 間隔 × 2
既定（4 秒・latency 30）:  間隔 < 4000 / 62 = 64.5ms
```

**480ms にはタイムアウト 32 秒（規格の上限）が要る。**変数が 2 つになるので、
**対照実験をした。60ms のままタイムアウトだけ 32 秒にして焼き直したら
0.33 / 0.28mA が再現した。→ タイムアウトは電流に効かない。**

**この対照は利用者の指摘で入った。**「まずは接続間隔以外いじりたくない」。
**入れていなければ、480ms の点は 2 つの変数が混ざったまま結論に使われていた。**

#### ⚠️ 未解明 — 低い側の値

**触っていないのに、電流が 2 つの値を行き来する。**

| 間隔 | 低い側 | 高い側 |
|---|---|---|
| 7.5ms | 0.49 | 0.66 |
| 15ms | 0.30 | 0.47 |
| 30ms | 0.29 | 0.38 |
| 60ms | **0.28** | 0.33 |

**低い側は 15ms 以降ほぼ床に張りついている。**60ms の 0.28mA は、
高い側の当てはめから出た床とぴったり同じ。

**出る条件が分かっていない。**打鍵のあとに落ちることも、何もせずに
落ちることもある。**だから条件間の比較には使っていない。**上の表と
寿命の計算は、すべて**高い側**（＝厳しい側）で書いてある。

> **一度「低い側も反比例する」と当てはめて、30ms で 0.205mA と予測した。
> それは床 0.28mA を割る値で、最初から成立していなかった。**
> 利用者の指摘（「0.29 ≒ 0.28 は床では」）で気づいた。
> **2 点だけで引いた線を、3 点目が来る前に信じていた。**

**ログで追った（2026-08-10・段 2）。**15ms ＋ UART ログで焼き、Saleae で
4 分ぶん取った（配線は [img/saleae-uart-hookup.svg](img/saleae-uart-hookup.svg)）。
ログを出すぶん電流は底上げされ、**0.90 → 0.77mA**（落ち幅 0.13mA）で
同じ現象が起きた。

```
起動 1.8 秒   スキャン開始
     5.4 秒   le_param_updated: interval 12 latency 0 timeout 72   ← ホスト
    13.5 秒   split_central_connected
    13.5 秒   New connection params: Interval: 12, Latency: 30, PHY: 1
    14.4 秒   security_changed: level 2
    ────────  **以降 4 分間、1 行も出ない**
    60〜80 秒  ← **電流が落ちた瞬間。ログは沈黙**
```

**分かったこと 2 つ。**

**① 接続間隔の設定は効いている。**`Interval: 12` ＝ 15ms。**「設定しただけでは
効いていない」ではなかった。**（8.75ms が当てはめから外れる件は、別の理由か
ばらつき）

**② 低い側は、BLE の接続の変化ではない。**

| 疑い | 判定 |
|---|---|
| ホストが接続間隔を変えた | **否定。**`le_param_updated` が出ていない |
| 分割リンクが張り直された | **否定。**`connected` / `disconnected` が出ていない |
| nRF52840 のコントローラ側の省電力 | **残る** |
| ZMK / Zephyr の電力管理（ログに出ない経路） | **残る** |

**接続パラメータは最初から最後まで同じ。**それでも電流が 2 つの値を行き来する。

#### ⚠️ ログという道具の限界（**上の結論を弱める**）

**ZMK は起動直後にしか喋らない。**接続が落ち着けば、DBG でも出す事が無い。

| 取り込み | 出たログ |
|---|---|
| 4 分 | device 時刻 14 秒までの分だけ。以降ゼロ |
| 2 分 | 10 秒ぶんだけ（電池報告を 10 秒周期にしていたとき） |
| **5 分** | **ゼロ。**その間に電流は落ちている |

**「ログに何も出ていない」は「何も起きていない」ではない。**ZMK が報告
しない層で起きていれば、同じように沈黙する。**上の「BLE の接続の変化では
ない」は、この弱さを抱えた結論。**

#### 現象の形（2026-08-10・テスターでの観察）

| | |
|---|---|
| **切り替わりの速さ** | **1〜2 秒で完了。**なだらかではない → **状態の切り替え** |
| **低い状態の長さ** | **60 秒／72 秒**（2 回の観察。**一定ではない**） |
| 高い状態の長さ | 60〜80 秒／3 分／4 分 20 秒／5 分 32 秒／6 分（**一定ではない**） |

> **一度「低い状態はちょうど 60 秒」と書いたが、誤り。**1 回の観察を
> 「きっかり」と書いてしまい、2 回目（72 秒）で崩れた。
> **周期性は確認できていない。**

**どちらの長さも一定ではない。**固定タイマーではなく、**確率的なもの**
（電波の再送・ホスト側の都合・アンカーのずれなど）の可能性がある。

**電池報告ではないことは変わらない。**この観察の時点で左右とも 10 秒に
変えてあった。

#### 次は電流を時系列で取る

**テスターの数字を目で追う限り、ここが限界。**

→ **オシロで GND 側シャントを読む。**配線は
[img/scope-shunt-hookup.svg](img/scope-shunt-hookup.svg)。
**USB を挿さなくなったので GND 側に入れられる**ようになった（片側が電池の
− なので、普通のプローブ 1 本で読める）。100Ω で 0.3〜0.9mA が 30〜90mV。

**ログではなく電流を見るのが要点。**ログは ZMK が報告することしか見えない
が、電流は何が起きても見える。

**それでも詰まるなら `CONFIG_BT_HCI_LOG` や `CONFIG_PM_LOG_LEVEL_DBG`。**
ただし **`BT_CONN_LOG_LEVEL_DBG` で装置を壊した領域の隣**なので慎重に。

#### 電池報告について分かったこと（副産物）

`CONFIG_ZMK_BATTERY_REPORT_INTERVAL` の既定は **60 秒**
（ZMK の `app/Kconfig.defaults`）。**10 秒に縮めると、高い状態が長く続いた**
（左右それぞれで確認）。**電池報告は高い状態を引き起こす要因の 1 つではある。**

**ただし低い状態の長さ（60 秒）は説明できない。**10 秒周期にしても 60 秒
だった。**「これが原因」とは言えない。**

> **「電池報告を伸ばせば電池が伸びる」と一度書いたが、根拠が無い。撤回する
> （2026-08-10・利用者の指摘）。**
>
> **直接の消費を数えれば桁が違う。**接続イベント 1 回に数バイト乗せるだけ
> なので、長く見ても 1ms × 6mA ＝ 6µC。60 秒に 1 回なら**平均 0.1µA
> ＝ 0.0001mA。**測っている 0.66mA の 1 万分の 1。ADC の測定も同じ桁。
> **100 倍見積もっても効かない。**
>
> **間接的な効果（報告が引き金で何かの状態に入る）も立証できていない。**
> 10 秒周期でも結局落ちたし、低い状態の長さは周期を変えても変わらなかった。
>
> **削りしろの候補から外す。**低い側の謎が解けたときの手がかりとしてのみ残す。

#### 床の内訳を割ろうとした（2026-08-10）— **途中まで**

**BLE を丸ごと切って測った。**

| 状態 | 実測（7.5ms のとき） |
|---|---|
| Mac と接続・右とも接続 | **0.65mA** |
| Mac の Bluetooth オフ（＝広告中）・右と接続 | **0.55mA** |
| **`CONFIG_ZMK_BLE=n`**（無線が全部止まる） | **0.03mA** |

**無線を全部止めると 0.03mA。**CPU の待機・DC/DC の静止・分圧 1.5µA の
合計がこれ。**予想よりずっと小さい。床 0.29mA の 9 割は無線。**

```
0.65mA
 ├─ 分割リンク          0.345mA   （2.591 ÷ 7.5。8 点の当てはめから）
 ├─ CPU・DC/DC・分圧     0.03mA   （実測）
 └─ **残り              0.275mA**  ← **割れていない**
```

**残りは「ホストと接続しているときの無線」と「BLE スタックが動いている
こと自体の代償」の合計。**この 2 つを分けるには「分割リンクだけ生かして、
ホスト側だけ止めた左」が要るが、**ZMK では作れない。**

| やりたいこと | できない理由（ソース） |
|---|---|
| ホスト側 BLE だけ切る | `ZMK_SPLIT_BLE` が `depends on ZMK_BLE`（`app/src/split/Kconfig`） |
| 広告だけ止める | `update_advertising()` は**未接続なら必ず広告**（`app/src/ble.c`）。タイムアウト無し |
| ペリフェラル役割だけ外す | `ZMK_BLE` が `select BT_PERIPHERAL`（`app/Kconfig:165`）。`select` は `.conf` で上書きできない |

**BLE の仕様としては「広告もせず接続もしない」は正当な状態。**作れないのは
ZMK の方針。**キーボードは電源を入れたら勝手に繋がるべき**なので、設計としては
筋が通っている（HHKB のように「キーを押したら広告」にするには、押されたことを
知るための走査が要り、そちらで電気を食う）。

#### 次にやること — ホストの接続間隔を変えて割る

**分割リンクを 8 点で割ったのと同じ手を、ホストリンクに使う。**

```
全体 ＝ 底 ＋ K分割 ÷ 分割の間隔 ＋ Kホスト ÷ ホストの間隔
                                     ↑ ここを動かす
CONFIG_BT_PERIPHERAL_PREF_MIN_INT / MAX_INT   （単位 1.25ms）
```

**⚠️ Apple の規則を踏まえて値を選ぶこと。**

```
Accessory Design Guidelines §49.6
  接続間隔は 15ms 以上、かつ **15ms の整数倍**
```

**これで macOS が 15ms を選んだ理由が説明できる。**ZMK は 7.5〜15ms を
希望していたが、**その範囲で Apple の規則を満たすのは 15ms だけだった。**
**「最大値を採った」ではなく「それしか採れなかった」。**

→ **次に試す値は 30ms・45ms・60ms**（15 の倍数）。7.5 の倍数ではない。

**実際に何が採られたかは、必ずログの `le_param_updated` で確かめること。**
希望と結果は違いうる（latency 30 を希望して 0 を返された前例がある）。

**これは要素 4（ホストリンクを長くする価値）の実測を兼ねる。**

##### ファームの置き場所

**`~/Downloads/firmware/2026-08-10/`**（セッションの一時領域は消えるので移してある）

| ファイル | 中身 |
|---|---|
| `baseline--left.uf2` / `baseline--right.uf2` | **基準**（`b893283`・7.5ms・ログ無し・電池報告 60 秒） |
| `host-30ms--left.uf2` ほか 3 本 | ホスト間隔 30/45/60ms・**測定用** |
| `host-30ms-log--left.uf2` ほか 3 本 | 同・**確認用**（UART ログ入り） |
| `matrix--proto_direct.uf2` ほか 3 本 | **マトリクスの上乗せを測る用**（下記） |
| `settings_reset.uf2` | ボンドを消す救済用 |

**大きさで取り違えを防げる。**左のファームは **469,504 バイト**（ログ入りは
569,344）。**115KB 級なら BLE が入っていない**ので、焼く前に気づける。

> **2026-08-10 に 6 本を無駄にした。**`CONFIG_ZMK_BLE=n` を消し忘れたまま
> 土台にしていた。**サイズを基準と比べて気づいた。**ビルドの script に
> 400KB 未満なら止まる検査を入れてある。

##### 測る前の予測（**2026-08-10 に固定。あとで書き換えない**）

**結果を見てから説明を作るのを防ぐため、先に書いておく。**
今日それを 3 回やった（低い側の 0.205mA・電池報告・「ちょうど 60 秒」）。

**macOS が何を返すか**

| 結果 | 見立て | 根拠 |
|---|---|---|
| **30ms を採る（latency は 0）** | **70%** | Apple の規則を全部満たす |
| 15ms のまま返す | 20% | latency 30 を拒否した前例がある |
| 拒否／接続しない | 10% | 規則は満たしているので低いはず |

**電流がいくつになるか**

前提: **無線イベント 1 回あたりの費用は、分割リンクもホストリンクも同じ**
（`Khost = Ksplit = 2.591`）。すると ホストリンク(15ms) = 0.173mA、
底 = 0.132mA。

| ホスト間隔 | **予測** | 減り |
|---|---|---|
| 15ms（いま） | 0.65mA | — |
| **30ms** | **0.56mA** | −0.09 |
| **45ms** | **0.54mA** | −0.11 |
| **60ms** | **0.52mA** | −0.13 |

**外れ方から分かること**

| 実測 | 意味 |
|---|---|
| **0.51mA を下回る** | **ありえない。**底の実測 0.03mA と矛盾する。測定か仮定の誤り |
| 0.51〜0.56 | ホストリンクは分割リンクより 1 イベントあたり高い |
| **0.56 前後** | **仮定どおり。**1 イベントの費用は役割によらない |
| **0.60 以上** | **ホストリンクは安い。**残り 0.275mA の大半が「底」＝**壁が厚い** |
| 0.65 のまま | 希望が通っていない（`-log` の確認と矛盾する） |

##### 採否の基準 — **いまは決めない（材料を揃えるまで）**

**2026-08-10 に、利用者と話して決めたこと。**

| | |
|---|---|
| **順序** | **分割リンクをどうするか先に決め、そのうえでホストを考える** |
| **理由** | 分割のほうが 1ms あたりの効率が良い。ただし**左右差を生む**（上記） |
| **左右差の上限** | **未定。**体感できる範囲かどうかで決める（D2） |
| **そもそも要るか** | **未定。**595 の上乗せと完成品の実測しだい。いま 4.2 ヶ月で仕様は満たしている |

> **私（Claude）は「合計遅延 18ms までは採る」という基準を提案したが、
> 利用者が却下した。**「いまは判断材料が無い。判断ではなく材料を揃えるまで」。
> **左右差の軸を私が見落としていたので、その提案は前提から不足していた。**

##### 実測 — **ホストリンクは触れた**（2026-08-10）

**30ms が受理された。しかも latency 30 も一緒に通った。**

```
[00:00:04.207] le_param_updated: interval 12 latency 0  timeout 72    ← macOS の初期値
[00:00:08.492] le_param_updated: interval 24 latency 30 timeout 400   ← **希望が通った**
```

| ホスト間隔 | latency | 判定 | 電流 | 寿命 |
|---|---|---|---|---|
| **15ms**（macOS の初期値） | 0 | 基準 | **0.65 / 0.49mA** | 4.2 ヶ月 |
| **30ms** | **30** | **受理** | **0.43mA** | **6.5 ヶ月** |
| 45ms（タイムアウト 4 秒のまま） | — | **拒否** | 0.49（＝基準の低い側） | — |

**−0.22mA。34% 減。**

###### なぜ今まで拒否されていたか — **こちらの要求が規則違反だった**

**Apple Accessory Design Guidelines（2026-06-08 版）§58.6 の原文。**
PDF を取得して `pdftotext` で読んだ。**検索結果の要約ではない。**

```
Connection parameter requests may be rejected if they do not meet these guidelines.

  Peripheral Latency ≤ 30 connection intervals.
  Supervision Timeout from 6 seconds to 18 seconds.
  Interval Min ≥ 15 ms.
  Interval Min ≤ 2 seconds.
  Interval Min is a multiple of 15 ms.
  One of the following:
      Interval Max at least 15 ms greater than Interval Min.
      Interval Max and Interval Min are both 15 ms.
  Interval Max * (Peripheral Latency + 1) of 6 seconds or less.
  Supervision Timeout greater than Interval Max * (Peripheral Latency + 1) * 3.
```

**ZMK の既定（min 7.5ms）は「Interval Min ≥ 15 ms」と「15ms の倍数」の
両方に違反する。**→ macOS は**要求ごと捨てて**、自分の初期値
（15ms・latency 0）を使い続ける。

**「macOS は latency を拒否する」と今日ずっと書いていたが、誤り。**
**拒否されていたのは要求全体で、原因は latency ではなく間隔の下限だった。**

###### **macOS が実際に見ているのは 1 行だけらしい**

**私たちの 30ms の要求（min=max=30・latency 30・timeout 4 秒）は、
規則を 2 つ破っているのに通った。**

| 規則 | 30ms の要求 | |
|---|---|---|
| latency ≤ 30 | 30 | ✅ |
| **timeout は 6〜18 秒** | **4 秒** | **❌ 違反。なのに通った** |
| min ≥ 15ms・15ms の倍数 | 30 | ✅ |
| **max ≥ min ＋ 15ms（または両方 15ms）** | **30 = 30** | **❌ 違反。なのに通った** |
| max ×(lat+1) ≤ 6 秒 | 0.93 秒 | ✅ |
| **timeout ＞ max ×(lat+1)×3** | 4,000 ＞ 2,790 | ✅ |

**45ms が落ちたのは、最後の 1 行だけの違い。**

| 要求 | timeout ＞ max×(lat+1)×3 | 判定 | 実測 |
|---|---|---|---|
| 30ms | 4,000 ＞ **2,790** | ✅ | **通った** |
| 45ms | 4,000 ＜ **4,185** | ❌ | **拒否された** |
| 60ms | 4,000 ＜ **5,580** | ❌ | （未測定） |

**タイムアウト 4 秒のままで通せる上限は 43ms。**だから **30ms が、15 の倍数で
通る最大値**だった。**偶然ではない。**

**45・60 を通すにはタイムアウトも伸ばす必要がある**（45ms → 5 秒、60ms → 6 秒）。
**規則上は 6〜18 秒が正しいが、macOS はそこを見ていないようだ。**

**HID についての注記もある。**

```
If Bluetooth Low Energy HID is one of the connected services of an accessory,
a connection interval down to 11.25 ms may be accepted by some devices.
```

###### ⚠️ これは macOS 1 台での観察

**Apple の規則は Apple 機器の話。**Windows は別の判断をする。
**「30ms にすれば 34% 減る」は、いまのところ macOS でしか確かめていない。**
→ **Windows 11 でも同じことを測る**（下記）。

###### 交換レート — **分割リンクと並べる**

| | 得るもの | 払う遅延 | 1ms あたり | **左右差** |
|---|---|---|---|---|
| 分割 7.5 → 15ms | ＋1.7 ヶ月 | ＋3.75ms | 0.45 ヶ月 | **増える** |
| **ホスト 15 → 30ms** | **＋2.3 ヶ月** | **＋7.5ms** | **0.31 ヶ月** | **増えない** |

**latency は打鍵の遅延を増やさない。**「送るものが無ければ飛ばしてよい」
仕組みなので、キーを押せば次の接続イベントで送れる。**増えるのは間隔のぶんだけ。**

> **予測（0.56mA）は外れた。**「latency は 0 のまま」を前提にしていたため。
> **前提が崩れた形の外れ方**なので、測定前に訂正して記録した（0.38〜0.45mA）。
> 実測 0.43mA はその範囲。

##### 内訳が完全に割れた（2026-08-10）

**45ms でも 0.43mA から下がらなかった。**ホストリンクはもう測定分解能
（0.01mA）以下、ということ。そこから逆算すると:

| | 値 | 出どころ |
|---|---|---|
| **CPU・DC/DC・分圧** | **0.030mA** | `ZMK_BLE=n` の実測 |
| **BLE スタックが動いている代償** | **0.055mA** | 差し引き |
| **分割リンク（7.5ms）** | **0.345mA** | 8 点の当てはめ |
| **ホストリンク（15ms・latency 0）** | **0.220mA** | 差し引き |
| **合計** | **0.650mA** | **実測と一致** |

**「割れない」と言っていた残り 0.275mA が、0.22（ホスト）＋ 0.055（スタック）
に割れた。**

###### 効いているのは間隔ではなく latency だった

```
15ms・latency 0   → 67 回/秒     → 0.220mA
15ms・latency 30  → **2.2 回/秒** → **0.007mA**
30ms・latency 30  → 1.1 回/秒     → 0.004mA
45ms・latency 30  → 0.7 回/秒     → 0.002mA
```

**latency 30 が通った時点で、ホストリンクはほぼ消える。**間隔を伸ばしても
減るものが残っていない（30ms も 45ms も 0.43mA で同じだった）。

**→ 間隔を伸ばすという発想そのものが要らなかった。**

###### 候補 A — **遅延を 1ms も払わない**

**Apple の規則には例外がある。**

```
One of the following:
    Interval Max at least 15 ms greater than Interval Min.
    **Interval Max and Interval Min are both 15 ms.**   ← これ
```

| 候補 | 打鍵の遅延 | 予測電流 | 規則 |
|---|---|---|---|
| いま（15ms・latency 0） | 7.5ms | 0.65mA | — |
| **A: 15/15ms・latency 30・timeout 6 秒** | **7.5ms（変わらない）** | **0.44mA** | **完全に適合** |
| C: 30/45ms・latency 30・timeout 6 秒 | 15〜22.5ms | 0.43mA | 適合 |

**A なら 32% 減って、打鍵の遅延は 1ms も増えない。**

###### 検証済み — ×3 の規則は macOS が実際に見ている

**間隔を変えず、タイムアウトだけ変えて結果が反転した。**

| 要求 | timeout | ×3 の判定 | 結果 |
|---|---|---|---|
| 45ms・latency 30 | **4 秒** | 4,000 ＜ 4,185 ❌ | **拒否** |
| 45ms・latency 30 | **5 秒** | 5,000 ＞ 4,185 ✅ | **受理**（`interval 36 latency 30 timeout 500`） |

**「45ms だから落ちた」ではなく「タイムアウトが足りなかった」。**

##### 本番の設定に何を選ぶか（**規則を守ったものだけ**）

**利用者の判断（2026-08-10）: 「違反しているが通った」設定は候補に入れない。**

> **いまの 30ms の設定は、文書化された規則を 2 つ破っている**
> （timeout 4 秒 ＜ 6 秒、min = max ≠ 15ms）。**通ったのは、いまの macOS が
> そこを見ていないだけ。**OS の更新で挙動が変われば、要求ごと落ちて
> 0.65mA に**静かに**戻る。利用者は電池が減ったことにしか気づかない。

**規則を守った候補（latency は常に 30。遅延を増やさないので）**

| # | Min | Max | timeout | 打鍵の遅延（平均） |
|---|---|---|---|---|
| **A** | **15ms** | **15ms**（例外規定） | 6 秒 | **7.5ms** |
| B | 15ms | 30ms | 6 秒 | 7.5〜15ms |
| C | 30ms | 45ms | 6 秒 | 15〜22.5ms |
| D | 45ms | 60ms | 6 秒 | 22.5〜30ms |
| … | … | … | … | … |
| 上限 | 165ms | 180ms | 17 秒 | 82.5〜90ms |

**A で足りるなら、それ以降は要らない。**

##### 候補 A の実測（2026-08-10）— **0.43mA。遅延は据え置き**

```
[00:00:10.897] le_param_updated: interval 12 latency 30 timeout 600
                                 ↑ 15ms・latency 30・タイムアウト 6 秒
```

**2 回の接続で 2 回とも受理された。**

| | 電流 | 打鍵の遅延（左/右） | 寿命 |
|---|---|---|---|
| 基準（15ms・latency 0） | **0.63mA** | 10.5 / 14.25ms | 4.3 ヶ月 |
| **候補 A（15ms・latency 30）** | **0.43 / 0.44mA** | **10.5 / 14.25ms** | **6.3〜6.5 ヶ月** |

**遅延は 1ms も増えていない。分割リンクにも触っていない。左右差も同じ。**
**2 回測った**（2 回目はボンドを消してペアリングし直したあと）。

###### 手順を固定してやり直したら、一発で成功した（**候補 A は無罪**）

**Mac の登録を消し、左右とも `settings_reset` してから、途中で電源を
入れ直さずに 1 回だけ試した。**

| 段階 | 電流 | |
|---|---|---|
| 左だけ ON | **2〜3mA** | スキャン中（**基準の 3.65mA より低い。違和感あり・下記**） |
| 左右 ON | **0.56mA** | 分割リンク接続 ＋ Mac へ広告中 |
| **ペアリング** | — | **一発で成功** |
| 接続後 | **0.43mA** | **3 回目の測定**（0.43 / 0.44 / 0.43） |

→ **前回できなかったのは、ログ入りのファームが原因。**候補 A の
パラメータは無罪。

**副産物: 広告の電流が出た。**

```
0.56mA ＝ 底 0.085 ＋ 分割リンク 0.345 ＋ **広告 0.13mA**
```

**7.5ms のときの「Bluetooth オフで 0.55mA」とも辻褄が合う。**

###### 参考 — 切り分けに穴があった経緯

**一度「ボンドが無い状態からペアリングできない」と結論しかけたが、切り分けに
穴があった。**

| 試したもの | 接続パラメータ | **ログ** | ペアリング |
|---|---|---|---|
| 候補 A（1 回目） | 15ms・latency 30 | **有り** | **失敗** |
| 基準（対照） | 15ms・latency 0 | **無し** | 成功 |
| **候補 A（2 回目）** | 15ms・latency 30 | **無し** | **成功**（ただし電源を入れ直す必要があった） |

**1 回目と対照は、パラメータとログの 2 つが違っていた。**「パラメータが原因」
とは言えない。**ログ入りのファームが原因だった可能性が高い**（今日、
`BT_CONN_LOG_LEVEL_DBG` でも同じように BLE 接続が確立しなくなった）。

**ただし 2 回目も一発では繋がらず、左右の電源を入れ直して成功した。**
→ **「ログが原因」も確定していない。**

**確かめるなら、ログ無しで複数回ペアリングし直す。**いまはリセットボタンの
往復が重いので、**先に `&bootloader` / `&bt BT_CLR` をキーに割り当てる**（下記）。

###### 未解明 — 左だけを起動したときの電流が基準より低い

**基準では 3.65mA だった**（右を探してスキャン中）。**候補 A では:**

| 回 | 実測 |
|---|---|
| 1 回目 | **1.16mA** |
| 2 回目 | **2〜3mA** |

**スキャンの設定はホストリンクと無関係なので、下がる理由が無い。**
**説明がつかない。**記録だけ残す。

**低い側の謎（別セッションへ投げた件）と同じ系統かもしれない。**
「触っていないのに電流が動く」という点で共通している。

**原因の見当（未検証）**

```
接続 → **約 5 秒後に Zephyr が接続パラメータの更新を要求**
       （CONFIG_BT_CONN_PARAM_UPDATE_TIMEOUT の既定）
     → ペアリングの途中で latency 30 が効き始める
     → 装置が 465ms に 1 回しか聞かなくなり、やり取りが停滞する
```

**ログでも、更新は接続から約 5 秒後に来ている**（10.9 秒 − 6.0 秒）。
**ペアリングにかかる時間と重なりうる。**

**試す価値のある手**

| | |
|---|---|
| `CONFIG_BT_CONN_PARAM_UPDATE_TIMEOUT` を伸ばす | ペアリングが終わってから要求させる |
| latency を下げる（例: 10） | 効果は落ちるが、停滞しにくくなるか |

**この 2 つを試すまで、候補 A は採用できない。**

> **今日ここまで、30ms・45ms は「既存のボンドがある状態」でしか試していなかった。**
> **ペアリングそのものを試したのは、候補 A が初めてだった。**
> → **30ms・45ms でも同じ問題が起きる可能性がある。**確かめていない。

##### 内訳の精度を上げた（2026-08-10・**推論を外した**）

**latency 0 に固定して、ホストの間隔を振った。**ホストリンクが大きいまま
測れるので、「45ms で下がらない → ほぼ 0」という推論を使わずに済む。

```
電流 ＝ 0.447mA ＋ 2.725 ÷ ホストの間隔[ms]      （latency 0 のとき）

 15ms  実測 0.63  当て 0.629   残差 +0.001
 45ms  実測 0.50  当て 0.508   残差 −0.008
 60ms  実測 0.50  当て 0.493   残差 +0.007
```

| 出し方 | 底 |
|---|---|
| 推論あり（午後） | 0.085mA |
| **2 点（15・45）** | **0.090mA** |
| **3 点（15・45・60）** | **0.102mA** |

**0.085〜0.102。ばらつき 0.017mA**（分解能 0.01mA の 2 倍以内）。
→ **「底は 0.09mA 前後」**と言ってよい。

###### 独立の裏づけ — 候補 A を当てた

```
候補 A の予測 ＝ 底 ＋ 分割リンク 0.345 ＋ Khost ÷（15 × 31）
              ＝ **0.441〜0.453mA**
実測          ＝ **0.43 / 0.44 / 0.43 / 0.43mA**
```

**latency 0 の掃引から作った式が、latency 30 で測った値を当てた。**
別々の測定なので、独立の裏づけになる。

###### 内訳（確かさが上がった版）

| | 値 | 根拠 |
|---|---|---|
| CPU・DC/DC・分圧 | **0.030mA** | 直接の実測（`ZMK_BLE=n`） |
| **BLE スタック** | **0.060mA** | 底 0.09 − CPU 0.03（**推論なし**） |
| 分割リンク（7.5ms） | **0.345mA** | 8 点の当てはめ |
| **ホストリンク（15ms・latency 0）** | **0.182mA** | Khost ÷ 15（**推論なし**） |
| 合計 | **0.617mA** | 実測 0.63 |

###### ⚠️ 60ms は、採られた値を確かめていない

**60ms を要求したときの実測が 0.50mA で、45ms とまったく同じだった。**
**当てはめの予測は 0.493 なので残差の範囲**だが、**ログで採られた値を
読んでいない。**60ms が拒否されて 45ms 相当で動いていた可能性は残る。

**確かめるなら `-log` 入りを 1 本作って `le_param_updated` を読む。**

##### ⚠️ やり残し（**済み**）— 内訳の精度を上げる

**いまの内訳のうち、③④⑤は推論を 1 つ挟んでいる。**

```
③ 底 0.085mA        ← 「45ms で下がらなかった → ホストはほぼ 0」から
④ ホスト 0.220mA    ← ③ に依存した引き算
⑤ スタック 0.055mA  ← ③ に依存した引き算
```

**小さすぎて測れないものを「0」と置いている**のが弱点。

**直し方: latency を 0 に固定して、間隔を振る。**そうすればホストリンクが
大きいまま測れるので、分割リンクを 8 点で割ったのと同じ手が使える。

| 設定 | ホストの起床 | 実測 |
|---|---|---|
| **15ms・latency 0** | 67 回/秒 | **0.63mA**（済み） |
| **45ms・latency 0** | 22 回/秒 | 未 |
| **60ms・latency 0** | 17 回/秒 | 未 |

```
電流 ＝ C ＋ Khost ÷ 間隔      → **底 ＝ C − 0.345**（推論なし）
```

**`Khost` が出れば、候補 A のホストリンク（いま 0.007mA と計算した値）も
計算で確定する。**ビルド 2 本。

##### 治具にキーを 2 個足した（2026-08-10・**リセットボタンから解放**）

**配線は [img/left-jig-keys.svg](img/left-jig-keys.svg)。**既存の配線は 1 本も
動かしていない。

| キー | 割り当て | 効果 |
|---|---|---|
| 左 ①（9・11 列） | **`&bootloader`** | **左**がマウント。リセット 2 回押しが不要 |
| 左 ②（15・17 列） | **`&bt BT_CLR`** | ホストのボンドを消す。`settings_reset` が不要 |
| 右 1（10・12 列） | `&kp C` | 繋がり確認用 |
| 右 2（14・16 列） | **`&bootloader`** | **右**がマウント |

**全部動いた。そして電流も 0.43mA のまま**（配線を足しても変わらなかった）。

###### ⚠️ キーマップを解釈するのはセントラル（左）だけ

**右のファームにキーマップが入っていても使われない。**

```
右のキーを押す → 「4 番目が押された」という位置情報だけが左へ送られる
              → **左のキーマップ**が振る舞いを決める
              → &bootloader なら **押された側（右）** で実行される
```

**`&bootloader` が押された側で動くのは、そう作られているから。**

```c
// app/src/behaviors/behavior_reset.c
.locality = BEHAVIOR_LOCALITY_EVENT_SOURCE
```

**`&bt` は locality の指定が無いので既定の CENTRAL。**常に左で実行される
（ホストのプロファイルはセントラルが持つので、それが正しい）。

> **一度「右にキーマップを焼けば右のキーが効く」と説明したが誤り。**
> 左を焼くまで、右のキー 2 は古いキーマップの `d` のままだった。

###### つまずいた 3 つ（記録）

| | |
|---|---|
| `&bootloader` の `&` | **SVG の特殊文字。**逃がさないと図の描画ごと落ちる |
| `BT_CLR` | **`dt-bindings/zmk/bt.h` の include が要る。**無いと devicetree が読めない |
| 上半分の行の並び | `i` は `f` より上。min/max で取らないとスイッチの胴体が描かれない |

##### ⚠️ 治具に手を入れたら、既知の値で確かめてから進む

**キーを 2 個足すと条件が変わる可能性がある**（配線が増える・GND の経路が
変わる）。**新しい測定に入る前に、既知の条件で同じ値が出るか確かめること。**

| 確かめる条件 | 既知の値 |
|---|---|
| 基準（ZMK の既定） | **0.63 / 0.49mA** |
| 候補 A | **0.43mA**（4 回とも。0.43 / 0.44 / 0.43 / 0.43） |

> **2026-08-10 の朝、これで救われた。**別セッションが触ったあとの治具を
> 組み直し、**基準で 0.65 / 0.49 を再現してから** D1 の掃引に入った。
> 再現を確かめずに進んでいたら、8 点の当てはめごと疑わしくなっていた。

##### ★ Windows 11 でも同じだった（2026-08-10）

**候補 A（15ms・latency 30・timeout 6 秒）を焼いて、両方のホストで測った。**

| ホスト | 電流 |
|---|---|
| **macOS** | **0.43mA** |
| **Windows 11** | **0.43mA** |

**同じ設定が、両方で同じように効く。**別々の値を探す必要は無かった。

###### Windows は `le_param_updated` を出さなかった

**40 秒の取り込みで 1 回も出ていない。**接続の 5.9 秒後にログが沈黙し、
そのあと何も来ない。

**それでも電流は 0.43mA だった**（ログ入りで 0.88mA。macOS の候補 A ＋ ログ
0.87mA とほぼ同じ）。

| 可能性 | |
|---|---|
| **Windows が最初からその値で繋いだ** | 更新が要らないのでログも出ない |
| Windows の既定がもともと省電力寄り | |

**どちらでも結論は変わらない**（同じ電流になる）。**が、どちらかは分かっていない。**

##### 候補 A の測り終えた姿（**決定ではない。材料**）

> **利用者の方針（2026-08-10）: 「ここでは判断するのではなく、判断のための
> 材料を揃えるまで」。**一度「本番の設定が決まった」と書いたが、越権だった。

```
CONFIG_BT_PERIPHERAL_PREF_MIN_INT=12    # 15ms
CONFIG_BT_PERIPHERAL_PREF_MAX_INT=12    # 15ms（Apple の例外規定）
CONFIG_BT_PERIPHERAL_PREF_LATENCY=30
CONFIG_BT_PERIPHERAL_PREF_TIMEOUT=600   # 6 秒
```

| | 電流 | 打鍵の遅延（左/右） | 左右差 | 寿命 |
|---|---|---|---|---|
| ZMK の既定 | 0.63mA | 10.5 / 14.25ms | 3.75ms | 4.4 ヶ月 |
| **候補 A** | **0.43mA** | **10.5 / 14.25ms** | **3.75ms** | **6.5 ヶ月** |

**遅延を 1ms も払わない。分割リンクにも触らない。左右差も増えない。**
**Apple の規則にも完全に適合する**（違反したまま通っている設定は採らない、
という利用者の判断に沿う）。

###### 採否を決めるには、まだ足りない

| | |
|---|---|
| **そもそも変える必要があるか** | **いまの 4.4 ヶ月でも仕様（3〜8 ヶ月）を満たしている** |
| **本番の床** | **595 とマトリクスの上乗せが未測定。**ここが大きければ話が変わる |
| **ホストが戻してくる影響** | 候補 A で起きるか未確認。**起きるなら 0.43 は上限で、平均は 0.63 との間** |
| 打鍵感 | D2。完成品ができてから |

**「払うものが無い」も、確かめきっていない。**

| 気になっていること | |
|---|---|
| ペアリングが不安定になる場面があった | 原因未特定（60ms のファームの疑いが濃いが、確定していない） |
| Windows で `le_param_updated` が出ない | **理由が分かっていない。**同じ電流にはなったが、機序は不明 |
| タイムアウト 6 秒 | 既定の 4 秒より、**切断の検知が遅くなる**。実使用で気になるかは未確認 |

##### （元の計画）Windows 11 でも同じことを測る

**やることは macOS と同じ。**`-log` を焼いて `le_param_updated` を読むだけ。
**ファームも配線もそのまま使える。**

| 見るもの | なぜ |
|---|---|
| **既定（min 7.5ms）で何が採られるか** | Windows が通すなら、**Windows のほうが元から省電力**（latency 30 が効く） |
| **30ms の要求が通るか** | 通れば、**両方で同じ設定が使える** |
| 別の値を強いてこないか | 設定を決めるとき、**両方を満たす形**を探す必要がある |

**本番の設定は、macOS と Windows の両方で通る形にする必要がある。**
片方だけを見て決めると、もう片方で要求ごと捨てられる（＝いま起きていたこと）。

##### 次回の手順（**ファームはビルド済み**）

**2026-08-10 に 6 本ビルドして置いてある。**分割リンクは既定の 7.5ms のまま
なので、**今日の基準 0.65mA と直接比べられる。**変えたのはホスト側だけ。

| ファーム | ホスト間隔 | ログ |
|---|---|---|
| `30ms` `45ms` `60ms` | 30 / 45 / 60ms | 無し（**測定用**） |
| `30ms-log` `45ms-log` `60ms-log` | 同上 | 有り（**採られた値の確認用**） |

**手順は 1 つの間隔につき 2 段階。**

1. **`-log` を焼き、Saleae で `le_param_updated` を読む**
   （配線は [img/saleae-uart-hookup.svg](img/saleae-uart-hookup.svg)）
   → **希望した値が採られたかを先に確かめる。**違えばそこで止める
2. **ログ無しを焼き、Saleae を外して電流を読む**
   （Saleae のプローブは微小な負荷になりうるので、基準と同じ環境に揃える）

**⚠️ 測る前に毎回 `c` `d` が打てることを確かめる。**繋がっていない状態を
測ると、待機電流ではなくスキャン中の値（3.7mA 級）になる。

##### 何が出るか

```
全体 ＝ 底 ＋ 2.591 ÷ 7.5 ＋ Kホスト ÷ ホスト間隔
        ↑ 分割リンクは 0.345mA で固定

3 点あれば Kホスト と 底 が分離できる（分割リンクを 8 点で割ったのと同じ手）
```

**同時に、要素 4 の交換レートが出る。**ホストリンクの間隔を伸ばすと、
**払うのは遅延の鎖でいちばん大きい項**（平均 7.5ms → 15ms → 22.5ms）。
分割リンクより分が悪いが、**数字で比べられるようになる。**

**macOS が拒否する可能性もある。**そのときは `-log` の段階で分かるので、
**無駄な電流測定をせずに済む。**

#### マトリクスの上乗せを測る（**配線が要らない**）

**本番の床がいくつになるかは、これでしか分からない。**基板を待たなくてよい。

**知りたいことは 2 つあって、道具が違う。**

| # | 何 | 595 が要るか |
|---|---|---|
| **①** | **キーを押していないとき、ZMK は走査し続けるか** | **要らない** |
| ② | SPI の周辺回路が待機中も食うか | 要る |

**①が大きい。**走査し続けるなら 0.1〜1mA 級、割り込みで待つなら µA 級。
**行は本番でも MCU 直結**（`&xiao_d 1..5` ＋ `wakeup-source`）なので、
**列が 595 か直結かは待機の振る舞いに関係しない。**

**そして、スイッチを繋ぐ必要も無い。**走査し続けるかどうかはファームの動きで、
スイッチの有無に関係しない。**595 も同じ**（ZMK は MISO を使わないので、
チップが居なくても SPI の書き込みは走る）。

→ **C4 の治具に焼いて電流を読むだけ。配線は一切変えない。**

```
proto_matrix − proto_direct  ＝ 行列走査の上乗せ（①）
proto_shift  − proto_matrix  ＝ SPI の上乗せ（②）
```

**⚠️ ~~3 つとも Mac に単独で繋がる別の装置として見える~~**
→ **誤り。2026-08-11 に conf を読んで判明。3 つとも `CONFIG_ZMK_BLE=n`。**
**どのホストとも繋がらない。**ホスト側の状態を揃える必要は無い。

**だからこの 3 本で出るのは「無線を除いた床」である。**
本番の床は **この値 ＋ BLE の分**。無線ぶんは C3 側で別に測ってある
（BLE スタック 0.060 ＋ 分割リンク 0.345 ＋ ホストリンク）。

**⚠️ USB を挿さないこと。**`CONFIG_ZMK_USB=y` なので、挿すと USB 給電に
なって電流計が 0.01mA を指す（2026-08-11 に実際にやった）。

**74LVC595 の静止電流は考えなくてよい**（最大 10µA 級 × 2 ＝ 0.02mA）。
**「595 が食う電流」と「595 を使うことで SPI 周辺回路が食う電流」は別物。**

##### ★ 実測（2026-08-11）— **床は増えない**

```
proto_direct   **0.03mA**
proto_matrix   **0.02mA**
proto_shift    **0.03mA**

proto_matrix − proto_direct  ＝ **−0.01mA**  ← 分解能そのもの
proto_shift  − proto_matrix  ＝ **+0.01mA**  ← 同上
```

**行列の走査も、SPI による 595 の駆動も、待機中の電流に乗らない。**
ZMK は行の割り込みで寝ていて、押されるまで走査していない。
→ **キーが 4 個でも 60 個でも、待機電流は同じ。**

| | |
|---|---|
| 走査の上乗せ | **0.00mA**（測定限界以下） |
| SPI の上乗せ | **0.00mA**（同上） |
| 74LVC595 の静止電流 | 0.02mA（**データシート。実測ではない。**治具に 595 は載っていない） |

**「本番の床が 0.60mA 級になる」という心配は消えた。**
**電池寿命を決めているのは無線だけである。**

```
床（CPU ＋ 走査 ＋ 595）  0.03 ＋ 0.02 ＝ **0.05mA**   ←  8%
BLE スタック                            0.06mA
**分割リンク（7.5ms）                    0.345mA**      ← **55%。最大**
ホストリンク                     0.006〜0.18mA
```

##### ⚠️ 測り方 — **書き込んだあとは電池を入れ直す**

**2026-08-11 に実際に起きたこと。**`proto_direct` を焼いて USB を抜いたら
**0.47mA。**電池を入れ直したら **0.03mA。**

```
USB を挿したまま起動  → VBUS があるので nRF の USB 周辺回路が有効になる
USB を抜くだけ        → ソフトは USB を掴んだまま。**高速クロックが回り続ける**
電池を入れ直す        → VBUS 無しで起動。USB は最初から立ち上がらない
```

**`CONFIG_ZMK_USB=y` のファームは、必ず電池を入れ直してから測ること。**

**候補 A（BLE 側）は影響を受けなかった。**電池を入れ直して測り直して **0.43mA**
（今日 Mac で測った値と同じ）。**BLE のファームでは USB の残りは乗らない。**

##### ⚠️ この 3 本には `&bootloader` が無い

**`proto_direct` の D1 は `&kp B`。**①・② のキーは `proto_split` の
キーマップにしかないので、**この 3 本を焼いている間はリセットボタンの
2 回押しに戻る。**次に焼くときは、3 本のキーマップに `&bootloader` を
1 つ足しておくとボタンを温存できる。

#### 寿命の見積もりを詰めるのは、あとで（2026-08-10 に判断）

**いまの月数は 2 つのぶん楽観的。**

| | 効き方 |
|---|---|
| **電池が減ると電圧が下がり、DC/DC の入力電流は増える** | 終盤で 1.3 倍。**寿命を短くする向き** |
| **本番はマトリクスと 595 が乗る** | 床が増える。**同じく短くする向き** |

**どちらも「表の月数は上限」を意味する。**

**詰めるのは、ホストリンクの内訳と 595 の上乗せが確定してから。**
先に計算しても、前提が動けば引き直しになる。

#### ⚠️ ~~★ 解けた — ホストが、あとからパラメータを自分の値に戻す~~（2026-08-10）
#### **→ 2026-08-11 に宙へ戻した。下の「候補 A を 2 つのホストで測り直した」を先に読む**

> **この節の結論は確定していない。**根拠は `le_param_updated` が 1 回出たこと
> だけで、**同じ時刻に切断が起きていなかったかを見ていない。**再接続の直後にも
> まったく同じ行が出ることが、2026-08-11 に分かった（Mac は接続のたびに
> `interval 12 latency 0 timeout 72` を投げてくる）。
>
> **確かめ方は残っている。**当時の取り込みが保存されていれば、t=287.8 秒の
> 前後に `disconnected` の行があるかを見るだけでよい。
> **あれば再接続。無ければ本当に戻された。**

**今日ずっと追ってきた「電流が 2 つの値を行き来する」の正体。**

##### 捕まえた瞬間

**60ms・latency 0 ＋ UART ログで 5 分取り込み、電流を見ながら待った。**

```
取り込み 287.8 秒（4 分 48 秒）
  [00:05:30.393] le_param_updated: 3C:A6:F6:3F:0B:48 (public): interval 12 latency 0 timeout 72
                                                               ↑ **macOS の初期値**

利用者の報告 4 分 50 秒ごろ
  電流 0.90 → 1.05mA
```

**時刻が一致した。**装置は 60ms を要求して受理されていたのに、
**macOS があとから 15ms・latency 0 に戻した。**

##### これで説明がつくもの

| 今日の観察 | 説明 |
|---|---|
| 触っていないのに 2 つの値を行き来する | **ホストが戻す／装置が要求し直す** |
| 切り替わりが 1〜2 秒 | パラメータ更新は一瞬 |
| 差が 0.17mA 前後 | **ホストリンク（15ms・latency 0）の実測 0.182mA とほぼ一致** |
| 滞在時間がばらばら（60 秒／72 秒／3 分／6 分） | ホスト側の都合。周期性が無いのも当然 |
| 60ms で 0.50 ⇄ 0.65 | **0.65 は 15ms のときの電流そのもの** |

> **「ログに何も出ないから BLE の変化ではない」と一度結論しかけたが、誤りだった。**
> あのときは 5 分の取り込みで**フレームが 0** だった。**ログが出ていなかっただけ。**
> **道具が沈黙していることを、対象が沈黙している証拠にしていた。**

##### 測定への影響 — **引き直しが要る**

**今日の値は、どちらの状態を読んだかで変わる。**

| 条件 | 記録した値 | 疑い |
|---|---|---|
| 基準（ZMK の既定・7.5ms 分割） | 0.63 / 0.49 | **要求が常に拒否されるので、パラメータは動かないはず。**別の要因？ |
| **候補 A**（15ms・latency 30） | **0.43 × 4 回** | **4 回とも同じ。安定していた可能性が高い** |
| 45ms・latency 0 | 0.50 / 0.45 | 混ざっている疑い |
| **60ms・latency 0** | 0.65 / 0.50 | **確実に混ざっている** |

**→ ホストリンクの当てはめ（`0.447 ＋ 2.725 ÷ 間隔`）は、引き直しが要る。**

**候補 A は影響が小さいと思われる**（4 回とも 0.43）。**が、確かめていない。**

##### 次に確かめること

| | |
|---|---|
| **戻される周期** | 何分ごとか。**1 回しか捕まえていない** |
| **戻されたあと、装置は要求し直すか** | 行き来しているので、しているはず。**ログで確かめる** |
| **候補 A でも戻されるか** | 戻されるなら、**平均電流は 0.43 と 0.63 の間のどこか**になる |
| 基準（7.5ms）の 2 状態 | **要求が拒否される構成なのに 2 状態がある。**別の要因が残っている疑い |

**3 番目が本番の設定に直結する。**候補 A で戻されるなら、
**「32% 減」は戻されるまでの間だけの話**になる。

#### ★★ 統合 — **どこを変えると、どれだけ効くか**（2026-08-11）

**[img/power-model.svg](img/power-model.svg) が、この節の全部を 1 枚にしたもの。**
生成器は [tools/gen_power_model.py](../../tools/gen_power_model.py)。
**SVG を手で編集しない。**係数を変えたら生成器を直して実行する
（当てはめが実測を当てることを、書き出すたびに検査している）。

##### 式

```
I ＝ 床 0.090 ＋ 2.591 ÷ 分割間隔[ms] ＋ 2.725 ÷ ( ホスト間隔[ms] × (latency+1) )
```

**前提は 1 つだけ。「無線イベント 1 回あたりの費用は、相手が右手でもホストでも
同じ」。**2 つの係数が独立の実験から 2.591 と 2.725 という近い値になったことが、
その傍証になっている。

**latency は「送るものが無いとき、何回まで聞き逃してよいか」。**
送るものがあれば次の機会に送るので、**打鍵の遅延は増えない。**
これが「latency はタダ、間隔は有料」という非対称の正体。

##### 内訳と、設定を変えたときの姿

| 構成 | 分割 | ホスト | 床 | 分割リンク | ホストリンク | 合計 | 寿命 | 実測 |
|---|---|---|---|---|---|---|---|---|
| **既定（ZMK のまま）** | 7.5ms | 15ms・lat 0 | 0.090 | **0.345** | **0.182** | 0.617 | 4.4 ヶ月 | **0.63** |
| **候補 A** | 7.5ms | 15ms・**lat 30** | 0.090 | **0.345** | 0.006 | 0.441 | 6.2 ヶ月 | **0.43** |
| 候補 A ＋ 分割 15ms | 15ms | 同上 | 0.090 | 0.173 | 0.006 | 0.269 | 10.2 ヶ月 | 未 |
| 候補 A ＋ 分割 30ms | 30ms | 同上 | 0.090 | 0.086 | 0.006 | 0.182 | 15.0 ヶ月 | 未 |

**電池 2000mAh 換算。本番はこれに 74LVC595 ×2 の静止電流 0.02mA が乗る**
（データシート。実測ではない）。

##### レバーの一覧 — **効く順**

| 変えるもの | 電流 | 寿命 | 払う代償 | 何で確かめたか |
|---|---|---|---|---|
| **分割リンク 7.5 → 30ms** | −0.259 | **＋8.8 ヶ月** | **右手だけ平均 +7.5ms。左右差** | 8 点の当てはめ（実測） |
| **分割リンク 7.5 → 15ms** | −0.173 | **＋4.0 ヶ月** | **右手だけ平均 +3.75ms。左右差** | 同上 |
| **ホストの latency 0 → 30** | −0.176 | **＋1.8 ヶ月** | **なし** | 実測（Mac・Windows とも 0.43）＋ログで受理を確認 |
| ホストの間隔 15 → 60ms | −0.004 | ＋0.1 | 両手に +22ms | 式。**分解能以下で実測できない** |
| マトリクス走査・595 を止める | 0.00 | ±0 | — | 実測（direct 0.03 / matrix 0.02 / shift 0.03） |
| 電池報告の間隔を延ばす | 0.000 | ±0 | — | 計算（60 秒に 1 回の通知） |

**寿命の伸びは分割リンクのほうが大きいが、代償を払うのも分割リンクだけ。**
**ホストの latency は、代償ゼロで取れる唯一のレバー。**

> **⚠️ 分割リンクの「左右差」がなぜ起きるか。**
> 左手のキーは分割リンクを通らずホストへ行く。右手のキーだけが
> 分割リンクを 1 回余分に渡る。**だから分割間隔を伸ばすと、右手だけが遅れる。**
> ホストの間隔を伸ばした場合は両手に等しく乗るので、左右差は生まれない。

##### どこまで確かか

| | |
|---|---|
| **強** | 分割リンクの係数 2.591 — 7.5〜60ms を 8 点。残差 ≤0.034mA |
| **強** | 床が増えないこと — 3 本のファームで実測。差はすべて分解能以下 |
| **強** | 候補 A の 0.43mA — Mac・Windows・電池入れ直しで一致。macOS はログで受理を確認 |
| **弱** | ホストリンクの係数 2.725 — 3 点だが 45ms と 60ms が同値。**実質 2 点・未知数 2 個で自由度 0。残差が小さいのは当然で、当てはまりの証拠にならない** |
| **弱** | 45/60ms の点 — 電流が 2 値を行き来する中から低い側を目で選んだ。ログで裏を取っていない |
| **未** | Windows の実際の interval / latency — 更新イベントを出さないので読めない |
| **未** | 0.87 → 0.96mA の 0.09（ログ入りファーム）。電池電圧は 3.00V で変わらず。**説明が無い** |

**弱い 2 つが効くのは「ホストの間隔を伸ばす価値」を語るときだけ。**
latency 30 が通っている以上その価値は最大 0.006mA なので、
**引き直す実益は薄い。**

##### ★★ 4 段目まで実測になった／**測定を 4 時間汚していたもの**（2026-08-11）

**図の 3・4 段目（分割 15ms・30ms）と、送信電力を実測した。**
**5 点すべてが式と 0.01mA 以内で一致する。**

| 構成 | 予測 | **実測** |
|---|---|---|
| 既定 | 0.617 | **0.63** |
| 候補 A | 0.441 | **0.43 / 0.44** |
| 候補 A ＋ 分割 15ms | 0.269 | **0.27** |
| 候補 A ＋ 分割 30ms | 0.182 | **0.18**（0.14〜0.21 で振れる） |
| 候補 A ＋ TX +8dBm | 0.541 | **0.54** |

**下の 3 つは、値を先に書いてから測って当てた。**

###### ⚠️ `&uart0` の宣言だけで **＋0.46mA** 乗る

**最初に測った分割 15ms は 0.75mA だった。**間隔を伸ばしたのに増えている。
**3 回焼き直しても再現した。**

| 焼いたもの | 実測 |
|---|---|
| 昨日の候補 A（分割 7.5ms） | 0.43 |
| 今日の分割 15ms | **0.75**（3 回） |
| 今日の分割 15ms・候補 A 抜き | 0.74 |
| **今日の分割 7.5ms（＝昨日と同じ設定）** | **0.90** ← **ここで分かった** |
| **同上・overlay の `&uart0` を外す** | **0.44** |

```
config/boards/shields/proto_split/proto_split_left.overlay
    zephyr,console = &uart0;
    &uart0 { status = "okay"; current-speed = <1000000>; };
```

**`CONFIG_LOG` も `CONFIG_SERIAL` も切ってあるのに、この宣言があるだけで
UART が初期化され、高速クロックが回り続ける。**ファイルの大きさに出ていた
（470,528 B → **474,112 B**）。

**昨日 0.43mA を測ったビルドは、この宣言を足す前のものだった。**
だから昨日と今日を比べていた 4 時間、**下駄を履いた値と履いていない値を
比べていた。**

###### 撤回 — **「ログは +0.44mA」は誤り**

```
0.43（UART 無し・ログ無し）  →  0.87（UART 有り・ログ有り）   差 +0.44
0.44（UART 無し・ログ無し）  →  0.90（UART 有り・ログ無し）   差 +0.46
                                 0.88（UART 有り・ログ有り）
```

**＋0.44 の中身はほぼ全部 UART で、ログを出すこと自体はほとんどタダだった。**

###### 教訓 — **`[一時的]` は conf だけでなく overlay にもある**

**conf のログ 6 行は外したのに、overlay は外していなかった。**
「ログ無しのビルド」と呼んでいたものが、ログ無し・**UART 有り**だった。

```
電流を測るときに外すもの
  conf     CONFIG_LOG / CONFIG_ZMK_LOG_LEVEL_DBG / CONFIG_LOG_BACKEND_UART
           CONFIG_SERIAL / CONFIG_CONSOLE / CONFIG_UART_CONSOLE
  overlay  **zephyr,console = &uart0 と &uart0 { status = "okay"; }**  ← これ
  見分け方 **ファイルの大きさ 470,528 B（素）／ 474,112 B（UART）／ 571,392 B（ログ）**
```

###### 送信電力 — **ZMK の「無視できる」は、この装置には当てはまらない**

**[ZMK の文書](https://zmk.dev/docs/troubleshooting/connection-issues)は
「接続に問題があるなら PLUS_8 を勧める。消費電力の差は無視できる」と書いている。**

```
Nordic のデータシート（DC/DC・3V）  TX 0dBm 4.9mA ／ +8dBm 14.1mA   差 9.2mA
分割リンク 7.5ms ＝ 133 回/秒、空パケット 80µs（1M PHY）

  計算  9.2mA × 80µs × 133 = **＋0.098mA**
  実測  0.44 → **0.54mA**   ＝ **＋0.10mA**
```

**寿命で 6.2 → 5.1 ヶ月。1.1 ヶ月を払う。**
ZMK の記述は**ホストと 30ms 以上で繋ぐだけの普通のキーボード**の話で、
**133 回/秒 の分割リンクを持つこの装置には当てはまらない。**

**タダではないので、既定で最大にはしない。**電波の余裕が要ると分かってから、
**必要なだけ**上げる。**技適の条件（認証時の送信電力）も未確認。**

###### 送信電力を**下げる**側と、2M PHY（2026-08-11・利用者の提案）

**上げる側だけ調べて終わりにしかけた。**利用者から「減らす側も調べては」と
指摘があり、測った。

| 設定 | 実測 | 差 |
|---|---|---|
| TX +8dBm | 0.54 | **＋0.10** |
| **TX 0dBm（既定）** | **0.44** | — |
| TX −20dBm | **0.43** | **−0.01**（分解能） |
| 2M PHY をやめて 1M | **0.49** | **＋0.05** |

**送信電力は非対称だった。**

```
上げる  0 → +8dBm    ＋0.10mA   **1.1 ヶ月を払う**
下げる  0 → −20dBm   −0.01mA   **0.15 ヶ月しか買えない**
```

**電力を 1/100 にしても電流が動かない。**送信の費用を決めているのは
電力設定ではなく、**電波に乗っている時間**である。**既定の 0dBm が正解**で、
下げるのは接続の余裕を捨てるだけになる。

###### ★ 2M PHY は**すでに効いていた**（私の「未検討のレバー」は誤り）

```
CONFIG_BT_CTLR_PHY_2M      既定 y
CONFIG_BT_AUTO_PHY_UPDATE  既定 y（接続時に自動で PHY 更新を仕掛ける）
```

**切ると 0.44 → 0.49mA。**2M は**0.05mA（0.7 ヶ月）をすでに稼いでいる。**
**「2M にする」という手は残っていない。**

###### 3 つの実測が、同じ絵で辻褄が合った

```
2M の送信時間 40µs（空パケット）＋ 立ち上がり 40µs ＝ **PA が入っている 80µs**

  +8dBm の代償   9.2mA × 80µs × 133 回/秒 = 0.098mA   実測 **+0.10**
  1M にする      送信 40 → 80µs が増える              実測 **+0.05**
  −20dBm         PA を絞っても時間は変わらない         実測 **−0.01**
```

**別々に測った 3 点が、1 つの機構で説明できる。**

###### ★ 電波の余裕 — **治具では 20dB 以上余っている**

**TX −20dBm（既定の 1/100 の電力）でも、左右を 1〜1.5m 離して打鍵できた。**

**⚠️ 片道だけの試験。**左だけ −20dBm にしたので、弱くなったのは
「左 → 右」の向きのみ。**それでも分割リンクに 20dB 級の余裕があることは言える。**

**これは open-gaps #23（アンテナが地板に挟まれている）の見通しになる。**
**20dB 余っているなら、多少削られても保つ**——ただし**本番基板で測るまでは
見通しであって、結論ではない。**

###### ★ 0.87 → 0.96mA の 0.09 が解けた — **電池を入れ直したかどうか**

**同じファーム（候補 A ＋ ログ）で、2 通りの手順を続けて測った。**

| 手順 | 実測 |
|---|---|
| **USB を抜くだけ**（電池はそのまま） | **0.97mA** |
| **そのあと電池を入れ直す** | **0.88mA** |
| | 差 **0.09** |

**USB を抜いてもソフトは USB を掴んだままで、周辺回路が回り続ける。**
`proto_direct` で見た 0.47 → 0.03 と同じ現象が、BLE のファームでは 0.09 として出る。

**2026-08-10 の 0.87 と 2026-08-11 の 0.96 は、この差だった。**
→ **「電池を入れ直してから読む」は必須。**測り方の 4 か条の 2 番目が、
これで 2 度目の裏づけを得た。

###### ホストリンクの係数の測り直し — **測定系のばらつきに阻まれた**（打ち切り）

**やり方は正しく組めた。**

```
latency 0 に固定（ホストリンクを大きいまま測る）
Apple の規則を守った Min/Max（Min は 15 の倍数・Max = Min+15）
ログ入りどうしで比べる（UART の下駄は共通項として落ちる）
**採られた間隔をログで確認する**
```

**1 点目でこれが確認できた。**

```
要求  Min 15ms / Max 30ms / latency 0 / timeout 6s
受理  **interval 12 ＝ 15ms**   ← **macOS は範囲の下限を採る**
```

**しかし電流が回ごとに 0.93 ⇄ 1.04mA を行き来した。**電源を 9 回入れ直して
ログと突き合わせた結果：

| 起動 | ログ | 電流 |
|---|---|---|
| 6 回 | `timeout 72` → `timeout 600`（要求が通った） | 1.04 側 |
| **2〜3 回** | **`timeout 72` のみ**（要求が通っていない） | 0.93 側 |

**interval も latency も、9 回すべて 12 / 0 で同じだった。**
**supervision timeout は無線の起床回数を変えない**（2026-08-10 に対照実験済み）。
→ **0.11mA の差は、接続パラメータでは説明できない。**

**打ち切った理由。**

| | |
|---|---|
| 判断に効く量 | **最大 0.006mA**（latency 30 が通っているため） |
| 測定系のばらつき | **±0.11mA ＝ 効く量の 18 倍** |
| テスターの分解能 | 0.01mA |

**桁が足りない。**意味のある精度で測るには電流計を替える（オシロ ＋ シャントで
波形を積分する）必要があり、**0.006mA の判断のために払う手間ではない。**

**係数 2.725 は「弱い」ままで確定とし、図にもそう書いてある。**

> **副産物: macOS は接続パラメータの要求を、9 回中 2〜3 回無視する。**
> 無視されるのは timeout だけで、interval と latency は毎回通った。
> **候補 A（latency 30）が無視される例は、今日 1 度も無い。**

###### 打鍵感 — **弱い観察**

**分割 15ms でも 30ms でも「特に違和感なし」**（利用者・2026-08-11）。
**ただし目隠しではなく、条件を知っていて、右の 1 キーだけ。**
**判断材料にはならない。**D2（完成品での目隠し試験）は別に要る。

##### ★★ 支配的なのは「広告中」だった／**スリープは寿命を 3 倍にしている**（2026-08-11）

**利用者の指摘から始まった。**

> 一番多いのは「左右ともに電源入れっぱなし。PC だけ日々シャットダウン。
> 夜間、左がホストと切れている」と考えます。

**そのとおりだった。**そして**候補 A がこの状態を相対的に高くしていた。**

| 状態 | 実測（候補 A・2026-08-11） |
|---|---|
| 左・接続中 | **0.44mA** |
| **左・ホストへ広告中**（右とは接続） | **0.55mA** |

**候補 A は「繋がっている状態」を安くしたので、触れない広告状態のほうが
高くなった。**既定（接続 0.65）のときは広告 0.55 のほうが安く、
**私はその古い測定のまま「広告は安い」と書いていた。**

###### 1 日の使い方で計算する

**PC が動くのは 1 日 8.5 時間、残り 15.5 時間は広告中**という想定。

| スリープの時限 | 候補 A | ＋分割 15ms | ＋分割 30ms |
|---|---|---|---|
| **30 分（いま）** | **16.2 ヶ月** | 26.0 ヶ月 | 38.3 ヶ月 |
| 2 時間 | 13.5 ヶ月 | 21.3 ヶ月 | 30.6 ヶ月 |
| 8 時間 | 8.1 ヶ月 | 12.3 ヶ月 | 17.0 ヶ月 |
| **スリープしない** | **5.4 ヶ月** | 8.1 ヶ月 | **10.9 ヶ月** |

**仕様は 3〜8 ヶ月。表のどれもが満たす。**
**「空打ちが嫌ならスリープを切る」という選択が、現実に取れる。**

###### ⚠️ 撤回 — 「8 時間にしても損失 1.5%」

**私は「右だけ不在」というレアなケースだけで計算していた。**
**毎晩広告している**という支配的なケースを勘定に入れていなかった。
**実際は 30 分 → 8 時間で寿命が半分になる**（16.2 → 8.1 ヶ月）。

**そして、スリープを外すと 3 分の 1 になる**（16.2 → 5.4 ヶ月）。
**外してよい機能ではなかった。**

##### ★ 寝起きの試験（2026-08-11・ブレッドボードで実測）

**左右は別々に寝る。**片方を起こしても、もう片方は寝たまま——という疑いを
実測で確かめた。スリープの時限を **1 分**にし、左の 2 つ目を
`&bt BT_CLR` → **`&kp A`** に変えたファームで行った
（左に副作用の無い普通のキーが 1 つも無いと試験できない）。

| | 結果 |
|---|---|
| ① 1 分強で電流が **0.00** に落ちるか | **落ちる。**深いスリープに入っている |
| ② **右のキーで左が起きるか** | **起きない。**文字も出ず、電流も上がらない |
| ③ 左のキーで左が起きるか | **起きる。ただし 1 打目は文字にならず、復帰だけに使われる** |
| ④ 片方を起こしたら右も起きるか | **未測定**（右を USB 給電にしたため。電池が要る） |

**深いスリープは nRF52840 の SYSTEM OFF で、自分の GPIO でしか起きない。**
②はその裏づけ。**④ も同じ原理なので同じ結果になるはず**だが、
**推論であって実測ではない。**

###### 復帰にかかる時間 — **10 秒**

**2 打目が入るまで、体感で約 10 秒。**
**しかもこれは良いほうの値で、右が USB 給電で起きたままだった。**
**本番は右も寝ているので、さらに伸びる可能性がある。**

```
本番で予想される復帰（**推定**）

  30 分放置 → 右手から打つ  → **無反応**（左が寝ている）
             左手 1 打      → **消える。**約 10 秒待つ
             左手が入るようになる
             右手 1 打      → **消える**（右が寝ている）
             右手も入るようになる
```

###### ⚠️ 判断は、実機と突き合わせてから

**基本方針は「HHKB オリジナルの使い勝手を極力踏襲する」。**
**実機の HHKB Professional HYBRID も自動スリープを持つ。**

```
実機も 1 打を捨てて数秒待たせる → **欠陥ではなく再現。許容範囲**
実機のほうが速い               → **差として記録し、詰めるかを決める**
```

**測り方: 実機を 30 分以上放置し、1 打目の文字が出るか・入るまで何秒かを見る。**
**未着手。**

##### 測定環境 — **再現するときはこれと同じにする**

**配線図: [img/breadboard-c4.svg](img/breadboard-c4.svg)**（生成器 `tools/gen_breadboard_c4.py`）。
**左のキーは [img/left-jig-keys.svg](img/left-jig-keys.svg) を足した状態。**

```
電池（単3 ×2）＋ → ① シャント → ② スライドスイッチ → ③ 1N5819（ショットキー）
                                                        → レール（XIAO の 3V3 ピン）
                    ⑥ 100µF バルク（レール ↔ GND）
                    ④⑤ 1MΩ ＋ 1MΩ の分圧 → D0（電池残量）
```

| | |
|---|---|
| 電流の読み方 | テスターを **29 列 c 行 ↔ 27 列 c 行**（シャントの位置）に直列・mA レンジ |
| 分解能 | **0.01mA** |
| ログ | D6 → Saleae Logic（CH0 ＝ 7 列 c 行、GND ＝ 2 列 i 行）・1Mbps 8N1・[img/saleae-uart-hookup.svg](img/saleae-uart-hookup.svg) |
| 右（ペリフェラル） | `keys--right.uf2`・**基準のまま動かさない** |
| 電池電圧 | **3.00V**（テスター実測）。ZMK の ADC は 2.95V ＝ **50mV 低く読む** |
| ホスト | Mac（3C:A6:F6:3F:0B:48）／ Windows 11（CC:47:40:6B:6C:55） |

**⚠️ 測るたびに守ること。**

```
1. **USB を抜く**（挿さっていると USB 給電になり 0.01mA になる）
2. **電池を入れ直してから読む**（USB スタックが起きたままだと +0.44mA）
3. **測らない側のホストの Bluetooth を切る**（古いボンドが接続 → 即切断を繰り返す）
4. **ログ入りファームは +0.44mA。**素の値と混ぜない
```

##### ⚠️ 本番基板でどれだけ動くか — **回路を端から端まで数えた**（2026-08-11）

**この測定は C4 の治具（ショットキー・100µF・分圧つき）での値である。**
本番との差を、`tools/circuit.py`（回路の唯一の出どころ）から数えた。

**治具にすでに載っているもの**（本番と同じ）: ショットキー・100µF バルク・
1MΩ×2 の分圧・XIAO 本体。**電源の鎖の形は本番と同じ。**

**本番の左だけに増えるもの:**

| 部品 | 数 | 待機時の電流 | 出どころ |
|---|---|---|---|
| SN74LVC595A | **1**（右は 2） | **≦0.010mA** | データシート ICC max 10µA。**実測ではない** |
| 0.1µF パスコン | 3 | ≦0.001mA | MLCC の絶縁抵抗。事実上ゼロ |
| BAT46W | 27 | **0** | スイッチが開いていれば経路が無い |
| ホットスワップソケット | 27 | 0 | 同上 |
| FFC コネクタ | 1 | 0 | 信号のみ |
| **合計** | | **≦0.011mA** | **0.43mA の 2.6%** |

**→ 待機電流は、本番でもほぼ変わらない。**

**打鍵中だけ増えるもの（治具には存在しない経路）:**

```
列（595 が High）→ キースイッチ → BAT46W → 行の内部プルダウン → GND

I = (レール 2.80 − 列の降下 0.20 − ダイオード 0.30) ÷ プルダウン
  = **0.144mA**（16kΩ）〜 **0.209mA**（11kΩ）  ← **押している 1 キーあたり**
```

**待機中は 0。押している間だけ流れる。**
1 分間に 300 打鍵・1 打鍵 100ms なら滞空率 50% で **＋0.07〜0.10mA**、
1 日 2 時間打つとして日平均 **＋0.007mA**。**寿命への影響は小さい。**

##### ⚠️ 本当に効きうるのは、部品ではなく電波

**上の表は「静的な部品」しか数えていない。**大きく動きうるのは 2 つで、
**どちらも数えられていない。**

| | なぜ効くか | いまの状態 |
|---|---|---|
| **アンテナ（[open-gaps #23](open-gaps.md)）** | **地板に挟まれると放射が落ちる。**届かなければ再送が増え、再送は無線イベントそのもの | **未解決。発注前に決める 1 件** |
| **左右の距離と人体** | 治具は数 cm・机の上。本番は 30cm 以上離れ、**手と腕が間に入る**。2.4GHz は人体で減衰する | **未測定** |

```
分割リンク 0.345mA は「再送が無い」前提の値
→ **再送が 10% 増えれば 0.38mA、30% 増えれば 0.45mA**
→ **候補 A で稼いだ 0.176mA を、電波環境が食い潰しうる**
```

**だから「基板が届いてから測る」が正しい。**
**RSSI と再送率は §6-6 で測る予定のまま未着手。**

##### そもそも変えるべきかは、基板が届いてから

**利用者の方針（2026-08-11）: 「それは、基板が届いて計測してから判断するもの」。**
治具の値は本番の値ではない。**この節は材料であって、決定ではない。**

#### ★ 候補 A を 2 つのホストで測り直した（2026-08-11）

**この日いちばん効く事実は 1 行で言える。**

> **候補 A は、macOS でも Windows 11 でも、同じ電流で動く。**
> **macOS については、受理されたことがログの文字列で確認できている。**

##### 何をどう測ったか

| | |
|---|---|
| ファーム | `keys-candA-log--left.uf2`（候補 A ＋ UART ログ）／右は `keys--right.uf2` |
| 電源 | **電池のみ。USB は抜く**（挿すと USB 給電になり、電流計は 0.01mA を指す） |
| ログ | D6 → Saleae。取り込み → 解析器を後付け → CSV |
| 電池 | 2856〜2966mV（`bvd_sample_fetch` がログに出る。**別のテスターは要らない**） |

##### 確定したこと

**① macOS は候補 A を受理する。6 回とも同じ。**

```
connected                                            get_selected_transport: **USB is not ready. Falling back to BLE**
le_param_updated: interval 12 latency  0 timeout  72   ← macOS 自身の値
le_param_updated: interval 12 **latency 30** timeout 600 ← 接続から **4.36 秒**
```

**電池給電で、HID が BLE を通っている状態**での受理である
（`USB is not ready` がそれを示す）。**6 回すべてで 4.36 秒。**

**② 26 分間、何も起きなかった。**

| 証拠 | 中身 |
|---|---|
| UART ログ | `le_param_updated` **0 回**・切断 **0 回**（フレームそのものが 0） |
| 電流計の画面録画 | **0.88mA がほぼ一定** |

**独立な 2 つが同じことを指している。**剥がされていれば 1.04 付近へ跳ぶ。

> **フレーム 0 を「対象の沈黙」と読む前に、道具が生きていることを見せた。**
> 同じ手順で 90 秒撮って 2,649 フレーム取れることを確認してから、
> 26 分の沈黙を解釈している。

**③ Windows 11 でも電流が同じ。**

```
Mac     0.96mA  （latency 30 と確認済み）
Windows 0.96mA  （数分差・同じ電池・同じファーム）
```

**latency 0 なら 1.14 付近になるはずだった。ならなかった。**
→ **電力の上では同等**。今日の「Mac 0.43 / Windows 0.43」とも整合する。

##### ⚠️ Windows の実際の値は読めていない

**Windows は `le_param_updated` を出さない。**Zephyr のこのコールバックは
**パラメータが変わったときにしか呼ばれない**ので、更新が起きなければ 1 行も出ない。
**6 回つなぎ直して 0 回。**

```
ペアリング直後に 1 回だけ出た: interval 12 latency 0 timeout 960
その **3ms 後**に切断（reason 0x28 ＝ LMP 応答タイムアウト）
→ **折衝の途中の値。定常値ではない**
```

**再起動しても繋ぎ直しても出ない。**読むには装置側から
`bt_conn_get_info()` を定期的に印字するしかない（**ビルド 1 本。未着手**）。

**ただし判断には要らない。**知りたいのは電池の持ちで、それは電流が答えている。

##### 撤回（この日だけで 4 件）

| 書いたこと | 実際 | 何を間違えたか |
|---|---|---|
| 「Mac が 60 秒ごとに切断している」 | **利用者の BT 切→入** | 切断→再接続が 5.4/6.0/5.9 秒＝指示した「5 秒置いて」と一致していたのに、珍しいほうを取った |
| 「0.87→0.96 は電圧で説明できる（残差 0.01mA）」 | **使えない数字を代入していた** | 3292mV は **USB 接続中＝充電中**の端子電圧。0.87 のときの電圧は記録が無い |
| 「Windows は latency 30 を通さない」 | **不明** | **イベントが出ないことを「latency 0 のまま」と読んだ。**沈黙を証拠にした（この日 2 回目） |
| 「積集合と呼べるのは候補 A だけ」 | **測った点が 1 つなだけ** | 測った範囲を全体として書いた |

**型は 1 つ。「見えたものが少ない」ことを「無い」と読んでいる。**

##### 未解明のまま残すもの

| | |
|---|---|
| **0.87 → 0.96mA の 0.09** | 同じ設定・同じ接続状態なのに違う。**電池の減りが有力だが、0.87 のときの電圧を記録していないので確かめられない** |
| **0.96 が 3 条件で動かない** | Mac＋Windows 割り込み中／Mac だけ／Windows だけ、すべて 0.96。**割り込みは電流に乗らなかった** |
| **Windows の interval / latency** | 上記のとおり読めていない |

##### 副産物 — 測るときの作法

```
1. **USB を抜く。**挿さっていると USB 給電になり、HID も USB へ流れる
   （`get_selected_transport: USB is preferred and ready` が出る）
2. **測らない側のホストの Bluetooth を切る。**古いボンドが残っていると
   接続 → 即切断を繰り返す（CC:47:40:6B:6C:55 が 7 回）
3. **電池電圧はログから読む。**`bvd_sample_fetch` が毎回出る
4. **ログ入りファームは電流が ＋0.44mA。**素の値と混ぜない
```

**左の治具には無害な起こし方が無い**（① は `&bootloader`、② は `&bt BT_CLR`）。
接続をやり直させたいだけなら、**ホスト側の Bluetooth を切→入すれば足りる**
（左手には触らない）。

#### 別セッションへ — 2 状態を行き来する原因を探す（**上で解けた。残りは周期と頻度**）

**装置は要らない。ソースと資料だけで進む調査。**下のプロンプトをそのまま
投げる。

```
ZMK / Zephyr / nRF52840 のソースと資料から、次の現象の原因の候補を
挙げてほしい。装置は使わない。机の上で完結する調査。

## 何が起きているか（実測・2026-08-10）

ZMK の分割キーボードの**セントラル側**（左）の消費電流が、触っていないのに
2 つの値を行き来する。

  高い状態  0.47mA
  低い状態  0.30mA        （分割リンクの接続間隔 15ms のとき）

- **切り替わりは 1〜2 秒で完了。**なだらかではない＝状態の切り替え
- **低い状態の長さ: 60 秒／72 秒**（2 回の観察。一定ではない）
- 高い状態の長さ: 60〜80 秒／3 分／4 分 20 秒／5 分 32 秒／6 分
- **どちらも一定ではない。周期性は確認できていない**
- 差 0.17mA は、ホストリンク 1 本ぶん（0.11mA・別途実測）と同じ桁

**同じ現象は、接続間隔を変えても出る。**低い側の値は 15ms 以上で
ほぼ床（0.28〜0.30mA）に張りつき、7.5〜13.75ms では 0.44〜0.49mA だった。

## 構成

  ボード    Seeed XIAO nRF52840（board: xiao_ble//zmk）
  ZMK       main（Zephyr 4.1 系）
  役割      左＝セントラル（分割リンク）＋ ペリフェラル（ホストとの HID）
  分割リンク interval 15ms / latency 30 / supervision timeout 4 秒
  ホスト    macOS。interval 15ms / latency 0 / timeout 720ms（実測）
  PHY       設定上は 2M（BT_AUTO_PHY_CENTRAL=2M）
  電源      単3 × 2 → ショットキー → 3V3 ピン直結。**USB は挿していない**
  キー      左は 2 個、右は 2 個。打鍵していない状態での観察

## すでに潰した候補

- **ZMK の電池報告**（`CONFIG_ZMK_BATTERY_REPORT_INTERVAL`・既定 60 秒）
  → **左右とも 10 秒に変えても、低い状態の長さは変わらなかった。**
  ただし報告を頻繁にすると高い状態が長引いたので、**無関係ではない**
- **BLE の接続パラメータの変化** → ZMK の DBG ログに `le_param_updated` も
  `connected` / `disconnected` も出ていない。
  **ただしこの否定は弱い**（下記）

## ログという道具の限界（**重要**）

ZMK は起動直後にしか喋らない。UART ログを Saleae で取ったが、
**5 分の取り込みでフレーム 0** だった。**「ログに何も出ていない」は
「何も起きていない」の証拠にならない。**

## やってほしいこと

**「セントラルの平均消費電流が 0.17mA 級で 2 段に変わりうる仕組み」を、
ソースの裏づけつきで列挙する。**時間の長さは一定ではないので、
**固定タイマーだけを探さないこと。**

見るべき筋（例。これに限らない）:

- **ペリフェラルが slave latency を使い始める／やめる**
  （相手が応答しなければ、セントラルの受信窓は早く閉じる）
- **コントローラのスケジューラ**（`BT_CTLR_SCHED_ADVANCED` など）が
  ホストリンクと分割リンクの重なりを解消／再発させる
- **アンカー点のずれ**で 2 つのリンクが重なったり離れたりする
- Zephyr / nrfx の電力管理（`PM_DEVICE_RUNTIME` など）
- ホスト（macOS）側の都合で送受信量が変わる

## 出力

  | 候補 | ファイル:行 または 資料 | 電流に効く筋道 | 長さが一定でないことと整合するか | 分ける実験 |

**「分ける実験」がいちばん重要。**設定を 1 つ変えて振る舞いが変わるか、が理想。

## 評価の基準（この案件で高くついた教訓）

- **「〜のはず」を書かない。**ソースの行か、資料の記述を示す
- **自分の生成物どうしの一致は検証ではない。**外の事実と突き合わせる
- **接頭辞・数値での雑な走査をしない。**単位（秒・ミリ秒・ティック）を
  確かめてから候補に入れる
- **設定しただけでは効いていない。**Kconfig に定義があることと、
  この構成で有効になっていることは別。依存関係をたどる

## 禁止

- **`CONFIG_BT_CONN_LOG_LEVEL_DBG` を勧めないこと。**この案件で実際に
  装置を壊した（133 回/秒のログで CDC のリングバッファが溢れ、1.48 秒で
  ログが止まり、ホストとの BLE 接続まで確立しなくなった）

**「見つからなかった」も結論として歓迎する。**その場合は、
探した範囲（どのリポジトリの、どのディレクトリを、どう検索したか）を書くこと。
```

#### いま焼いてあるもの（2026-08-10 時点・**次に測るときは焼き直す**）

| | |
|---|---|
| 左 | 15ms ＋ UART ログ ＋ 電池報告 10 秒（`e0546ac`） |
| 右 | 電池報告 10 秒（`a3a08e7`） |

**リポジトリからは一時的な設定をすべて外した。**次にビルドすれば
**7.5ms・ログ無し・電池報告 60 秒**（ZMK の既定）に戻る。

#### ⚠️ 分割リンクは左右差を生む。ホストリンクは生まない（2026-08-10・利用者の指摘）

**左のキーは分割リンクを通らない。**セントラルは左なので、左のキーは
そのままホストへ行く。

```
左のキー   デバウンス 3 ＋ ホスト待ち 7.5                  ＝ 10.5ms
右のキー   デバウンス 3 ＋ **分割待ち** ＋ ホスト待ち 7.5
```

| 分割の間隔 | 左 | 右 | **左右差** |
|---|---|---|---|
| **7.5ms（既定）** | 10.5ms | 14.25ms | **3.75ms** |
| 15ms | 10.5ms | 18.0ms | **7.5ms** |
| 30ms | 10.5ms | 25.5ms | **15ms** |
| 60ms | 10.5ms | 40.5ms | **30ms** |

**ホスト側を遅くしても左右差は増えない**（両手に等しくかかる）。

```
分割リンク   安い（1ms あたり 0.45 ヶ月）が、**左右差を生む**
ホストリンク 高い（同 0.16 ヶ月の見込み）が、**左右で公平**
```

**「安いほうから使い切る」では決められない。**軸が 2 つある。

> **この観点を、私（Claude）は見落としていた。**「分割のほうが 1ms あたりの
> 効率が 3 倍いいので、先に分割を使い切るのが筋」と提案していた。
> **効率だけを見て、非対称を見ていなかった。**

##### D2 の設計を変える

**今日の「60ms でも違和感なし」は、左右差を試していない。**打ったのは
右のタクト 2 個（`c` `d`）だけで、**左右を交互に打っていない。**

**人は絶対的な遅延には鈍く、差には敏感なことがある。**

→ **D2 は「速いか」と「左右がずれて感じるか」の両方を見る**（2026-08-10・
利用者の指摘。私は「ではなく」と書いていた）。**別の失敗の仕方なので、
片方だけ見るともう片方を見逃す。**

| 見るもの | どちらが効くか | 課題 |
|---|---|---|
| **絶対的な遅さ**（全体がもっさりするか） | **分割・ホストの両方** | ふだんの文章を打つ |
| **左右のずれ**（片手だけ遅れて感じるか） | **分割だけ** | `asdf` と `jkl;` を交互に打つ |

**どちらも目隠しで。**判定は 2 つ別に記録する。

#### 打鍵感については、まだ決めない

**60ms（合計遅延 40ms 級）でも「違和感なし」との報告。**480ms で初めて
明確に体感した。**閾値は 60〜480ms のどこか。**

**ただし、タクトスイッチ 2 個をつつくのと、キーキャップを付けて文章を
打つのは別物。**完成品で目隠しにして測り直す（D2）。**そのときは、上の
交換レートを持って判断できる。**

### 残っている調査（2026-08-09 に列挙）

**この節を書く過程で出て、まだ調べていないもの。**会話に置いたままだと
消えるので残す。**順は「安いものから」。**

#### ⚠️ まず 2 つ — **確かめずに書いたものが、結論として載っている**

| # | どこに | 何を書いたか | 何で確かめたか |
|---|---|---|---|
| **A** | `proto_split_left.conf` のコメント | **「ZMK は idle で BLE の接続間隔を伸ばして消費を下げる」** | **何も。**ただし 2026-08-10 に「触らずにいると電流が落ちることがある」を実測（上の D1 の「低い側」）。**下がる向きは実在するが、接続間隔が伸びているかは未確認** |
| **B** | この節「PHY は測らない」 | **「定常状態は 2M」** | **設定（`BT_AUTO_PHY_CENTRAL=2M`）だけ。**電波では見ていない |

**B は「自分の生成物どうしの一致」ではないので全くの無根拠ではない**
（Zephyr の Kconfig という外の事実を読んでいる）。**が、実際に 2M に
遷移したかは見ていない。**要素 2 に削りしろが無いという結論は、
ここに乗っている。

**A は根拠が無い。**消すか、測るかのどちらか。

#### そのほか

| # | 何 | なぜ要るか |
|---|---|---|
| **C** | **Windows でも latency 0 を返すか** | 下記。**本番機ができてから測る**（2026-08-09 に判断） |
| **D** | **分割リンクの間隔と体感**（D2） | **電流は測った（上の D1）。残るは体感だけ。**完成品で目隠しにする |
| **E** | 要素 3（送信電力）の実環境での余裕 | **本番の筐体がまだ無い。**筐体ができてから |
| **F** | 右の起動直後 5.5mA・未接続時 0.11mA の中身 | 値は記録したが、**何をしている電流かは調べていない**（[#26](open-gaps.md)） |
| **G** | **左をセントラルにした理由** | 記録が無いと分かっただけ。**入れ替える価値は未検討。**電池の減りが 16 倍違う側の話 |
| **H** | マンガン電池が全ケースで厳しい側か | 今回は厳しい側と確認済み。**全ケースでそうかは未確認** |

#### C（Windows）について — **本番機で測る。ただし「対策が無い」わけではない**

**macOS がワーストケースだとは確かめていない。むしろ逆かもしれない。**

Windows の BLE スタックは HID 機器に短い接続間隔を採るとされる。もし
7.5ms を選ぶなら:

```
macOS   15ms  →  67 回/秒
Windows 7.5ms → 133 回/秒   ← **倍。Mac より悪い**
```

**左の無線負荷は 200 → 266 回/秒。**電池寿命 4.1〜11.6 ヶ月は
**macOS で測った挙動から出した数字**なので、**Windows では仕様
（3〜8 ヶ月）を割る可能性がある。**

**打てる手はある。**ホストは私たちが出した範囲から選ぶので、
**希望の下限を上げれば、どのホストもそれより短くはできない。**

```
いま ZMK が希望する範囲   7.5ms 〜 15ms   （CONFIG_BT_PERIPHERAL_PREF_MIN_INT）
下限を 15ms に上げると    15ms 固定
```

**それでも本番機まで待つ**（2026-08-09 に判断）:

| | |
|---|---|
| 発注をせき止めない | ファームの設定。**基板ができてからでも直せる** |
| 本番のほうが正確 | ブレッドボードの XIAO 単体とケース入りでは条件が違う |
| 払うものが見えてから決めたい | 下限を上げると、ホストが短い間隔を選ぶ余地を潰す |

**そのときは Mac と Windows の両方で `le_param_updated` を読む。**

#### 道具の側で残っているもの

| | |
|---|---|
| ログが溢れる | 1Mbps でも起動直後は足りない（`--- 72 messages dropped ---`）。**起動直後を詳しく見るなら `CONFIG_LOG_BUFFER_SIZE` を上げる** |
| Saleae が落ちる | `OtherUsbError` が 2026-08-09 に 5 回。**USB を挿し直すと復帰するが、原因不明。**次の測定でも足を引っ張る |

### 遅延の鎖（要素 1 を判断するための土台）

```
キーを押す
 → デバウンス            3ms（設定値）
 → 分割リンクの待ち      0〜7.5ms（平均 3.75ms）  ← 要素 1 が触る場所
 → 左で HID を組み立て    ほぼ 0
 → ホストリンクの待ち    0〜?ms（**未測定**）      ← 触れない
 → ホストの処理
```

**ホストが 15ms なら、分割を 7.5ms にしても end-to-end で稼げるのは
平均 3.75ms だけ。**そのために左の分割リンクの電力を 2 倍払っている。
**割に合っているかは、打ってみないと分からない。**

### 参考: なぜ左をセントラルにしたか — **記録が無い**

計画書に「右には `CONFIG_ZMK_SPLIT_ROLE_CENTRAL` を書かない」とあるだけで、
**理由が残っていない。**ZMK の慣習に従っただけと思われる。

結果として生じている差（大きい順）:

| # | 差 |
|---|---|
| 1 | **電池の減りが 16 倍。**セントラル側だけ年 1 回交換、反対側はほぼ不要 |
| 2 | **セントラルが落ちると全体が死ぬ。**ペリフェラルなら半分は生きる |
| 3 | 相手が居ないときの浪費もセントラルが大きい（走査 3.7mA vs 広告 0.11mA・[#26](open-gaps.md)） |
| 4 | セントラルは接続を 2 本維持する。電波環境の要求が高い |
| 5 | ホストとのペアリング（5 プロファイル）を持つ。`&bt` 系のキーは左に置く |
| 6 | キー数は右が多い（34 vs 27）。ペリフェラルのキーが多いほど分割リンクを通る量が増える。影響は小さい |

**4 に良い符合がある。**アンテナの禁止域を入れられたのは左だけ（[#23](open-gaps.md)）。
**電波の条件が良いほうに、接続を 2 本持つ重い役が乗っている。**
理由は記録されていないが、結果としては筋が通っている。

**ファームだけで入れ替えられる**（基板は無関係）。ただし入れ替えても
**電池交換の負担が右手側に移るだけで、総量は変わらない。**

---

## 6. 判定

### 6-1. 疎通

**a・b・c・d の割り当てで実施**（Shift へ変える前）。

| 押すキー | 期待 | 実測（2026-08-09） |
|---|---|---|
| 左 10 列 | **a** | **a** ✅ |
| 左 14 列 | **b** | **b** ✅ |
| **右 10 列** | **c** | **c** ✅ |
| **右 14 列** | **d** | **d** ✅ |

**`c` と `d` が出れば、BLE 分割は成立している。** 右の XIAO は Mac と
直接つながっていないので、これは左を経由して届いたということ。

**✅ 合格。4 つとも出たので、以降の試験の対照実験も兼ねている。**

### 6-2. 左右同時押し（本命）

**`a` と `c` を同時に押す。** 両方出れば成立。

| 押すキー | 期待 | 実測（各 5 回） |
|---|---|---|
| 左 a ＋ 右 c を同時 | ac または ca（順不同） | **a=5 c=5** ✅ |
| 左 b ＋ 右 d を同時 | bd または db | **b=5 d=5** ✅ |
| 4 つ全部を同時 | abcd（順不同） | a=5 b=5 **c=3** d=5 ⚠️ |

### ⚠️ 4 つ同時で c が 2 回落ちた → **スイッチの浮きだった**

**「4 つ同時は押しにくいから」で片づけない。**候補は 3 つあった。

| 候補 | |
|---|---|
| 機械的 | 押すときスイッチがブレッドボードから浮く |
| 右半分 | c と d を同時に押すと片方を落とす |
| 分割の経路 | 右から左への伝送で落ちる |

**落ちた条件だけを取り出して測り直した**（各 10 回）。

| 試験 | 実測 | 意味 |
|---|---|---|
| **順に押して 4 つ保持** | **40 文字すべて `abcd`** | 順序まで乱れない |
| **右 c ＋ d だけ同時** | **c=10 d=10** | **右半分も分割の経路も落としていない** |
| 左 a ＋ b だけ同時 | a=10 b=10 | 分割を通らない対照実験 |

**2 つ目が決め手。**落ちた条件そのものが 10/10 なので、原因は**押したときに
スイッチが板から浮いていたこと**で確定。本番の基板では半田付けなので起きない。

**治具の都合で落ちたときほど、条件を分けて測り直す。**「押しにくいからだろう」
で通していたら、右半分か分割の経路の不具合を見逃していた可能性が残った。

**✅ 合格。**「順に押して保持」が 10 回とも順序まで揃ったことのほうが、
同時押しより実用に近い（タイピングは同時ではなく重なり）。

**順序が入れ替わっても問題ない。** 無線分割では周辺側に数ミリ秒の遅延が
乗るため、ほぼ同時に叩くと順番が前後することがある。仕様書にも
既知のリスクとして記載済み。

**問題になるのは「出ない」「重複する」「押しっぱなしになる」場合。**

### 6-3. 修飾キーの保持（最も実用に近い試験）

実際のタイピングで効くのは「修飾キーを先に押して保持したまま、
反対側の文字を叩く」形。これが崩れないことを確かめる。

1. **左 a を押したまま**、右 c を叩く → `ac` が出る
2. 押したままさらに右 d を叩く → `ad` が続く
3. 左 a を離す

**実測（2026-08-09）: `acd`。**左の a を保持したまま、右の c と d が
独立に届いた。**離しても `a` が流れ続けない。** ✅

> **`d` を押し続けると macOS のアクセント候補（`ď ð`）が出るが、これは
> macOS の長押し入力で、不具合ではない。**むしろ**押しっぱなしが途切れずに
> 届き続けている証拠**になる。しかも `d` は右半分のキーなので、
> 「右 → 左 → Mac」の経路が保たれていることを示す。
> 邪魔なら `defaults write -g ApplePressAndHoldEnabled -bool false`。

### 6-3b. 修飾キーそのもの（**本命。文字キーでは代用にならない**）

**HID のキーボードレポートでは、修飾キーは通常のキーとは別のバイトに載る。**
文字キー同士のロールオーバーが通っても、修飾キーの保証にはならない。
C2 で 200kHz のファームを焼いてやり直したのと同じ形。

そこで治具の左の 1 つ目を `&kp LSHFT` に変えた。**この割り当て自体が
対照実験になる**（同じ半分の中で効くか／またいで効くか）。

| # | 押すもの | 期待 | 実測（各 5 回） |
|---|---|---|---|
| 1 | 左 14 列だけ | `b` | **b×5** ✅ |
| 2 | 右 10 列 / 14 列 | `c` / `d` | **c=5 d=5** ✅ |
| 3 | **左 Shift ＋ 左 b**（対照） | `B` | **B×5** ✅ |
| 4 | **左 Shift ＋ 右 c（本命）** | **`C`** | **C×5** ✅ |
| 5 | Shift 保持で c → d | `CD` | **CD×5** ✅ |
| 6 | 4 つ全部を保持 | `BCD` | **BCD×5** ✅ |
| 7 | **Shift を離してから c** | **`c`（小文字）** | **c×5** ✅ |

**✅ 合格。4 が本命** — 左半分の Shift が右半分の文字に効いている。
**7 も重要** — 修飾キーの離しが伝わらないと以後すべて大文字になる。
文字キーなら 1 文字化けるだけだが、修飾キーは**離すまで効き続ける**ので
被害が違う。

### 6-4. 遅延の体感

右のキーを連打して、**引っかかりを感じるか**を見る。数値で測る手段は
無いので体感でよい。気になる場合は BLE の接続間隔を詰める余地がある
（`CONFIG_BT_PERIPHERAL_PREF_MIN_INT` 等を最短の 7.5ms 付近へ）。

**実測（2026-08-09）: 引っかかりは感じられなかった。** ✅
接続間隔を詰める必要は無い。

### 6-5. 距離と遮蔽

**左右の距離は利用者が決める。**分割キーボードは肩幅に合わせたり、机の
両端へ離したりするために分けるもので、**設計側が距離を仮定してはいけない**
（2026-08-09 に指摘された）。ここで測るのは「どこまで離せるか」であって、
「どれくらい離すか」ではない。間に手や腕が入るのは共通。

- 左右を **40cm** 離して打つ
- 左右の間に**手をかざした状態**で打つ

途切れなければよい。

### 実測（2026-08-09）

| 条件 | 結果 |
|---|---|
| **130cm・遮蔽なし** | **違和感なし** ✅ 手順書の 40cm を大きく超えている |
| 130cm ＋ **左をアルミ缶で包む** | 稀に右のキーが遅れる。c=16 d=42 と偏った |

**アルミ缶は実使用より遥かに厳しい**（電波を金属で囲う）。**130cm・遮蔽なしで
問題が無かったことが、意味のある結果。**利用者がどれだけ離すかは分からないので、
余裕がどこまであるかを測っておく意味がある。

金属で囲うと劣化することは分かった。**ケースの材質は 3D プリントの樹脂**なので
この条件は当てはまらないが、[§6-6](#6-6-電波の強さを測る子基板が届いた日にまとめて)
で子基板の GND ベタの影響を測るときに、劣化がどう見えるかの目安になる。

---

## 6-6. 電波の強さを測る（**子基板が届いた日に、まとめて**）

### なぜ測るか

子基板は **XIAO のアンテナの真下と周囲を GND ベタが覆い、アンテナから
0.5mm のところに FFC コネクタがあり、そこから 12 芯・50〜115mm の
ケーブルが伸びる**設計（open-gaps #23）。電波には不利な条件が重なる。

### ⚠️ 日を分けて測っても比較にならない

**最初この手順は「いまブレッドボードで基準値を取り、あとで子基板と
比べる」と書いていた。それは誤り。**日を分けると、変わるのは子基板の
有無だけではない。

- その日の電波環境（WiFi・他の BLE 機器・人の出入り）
- スマホの位置と向きの再現性
- 部屋の金属の配置

さらに**子基板ができたときには、それはケースの中で電池と本体基板の隣に
ある**。組み立て全体の影響を測ることになり、**20dB 落ちても何が原因か
分からない**。子基板を作り直しても直らないかもしれない。

### 正しいやり方: 同じ日に、変数を 1 つずつ足す

**すべて同じ日・同じ場所・続けて測る。**各段階で足す変数は 1 つだけ。

| 段階 | 構成 | この段階で分かること |
|---|---|---|
| **① 基準** | XIAO 単体（ブレッドボード。ベタもケーブルも無い） | 最良の値。以降の原点 |
| **② ＋子基板** | XIAO を子基板に載せる。**ケーブルは繋がない・ケースに入れない** | **GND ベタの影響だけ** |
| **③ ＋ケーブル** | FFC ケーブルを繋ぐ（本体基板には繋がなくてよい） | **ケーブルの影響だけ** |
| **④ ＋ケース・電池・本体基板** | 実際に組み立てた状態 | 実使用の値 |

**①〜④ を続けて測るから比較になる。**①を今日測って④を来月測るのでは、
差がどこから来たのか永久に分からない。

**今日やるべきは測定ではなく、測り方の確認。**スマホの BLE スキャナで
XIAO が見えるか、RSSI がどれくらいふらつくかを見ておく。当日に
「そもそも測れない」となるのを防ぐため。

### 測り方（①〜④ 共通）

スマートフォンの BLE スキャナ（nRF Connect など）で RSSI を読む。

**固定するもの**（変えると比較にならない）:

- 距離 **30cm**（メジャーで測り、机に印を付ける。①〜④ で同じ印を使う）
- 向き（XIAO のアンテナ端をスマホへ向ける）
- 高さ（同じ机の上）
- **スマホは手で持たない。**体が電波を吸う。台に置いて動かさない
- 周りの金属を動かさない

**読み方**: 10 秒ほど眺めて**最強値と最弱値**を記録する。RSSI は常に
ふらつくので 1 回の値では判断できない。**ふらつきの幅より小さい差は、
差ではない。**

### 記録欄（**同じ日に上から順に**）

| 段階 | 最強 | 最弱 | ① との差 |
|---|---|---|---|
| ① XIAO 単体 | dBm | dBm | — |
| ② ＋子基板 | dBm | dBm | dB |
| ③ ＋FFC ケーブル | dBm | dBm | dB |
| ④ ＋ケース・電池・本体基板 | dBm | dBm | dB |

### 判定

**①→② の差**（GND ベタの影響）で子基板の改版を判断する。
④ の値には電池もケースも混ざるので、**改版の判断材料にはしない**。

| ①→② の差 | 判断 |
|---|---|
| ふらつきの幅以内 | **影響なし。そのまま進む** |
| 〜10dB 悪化 | 想定内。**そのまま進む** |
| 10〜20dB 悪化 | 改版の価値あり。③④ と実使用を見て判断 |
| **20dB 以上悪化** | **子基板を改版する。**アンテナを基板の縁から張り出させ、FFC を USB 側へ移す |

**②→③ が大きい場合はケーブルが主因**なので、子基板の改版では直らない。
ケーブルの引き回し（アンテナから離す・向きを変える）で対処する。

### 併せて実使用で確かめる

左右を**利用者が実際に使う間隔**に置き（決め打ちにせず、近い・遠いの両方を
試す）、**手を鍵盤に乗せた状態**で高速に
打鍵し、取りこぼしや切断が起きないか。人体は 2.4GHz をよく吸うので、
RSSI が良くても実使用で落ちることがある。

### 参考: 余裕は大きい

2.4GHz の自由空間損失と BLE の受信感度（-90dBm）から計算した余裕。

| 距離 | 受信レベル | 余裕 |
|---|---|---|
| 0.3m（左右間） | -30dBm | **60dB** |
| 2.0m | -46dBm | **44dB** |

**20〜30dB 劣化してもまだ 30dB 残る。**測るのは「動くか」ではなく
**「どれだけ悪いかを知って、改版するかを決める」**ため。

---

## 6-6b. 発注前に測る（**子基板が無くても測れる。費用 ¥0**）

> **✅ A〜F すべて、いつやってもよい（2026-08-11 に確認）。**
> ここには「C 以降は open-gaps #29 のあとで」と書いてあったが、
> **#29 は 2026-08-09 に対応済み**で、そのとき基板・子基板・XIAO の配置は
> 動かなかった（動いたのはチルト脚のピン長と上ケースの座ぐりだけ。
> どちらも電波に関係しない）。**待つ理由はもう無い。**
>
> **B から始めること。**B に出てくるのは **XIAO とリード線だけ**で、
> B が答えるのは**机上では判定できない唯一の点（共振）**。

**「発注をまとめる以上、届くまで測れない」は誤りだった**（open-gaps #23）。
測りたいのは電波の**絶対値ではなく、金属を近づけたときの差**で、
その差を作っているのは**金属そのもの**であって、基板である必要が無い。

**そして発注してからでは直せない。**子基板の配線は XIAO の本体の下に隠れ、
予備の板にも同じ配線が乗っている。**戻す手段は再発注だけ（$25〜35・2〜3 週間）。**

### 用意するもの（**買わない。家にあるもので足りる**）

| 要るもの | どうする |
|---|---|
| XIAO 2 個 | C3 の治具のもの |
| スマホの BLE スキャナ | nRF Connect など。§6-6 と同じ |
| **リード線 100mm 1 本** | 手持ち。**これがいちばん大事** |
| アルミホイル（15cm 角くらい） | 台所 |
| 単3 電池 2 本 | 手持ち |
| 定規 | 距離を測る |

### スペーサーは「厚紙」でなくてよい

**金属でなければ何でもよい。**大事なのは**厚みが分かっていること**だけ。

| 欲しい厚み | 代用できるもの |
|---|---|
| **約 4mm** | **コピー用紙を重ねる（1 枚 0.09mm → 45 枚）**／付箋 1 束／段ボール 1 枚／プラ定規を数枚重ね／レゴのプレート／割り箸 |
| **約 1.6mm** | コピー用紙 18 枚／名刺 6 枚／プラ定規 1 枚 |

**ぴったりでなくてよい。**4mm のつもりが 3.5mm でも、**測って記録してあれば読める**
（しかも薄いほうが厳しい側なので、安全側に外れる）。

### 段階（**同じ日に・同じ場所で・上から順に**）

測り方（距離 30cm・向き・スマホを置いたまま・最強と最弱を記録）は
**[§6-6](#6-6-電波の強さを測る子基板が届いた日にまとめて) と同じ**。

| # | 構成 | 何が分かるか | 要るもの |
|---|---|---|---|
| **A** | XIAO 単体 | 原点 | ― |
| **B** | **＋ GND のパッドにリード線をはんだ付けし、アンテナの脇を通して伸ばす。長さを 3 通り（50 / 100 / 150mm）試す** | **★ いちばん知りたいこと。**FFC ケーブルが共振してアンテナを引っぱるか | リード線 |
| **C** | ＋ アルミホイル（10cm 角）を**アンテナの横 6mm に立てる**（縁をアンテナへ向ける） | 本体基板の地板の**縁**の影響 | ホイル |
| **D** | ＋ 単3 電池 2 本を**アンテナの横 10mm** | 電池の影響 | 電池 |
| **E** | B＋C＋D を全部 | 実機に近い状態 | |
| **F** | 参考: ホイルを**アンテナの真上 4mm に水平に置く** | **作り直す前の姿。**今回の変更がどれだけ効いたか | ホイル＋スペーサー |

**スペーサーが要るのは F だけ。**A〜E はホイルを立てる／横に置くだけなので不要。

> **B を最初に置いているのは意図的。**面（ベタ）はもう設計から消してあり、
> 残っているのは 0.2mm の線 2 本と、その先の 100mm の FFC ケーブル。
> **面積の議論では判定できないのはここだけ**なので、いちばん先に測る。

### 記録欄

| # | 構成 | 最強 | 最弱 | A との差 |
|---|---|---|---|---|
| A | XIAO 単体 | dBm | dBm | — |
| B-1 | ＋リード線 50mm | dBm | dBm | dB |
| B-2 | ＋リード線 100mm | dBm | dBm | dB |
| B-3 | ＋リード線 150mm | dBm | dBm | dB |
| C | ＋ホイルを横 6mm | dBm | dBm | dB |
| D | ＋電池を横 10mm | dBm | dBm | dB |
| E | 全部 | dBm | dBm | dB |
| F | 参考: ホイルを真上 4mm | dBm | dBm | dB |

### 判定

| 見るもの | 判断 |
|---|---|
| **B が 3 本とも、ふらつきの幅以内** | **共振していない。**いちばん大きな未知が消える |
| **B が長さによって大きく変わる（1 本だけ悪い等）** | **共振している。**長さに敏感 ＝ 最も危ない形。**FFC の長さを設計で決め打ちにできない** |
| **B が 3 本とも同じくらい悪い（10dB 以上）** | 共振ではなく、**近くに導体があること自体**が効いている。引き回しで離す |

> **長さを 3 通り試すのは、あとで FFC の長さが変わっても使える結果にするため。**
> `FFC_LENGTH = 100mm` は暫定値で、#29 で配置が変われば動く。
> **1 本だけ測ると、その長さでたまたま良かった／悪かったのかが分からない。**
| C・D が大きい | 幾何の問題。ケース側で離せないか見る |
| **E の差が 20dB 以上** | **発注しない。**#23 の案 A（USB を内部で延長）か案 B（モジュール変更）へ戻る |
| F と E の差 | 今回の作り直し（アンテナを基板の下から出した）が何 dB 買ったか。**記録に残す** |

**この結果を open-gaps #23 に書けば、あの節は閉じられる。**

---

## 6-7. 判定（2026-08-09）

**✅ C3 は合格した。BLE 分割は成立する。**

| # | 確認対象 | 結果 |
|---|---|---|
| 1 | 左右がペアリングして、右のキーが左を経由して届く | ✅ `c` `d` が出た |
| 2 | **左右同時押しが崩れない** | ✅ 順に押して 4 つ保持が 10 回とも `abcd` |
| 3 | 遅延が体感できる水準でない | ✅ 引っかかりなし |
| — | **左 Shift ＋ 右手の文字**（仕様の必須要件） | ✅ `C` が出た |
| — | 修飾キーの離しが伝わる | ✅ 離すと小文字へ戻る |
| — | 距離 | ✅ 130cm・遮蔽なしで問題なし |

**残っているのは [§6-6](#6-6-電波の強さを測る子基板が届いた日にまとめて)（電波の強さ）だけ**で、
これは子基板が届いた日にまとめて測る。

### 引っかかったこと

**ペアリングで 2 度詰まった。**どちらも「見つかるのに繋がらない」で、
片方だけが古いボンドを持っている状態だった。

**`settings_reset` を焼くのが唯一の手段**（この治具にはレイヤーが無く
`&bt BT_CLR` を割り当てたキーも無い）。**片方だけ消すと、もう片方が
取り残される**ので、詰まったら**左・右・ホストの 3 者を全部消す。**

**新品でも中古でも、まず `settings_reset` を通してから始めるほうが速い。**

---

## 7. うまくいかないとき

### 右のキーがまったく出ない

1. **左右を取り違えていないか。** 両方に左用を書き込むと、2 台とも
   セントラルになってペアリングしない
2. 右の電源が入っているか（USB を挿しているか）
3. 右単体で C1 のファームを入れて、スイッチが反応するか確かめる
   → 反応しなければ配線の問題で、BLE は無関係

### 左のキーは出るが右が出ない

BLE のペアリングが成立していない。両方を同時にリセットする。
それでも駄目なら、両方のフラッシュを一度消して書き直す。

### 数秒おきに途切れる

距離を縮めて再現するか確認する。近くても途切れるなら、
**USB ハブや他の 2.4GHz 機器の干渉**を疑う。Mac に直挿しで試す。

---

## 8. このタスクで確定しないこと

- **乾電池での駆動** — Task C4。ここでは USB 給電で構わない
- **61 キーでの動作** — 方式が成立すればキー数は問題にならない
- **電池の持ち** — Task C4・C5

---

## 9. 不成立だった場合

仕様書では「左右間の無線は必須要件であり、有線化では逃げない」としている。
それでも成立しない場合に取りうる手は次の順。

1. **BLE の接続間隔を詰める**（遅延が原因のとき）
2. **アンテナの向き・位置を変える**（途切れが原因のとき）。XIAO の
   アンテナは基板の USB と反対側の端にある。金属や電池で覆わない
3. ここまでで駄目なら、**設計そのものを見直す**。TRRS 有線は
   常用構成として想定していないが、最終手段としてフットプリントは残す

**1・2 を試さずに 3 へ飛ばないこと。**
